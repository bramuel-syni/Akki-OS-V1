# Orchestrator Continuity Protocol (RMS Intelligence System)

> Purpose: after compaction, the orchestrator reads this file FIRST and follows the checklist before saying anything to the user or dispatching work. This file is the single source of truth for "where are we".

## 0. Immutable Ground Rules (never violate)
- Contract-First Hierarchy: `/app/backend/contracts/` schemas are FROZEN. Tests bend to contracts, never the reverse.
- **Rule 2 v2** (Substrate-Drop v1, 2026-07-01): Reshape-to-rewrite STOP applies when net-new LoC exceeds `lifted-verifiable` (kind ∈ {direct, transitive}). `unverifiable-substrate-absent` LoC stays in the manifest for provenance but is EXCLUDED from the lifted total for ratio purposes. Rule 2 is a **stop-and-judge trigger**, not an automatic shrink cap — when a ratio is high, the response is narrow-ratify-with-documentation (like Northena §12 mandate-forced scaffolding at G2a close), not force-the-number-down.
- **Discretionary-enumeration discipline** (added 2026-07-02T00:45Z at G6 close, following G5a ledger-completion pushback): Every phase report must include the full net-new-discretionary LoC enumeration INLINE. Confirming that the audit route serves live numbers is insufficient — the report itself must carry the numbers AND per-line rationale (file:line + one-line description + honest ratify rationale) at close. Aggregation is not allowed; every discretionary line is enumerated. If discretionary LoC == 0 across a phase, prove via §-anchor coverage per module.
- **Substrate-drop gate** (Substrate-Drop v1, 2026-07-01): A phase does not open until `phase_source_requirements` for that phase are all present in `/app/docs/mandates/` with matching SHA-256 in `MANIFEST.md`. The `backend/tests/invariants/test_substrate_drop_gate.py` invariant enforces this at CI time. If a required spec is missing or its hash mismatches, CI fails and the phase is blocked.
- Legacy repo `Akki-Executive-New-Arch` is UNREACHABLE from pod. Do NOT clone/probe. Use transitive-lift-with-manifest via `/app/docs/lift_manifest.json`.
- 4 HAZARD-STOPS (only reasons to break autonomy mid-phase):
  (a) frozen contract must mutate
  (b) governance decision needed (Owner/DPO)
  (c) substrate absent (log as unverifiable-substrate-absent)
  (d) Rule 2 trips
- Solva Boundary (when G3 opens): build `conclusion_class(load_bearing_units)` FIRST, no confidence params, freeze signature via `test_conclusion_class_signature.py`.
- **Read-only route invariant** (G5a): trace-lens + lift-manifest routes write zero rows to any persistent store; enforced by `test_trace_lens_readonly.py`.
- **Outer-gate irreversibility invariant** (G6): egress carries no plaintext identifiers; mint keys purged at end of window; enforced by `test_outer_gate_irreversibility.py`.
- **V2 refusal terminality** (G6): V2 refusal envelope IS the record; no partial-egress ever; enforced by `test_v2_gate_refusal_cumulative.py`.
- **Frozen-contract snapshot parity invariant** (Substrate-Drop v2, 2026-07-03): every declared-frozen contract source under `/app/backend/contracts/` has exactly one canonical `.contract_snapshot.json` under `/app/backend/tests/invariants/`, and vice versa; bijective mapping in `test_frozen_contract_snapshot_parity.py::CONTRACT_TO_SNAPSHOT`. Enforced by `test_every_frozen_contract_has_snapshot` + `test_every_snapshot_maps_to_a_contract` + `test_snapshot_mapping_is_bijective`. Load-bearing rule preventing the 2026-07-03 discovery class of drift (14 declared / 11 enforced under canonical name).

## 0.1. Standing Owner Dispositions (never re-litigate; log-only)

- **Ruling 2 (Substrate-Drop v2, 2026-07-03) — Literal-widening on a frozen contract is a HAZARD-STOP by definition.** Widening a `Literal[...]` on a frozen contract changes its validation surface and breaks the byte-identical snapshot — mutation wearing addition's clothes. Any phase whose design requires literal-widening: HAZARD-STOP (a) immediately, propose a NEW contract instead (the 13→14 precedent, `Service1Refusal@v0` at A2, is the standing pattern).
- **Ruling 4 (Substrate-Drop v2, 2026-07-03) — Uniform disposition on v3 §10 open items.** None gates a dispatch. All land as config swaps when owner rules.
  * **Model form** (v3 §6.5) → Phase 3 builds the REFUSAL, not the feature.
  * **Pricing values** (v3 §8) → Phase 6 ships the MECHANISM + `price-model@v0-exploratory` as config swap.
  * **Refused-after-acceptance commercials** (v3 §7 + §8) → Phase 5 builds the late-refusal ENVELOPE regardless; commercial terms are policy config.
  * **Retention window** (v3 §7.3) → honest default: indefinite, surfaced on DPO surface. Owner decisions land as config swaps.
- **Ruling 5 (Substrate-Drop v2, 2026-07-03) — v3 §6.3 (knowledge artifact) and §6.4 (callable skill) are CONFIRMED AS WRITTEN.** No longer `[STAKED]`. Do not re-litigate their shape at their phases. Real material is incoming owner-side. The perception-path phase carries instrumentation in its gate: GPU-hours per broadcast-hour per modality, unit yield, cost per qualified unit.
- **Elevated Doctrine (Substrate-Drop v2, 2026-07-03) — Validation surface IS contract surface.** The pre-G2 hardening on `NormalizedUnit` added a `model_validator` that changed effective validation without altering `five_rings.contract_snapshot.json` — a backdoor mutation path on a frozen contract's behaviour. Same class of failure as literal-widening. Any future validator, `Config` change, or import-time schema mutation touching a frozen contract requires an explicit re-snapshot bless or is a HAZARD-STOP.
- **Loose-as-frozen (Substrate-Drop v2 close, 2026-07-03) — deliberate under-determination is not a HAZARD.** The HAZARD-STOP-NOTES on `ObjectiveRequest v2.Reach.depth`, `Envelope.budget`, `Envelope.scope_ceiling`, `Envelope.availability_snapshot`, `Envelope.floor_feasibility`, `Envelope.committed_at` are RULED RESOLVED. Ranges are learned from practice + real material, not invented at freeze time. Freezing a guessed enum would be fabrication. Hardening lands as a NEW frozen contract version when ranges are known; in-place narrowing of any frozen field is mutation → HAZARD-STOP (a) by definition. Standing pattern going forward: when v3 or successor specs declare a field without narrowing its scalar type, freeze permissively per v0-precedent-anchored default; owner rules on narrowing via new contract addition.
- **Floor-feasibility shared-derivation (Substrate-Drop v2 → Phase 1 dispatch, 2026-07-03) — Ruling 4.** `floor_feasibility` is a governed value recorded in a frozen envelope; two consumers (wizard + admission) computing it independently is a second-computation-path (the A2 `supported_class` lesson exactly). Derivation from `class_distribution × requested_standard` lives in ONE shared function at `services/mtafiti/floor_feasibility.py::derive_floor_feasibility`. Reimplementation per consumer fails review regardless of output equality. Enforced by `tests/invariants/test_floor_feasibility_shared_derivation.py` with three grep-negative regression patterns.
- **Config-as-versioned-not-frozen (Substrate-Drop v2 → Phase 1 dispatch, 2026-07-03) — Ruling 3.** `feasibility-config@vN` follows the control-surface pattern: versioned (`v0.json`, `v1.json`, …), recorded, reversible. Byte-freezing an administrable value (freshness threshold, apportionment fractions, etc.) makes it immutable — the exact thing the `qualification_matrix` shape-classes-plus-config pattern was structured to avoid. Term 2 precedent rules: shape freezes, values version. If a config SCHEMA ever becomes load-bearing later, follow the shape-classes-plus-config precedent; the VALUES never freeze. Standing pattern for future config surfaces (`price-model@vN`, `fleet-policy@vN`, etc.).
- **Admission-refusal reason codes: extend by ADDITION via versioned registry (Phase 3 dispatch, 2026-07-03).** `AdmissionRefusal@v0.reason` is typed as constrained `str` (snake_case pattern), NEVER `Literal`. The valid-reason SET lives in a plain versioned registry `services/service_1/admission_refusal_reasons.vN.json` (following `qualification_matrix` + `feasibility-config@vN` precedents). Adding a new admission-refusal reason = REGISTRY bump (v0→v1 or append-only entry per config policy), NEVER a contract file modification, NEVER a Literal-widening. Snapshot stays byte-identical when a reason is added. Preserves Ruling 2 (no Literal-widening on frozen contract), Elevated Doctrine (validation surface = contract surface — registry lives OUTSIDE contract), and Term 2 (shape freezes, values version) simultaneously. Enforced by `test_admission_refusal_reason_extension_via_registry_bump` + `test_admission_refusal_contract_docstring_states_extension_path`. **Applied at Phase 4a Stage B close, 2026-07-04:** registry bumped v0→v1 additively (3 new codes: `grain_form_incompatible`, `standard_below_admission_floor`, `license_class_unavailable`) with v0.json byte-identical + AdmissionRefusal@v0 contract snapshot byte-identical.
- **§6.1 payload container: UNFROZEN by named wire-shape gate (Ruling 3, Phase 4a Stage B dispatch, 2026-07-03).** The §6.1 qualified-data payload container (three top-level keys `units`/`receipt`/`unit_count` + `computed_at`) does NOT get its own frozen contract. Container refactor that drops or renests `receipt` ships a deliverable without its outer-gate receipt — governance regression with no snapshot to catch it. Condition: a LOAD-BEARING gate `test_qualified_data_wire_shape_pins_governance_keys` pins the wire (three top-level keys present + `receipt` parses as `OuterGateReceipt_v0` + every unit carries its `defensibility` field). Governance rides on inner frozen shapes (`OuterGateReceipt_v0`, defensibility from Ring 5 vocabulary). Without the gate, the container would have to freeze (Candidate 1).
- **License-class Phase 7 seam pre-committed in module docstring (Ruling 4, Phase 4a Stage B dispatch, 2026-07-03).** `services/service_1/license_class_selection.py` module docstring documents (verbatim) the Phase 7 landing plan: when the shaping wizard lands, the negotiated `license_class` arrives on the objective via a versioned frozen-contract addition (`WizardCommitState_v0` or similar sidecar); at that point, `derive_license_class_from_commissioner` becomes the FALLBACK ARM of a single derivation function `derive_license_class(objective)` with two arms (explicit-value-if-present → primary; commissioner-derived → secondary); ONE site (this module), Ruling 4 shared-derivation unchanged. Enforced by `test_license_class_selection_phase_7_seam_documented` (docstring grep for "Phase 7 seam pre-committed" + "fallback arm"). Owner's Ruling 4 counter-verdict acknowledged inline: Option C is identity-proxy default — honest only because no use-purpose field exists yet anywhere; Phase 7's landing bounds the posture.
- **Implementation close reports land as on-disk canonical + SHA + inline (Phase 4a Stage B remediation, 2026-07-04).** Verbatim Owner directive: *"Implementation close reports (2026-07-04 onwards): every phase close lands its close report as on-disk canonical at `/app/docs/close_reports/<phase>_close.md`, with SHA-256 quoted in the return message AND full-text artifacts inline in the message body. Relay channels drop content; disk doesn't. Standing rule from Phase 4a Stage B onwards."* Applies to all future phase-close reports; the Stage A pattern (on-disk canonical + inline delivery) is now the standing rule for implementation phases, not just design phases. First application: `/app/docs/close_reports/phase_4a_stage_b.md` SHA-256 `f5bb38e7d25b3e295bb38aec24bf6e46404bb164ab0b9a5cd639c451234eb866`.
- **Frozen-field-changes-as-new-versions** — [Owner ruling, Phase 5 Stage A close, 2026-07-04] *"the outcome of Opt 4 via the doctrinal path: northena_ledger_row@v1 as a new frozen contract version. In-place widening of the Literal (Opt 4 as written) is mutation — the Phase 0 loose-as-frozen ruling already settled that changes to frozen fields land as new versions, never in-place."* — **Retroactively citation-audited 2026-07-04 (Phase 6 Stage B) per new meta-doctrine `Disposition-must-cite-owner-ruling`; original inline body preserved verbatim below.** Frozen contracts NEVER mutate. Any change to a frozen field — including a Literal-widening, a new field, a validator change, or a Config change — lands as a NEW contract version FILE, not an in-place edit of the prior version. The prior version file remains byte-identical forever (guarded by the byte-identity regression suite at each phase close). Standing pattern going forward: `<name>_v1.py`, `<name>_v2.py`, etc., each with its own snapshot in the mechanical parity map. **First application: Phase 5 Stage B, 2026-07-04** — `NorthenaLedgerRow_v1` landed as contract 19; `NorthenaLedgerRow_v0` (contract 6) remains byte-identical at SHA `68349bb0...`. v1 supersets v0's validation set (every valid v0 row also validates under v1) AND adds `terminate_cancelled` to the Literal decision axis. Enforced by `test_prior_contract_file_byte_identical_after_5b` (parametrised over 18 files) + `test_northena_ledger_v1_supersets_v0` + mechanical parity invariant at 20 entries. **Second application (Phase 6 Stage B, 2026-07-04):** `AsyncDeliveryAccepted_v1` landed as contract 22 (narrows `quote: Optional[Any]` → `Optional[QuoteEnvelope_v0]`); `AsyncDeliveryAccepted_v0` (contract 20) remains byte-identical at SHA `fc495b76...`. Guarded by `test_prior_contract_file_byte_identical_after_6b` (parametrised over 20 prior files) + `test_async_delivery_accepted_v1_supersets_v0` + mechanical parity invariant at 22 entries.
- **Infra-not-refusal** — [Owner ruling, Phase 5 Stage A close, 2026-07-04] *"async_queue_saturated — struck as a refusal code; it's a 503. Saturation is the system unable, not unwilling — an infra condition, not a governance decision, and the three-render-path doctrine forbids dressing infra as refusal."* — **Retroactively citation-audited 2026-07-04 (Phase 6 Stage B) per new meta-doctrine `Disposition-must-cite-owner-ruling`; original inline body preserved verbatim below.** Infrastructure faults return HTTP 503 (or 5xx family), NEVER a governed refusal envelope. Refusals are governance decisions (with `reason` + `what_you_can_do` actor-appropriate path forward); 503 is an infrastructure decision (retry-later signal). Struck code at Stage B: `async_queue_saturated` (was a candidate refusal reason at Stage A design; STRUCK at Stage B open as violating this Disposition). Standing pattern: substrate saturation, database unavailability, worker crashes, external-API timeouts — all infra; all 5xx; never refusals. Enforced by `test_no_async_queue_saturated_code_in_any_registry` (grep-negative across all four registries: `admission_refusal_reasons.v0/v1/v2.json` + `service_1_refusal_reasons.v0.json`) + `test_queue_overflow_raises_503_not_refusal` (LOAD-BEARING; queue full → HTTPException 503, response body does NOT carry `outcome=refused`). **Distinction retained at Phase 6 Stage B:** `fleet_policy_reserved_zero_capacity` is a GOVERNANCE refusal (Master Admin unwilling to allocate) @422 AdmissionRefusal_v0, NOT infra (503) — enforced by `test_fleet_capacity_governance_refusal_uses_admission_refusal_v0` (LB) + `test_queue_saturation_returns_503_not_refusal` (both distinct outcomes preserved).
- **Cancellation-is-a-state-not-a-refusal** — [Owner ruling, Phase 5 Stage A close, 2026-07-04] *"The decisive defect in Argument A: forcing a mid-running cancel into Service1Refusal_v0 means populating asked / supported_class / what_would_raise_it for an event that has none of those semantics — fabricated fields on a governed envelope, the exact A2 failure class."* — **Retroactively citation-audited 2026-07-04 (Phase 6 Stage B) per new meta-doctrine `Disposition-must-cite-owner-ruling`; original inline body preserved verbatim below.** Cancellation is a 5TH TERMINAL STATE (`cancelled`), NOT an admission refusal. State machine at Stage B is `{accepted, running, delivered, refused, cancelled}` — five states, three terminals (delivered/refused/cancelled). Refusal envelopes carry `outcome=refused` + `reason` + `what_you_can_do` (governance shape). Cancellation envelopes are THIN — exactly 4 keys: `objective_id`, `status="cancelled"`, `trace_id`, `cancelled_at` — no claim content, no path forward (caller already chose to abort). Struck code at Stage B: `caller_cancelled` (was a candidate refusal reason at Stage A design; STRUCK as a REFUSAL REASON CODE — but the STRING `caller_cancelled` remains legitimate as a state-transition reason label AND as a ledger-row `reason` prefix, e.g., `caller_cancelled:cancelled_at_state=running` on the `NorthenaLedgerRow_v1(decision="terminate_cancelled")` emit). Standing pattern: any caller-driven or infra-driven termination that is NEITHER "produced the deliverable" NOR "governance-refused the deliverable" is a distinct terminal state, not a refusal-with-path. Enforced by `test_no_caller_cancelled_code_in_any_registry` (grep-negative on refusal-reason CODES across 4 registries) + `test_cancelled_run_no_partial_egress` (LOAD-BEARING; thin envelope, forbidden claim-content keys absent) + `test_cancelled_run_is_ledgered` (LOAD-BEARING; emit `NorthenaLedgerRow_v1(decision="terminate_cancelled")`) + `test_state_machine_five_states_declared` + `test_state_machine_no_transition_from_terminal`.
- **Ruling — Inline delivery scope amended (Owner ruling, 2026-07-04, landed at Phase 6 Stage A close).** *"On-disk canonical + SHA quoted in the return message is the authoritative close record. Inline full-text is mandatory only for two content classes: (a) ruling-conditioned artifacts (the items acceptance was explicitly conditioned on) and (b) any new Standing Disposition text. Everything else is referenced by SHA. This matches the failure evidence: four recurrences of full-report inline drops through the summarising finish wrapper; zero recurrences on bounded artifact pastes. Standing rule from Phase 5 Stage B close onwards, superseding the Phase 4a Stage B inline-all requirement to this narrower scope. The Phase 4a Stage B rule (on-disk + SHA + inline) remains historically anchored for phases 4a/4b/5-Stage-A/5-Stage-B; the amendment applies from Phase 6 onwards."*
- **Disposition-must-cite-owner-ruling** — [Owner ruling, Phase 6 Stage B dispatch, 2026-07-04] *"Every Standing Disposition must cite the owner ruling it transcribes; the agent cannot mint doctrine, only transcribe it. A disposition without a citation-line is void by construction until re-landed with a verbatim owner-ruling quote."* NEW META-DOCTRINE landed at Phase 6 Stage B open. **Structural rule:** every entry in this §0.1 section MUST open with an italicised citation header line of the form `[Owner ruling, <phase-context>, <date>] "<verbatim quote>"` before its explanatory body. Retroactive audit at first application (Phase 6 Stage B, 2026-07-04): the three Phase 5 Stage A dispositions above (Frozen-field-changes-as-new-versions, Infra-not-refusal, Cancellation-is-a-state-not-a-refusal) were void by construction until the citation-line was re-landed with verbatim quotes from the Owner's Phase 5 Stage A close (2026-07-04); all three re-landed above with citation headers preserved.
- **Inline-delivery-scope-amended-v2** — [Owner ruling, Phase 6 Stage B dispatch, 2026-07-04, response to previous fork's inline-drop recurrence] *"Only ruling-conditioned artifacts and new Standing Disposition texts are pasted inline in the close return; everything else is referenced by on-disk canonical SHA. Full implementation bodies are NOT inlined. This matches the failure evidence: four recurrences of full-report inline drops through the summarising finish wrapper; zero recurrences on bounded artifact pastes."* v2 amendment (Phase 6 Stage B open, 2026-07-04) to the v1 Inline-delivery-scope-amended ruling. **Structural rule:** phase close return messages carry (a) the on-disk canonical path + SHA-256 as authoritative record, (b) ruling-conditioned artifacts pasted inline verbatim, (c) new Standing Disposition texts pasted inline verbatim, (d) EVERYTHING ELSE by SHA reference only. Applied from Phase 6 Stage B close onwards; supersedes v1 for the paste-scope width question.
- **Sizing-anchor-declares-snapshot-inclusion** — [Owner ruling, Phase 6 Stage B close, 2026-07-04] *"every future Stage A sizing anchor states explicitly whether machine-generated snapshots are in or out of its band — settle it at sizing time, never at close."* NEW STANDING DISPOSITION landed at Phase 7 Stage A close. **Structural rule:** every Stage A sizing anchor from Phase 7 onwards MUST include an explicit `snapshot_lloc_in_band: yes | no` declaration. Ambiguity at sizing time is a scheduled HAZARD-STOP at close. Prior phases whose bands were set without this declaration used the implicit "snapshots-in-band" convention (established at G6 and confirmed at Phase 6 Stage B close); that convention becomes the default when a Stage A anchor omits the declaration. First application: Phase 7 Stage A proposal (2026-07-04) declares `snapshot_lloc_in_band: yes` for all three anticipated Stage B sub-stages (Stage A §7.4).
- **Inline-delivery-scope-amended-v3** — [Owner ruling, Phase 6 Stage B close, 2026-07-04] *"Dev-inline full-text has now failed every single attempt across two content classes. A delivery requirement with a 100% failure rate is ritual, not discipline. Standing rule v3: on-disk canonical + SHA per close; close summaries carry one-line enumerations of new dispositions."* NEW STANDING DISPOSITION landed at Phase 7 Stage A close; RETIRES inline-full-text requirement. **Structural rule:** every close (a) on-disk canonical `/app/docs/close_reports/<phase>.md` OR `/app/docs/stage_a_proposals/<phase>.md` with SHA quoted in return; (b) close summary enumerates new Standing Dispositions as **title + `[Owner ruling, <phase-context>, <date>]` citation header + one-line quote** (this is the channel that has never dropped); (c) Owner-side push at each accepted close (Owner responsibility); (d) Owner audits full texts from repo directly. Supersedes v1 (Phase 4a Stage B) and v2 (Phase 5 Stage B / Phase 6 Stage A) inline-mandatory language for phases from Phase 7 onwards. The structural citation rule `Disposition-must-cite-owner-ruling` (Phase 6 Stage A close) STAYS LOAD-BEARING — a disposition citing no owner ruling is void by construction, which is what makes audit-not-gate safe. First application: Phase 7 Stage A close (2026-07-04) — this proposal delivered per v3 (one-line enumerations only in return; full text on disk at `/app/docs/stage_a_proposals/phase_7_stage_a.md`).
- **Agent-pluggable-with-stub-agent-first** — [Owner ruling, Phase 7 Stage A close, 2026-07-04] *"That's not a workaround — it's the right proof order: the mechanical guards (source-tagging, ask-slot structure) get proven independent of any prompt before an LLM ever sits behind the interface. If a guard only works with the LLM present, it was prompt discipline wearing a gate."* NEW STANDING DISPOSITION landed at Phase 7 Stage B-1 close (2026-07-04). **Structural rule:** any new agent-driven pipeline lands its mechanical guards + tests against a `DeterministicStubAgent` (or equivalent no-LLM implementation) FIRST; the LLM plugs behind the same Protocol interface at a subsequent sub-stage without touching guard tests. First application: `services/wizard/agent_interface.py::DeterministicStubAgent` at Phase 7 Stage B-1, 2026-07-04. Enforced by `test_no_second_llm_judge_in_wizard_pipeline` (grep-negative on LLM SDK imports across `services/wizard/*` — currently 0 matches, must remain 0 through B-1 close and re-verify at B-2 open).
- **Visibility-not-prohibition** — [Owner ruling, Phase 7 Stage A close, 2026-07-04] *"Visibility is the guard; prohibition would be the wrong guard."* NEW STANDING DISPOSITION landed at Phase 7 Stage B-1 close (2026-07-04). **Doctrinal principle:** when a seam risks laundering (agent-supplied laundering as operator-supplied; or a proposal that steers a buyer without disclosing the delta) the mechanical guard is VISIBILITY — the delta is surfaced on-wire (e.g. `{price_delta, class_delta}` on the buyer proposal payload) so the caller sees the change and can accept/reject with knowledge. Prohibition (blocking the proposal entirely) is the wrong guard because it forbids helpful steering (sales) that Owner has ratified as legitimate (see v3 §3.3 buyer variant "may propose"). First mechanical application (buyer proposal payload with `{price_delta, class_delta}` fields) PRE-COMMITTED for Phase 7 Stage B-2 landing under the buyer state machine.

## 0.2. Plan Debts (log-only; the phase pays the debt at its own dispatch)

- **§6.1 downgrade plan-debt** (Substrate-Drop v2 close, 2026-07-03) — **RESOLVED at Phase 4a Stage B close, 2026-07-04**: Phase 4a Stage B landed §6.1 as three-item extension surface exactly as the debt anticipated (grain dispatch via shared `grain_compatibility.py`, license_class via `license_class_selection.py` + `license_classes.v0.json`, standard hard input filter inside `qualified_data._standard_hard_filter`). All three items surfaced as ADDITIVE additions to `admission_refusal_reasons.v1.json` (Ruling 7 unified `grain_form_incompatible`, `standard_below_admission_floor`, `license_class_unavailable`) — zero new frozen contracts, parity stays 17.
- **Phase 3 model-refusal plan-debt** (Phase 2 close, 2026-07-03) — **RESOLVED at Phase 3 close, 2026-07-03**: v3 §6.5 (`model` form off-menu refusal) — resolved by landing `AdmissionRefusal@v0` as the 17th frozen contract. Owner redefined the Phase 3 receiver from a model-specific refusal contract to the UNIFIED admission-refusal envelope (Phase 3 dispatch ruling), with `form_not_offerable` as the FIRST firing reason. Future admission-refusal reasons extend via the versioned reason registry (new Standing Owner Disposition in §0.1). Phase 2's scaffold placeholder for `form == "model"` was REPLACED (Condition 5 migration) by an `AdmissionRefusal@v0` emission at HTTP 422 in the same v2 dispatch code path.
- **Phase 4 transform-variants plan-debt** (Phase 2 close, 2026-07-03; restated Phase 3 close, net-of-refusal-work; **split Phase 4a/4b at Stage A close 2026-07-03; 4a portion RESOLVED at Phase 4a Stage B close 2026-07-04**): v3 §6.1 (qualified_data) DONE via Phase 4a (§6.1 landed live: warm+qualified_data → 200 with UNFROZEN `QualifiedDataPayload` container + inner-frozen `OuterGateReceipt_v0`; three additive reason codes via v1 registry bump; MODEL-cell defense-in-depth per Ruling 5; Phase 7 seam pre-committed per Ruling 4; wire-shape gate per Ruling 3). §6.2 (composed_conclusion) + 18th frozen contract `ComposedConclusion_v0` remain as **Phase 4b debt (Ruling 2 confirmed at Stage A close)**. §6.3/§6.4 (knowledge_artifact/callable_skill — Ruling 5 CONFIRMED AS WRITTEN, not to be re-litigated) still emit Phase-2 scaffold placeholders at 4a; land as separate future dispatch if/when owner directs.
- **Phase 5 async-delivery plan-debt** (Phase 2 close, 2026-07-03): v3 §7 — Phase 2 dispatch emits a placeholder naming this debt on the fresh-fork path (`route_target=admission_fresh_fork_via_northena_admit`). Phase 5 lands the `POST /api/objectives` async envelope (202 accepted + state machine + webhook + idempotency-key check).
- **Phase 7 wizard-side dispatch plan-debt** (Phase 2 close, 2026-07-03; **restated Phase 4a Stage B close 2026-07-04 with license-class fallback-arm wrap; PARTIALLY RESOLVED at Phase 7 Stage B-1 close 2026-07-04; buyer-variant + LLM portion RESOLVED at Phase 7 Stage B-2 close 2026-07-04**): v3 §3.3 — the shaping wizard invokes `compute_feasibility` per-turn (guard 3). **RESOLVED at Phase 7 Stage B-1 close, 2026-07-04:** operator-variant state machine + Guard 1/2/3 + source-tag XOR invariant + license-class Option C wrap landed. `services/service_1/license_class_selection.py::derive_license_class(envelope, wizard_state=None)` primary arm reads FROZEN wizard state (Owner E1 branch-discrimination LB gate). **RESOLVED at Phase 7 Stage B-2 close, 2026-07-04:** buyer state machine + dual-delta gate (Owner E6 `Visibility-not-prohibition` mechanical application) + Sonnet 4.6 LLM (`SonnetWizardAgent` inside `services/synisense/shield/llm_router.py`) landed. Owner Condition-A pre-LLM Guard-1 mandatory-tier gate on `POST /api/wizard/operator/{sid}/agent-assumption` (3 LB gates: refuse mandatory-tier operator-variant / never-mint operator-source CommittedValue / never-append OperatorTurn). Owner Condition-2 grep-negative single-source × 3 symbols (`validate_source_tags`, `validate_guard_1_operator_mandatory_all_operator_supplied`, `_record_feasibility_snapshot`) — buyer SM imports each from operator-proven module. Zero new frozen contracts (parity 26 unchanged); zero new §0.1 Standing Dispositions (§0.1 FROZEN per Owner correction). **REMAINING at Phase 7 Stage B-3:** commit-review + buyer freeze + admission handoff to `POST /api/objectives`. **Additional Phase 7 receiver debt (Phase 3 close, 2026-07-03):** wizard-side rendering of `AdmissionRefusal@v0` as REFUSAL-WITH-PATH per UI Spec §3.3 (refusal, never error) — contract shape is Phase-7-consumable unchanged (Condition 4). **Additional Phase 7 license-class debt (Phase 4a Stage B close, 2026-07-04):** wrap `derive_license_class_from_commissioner` as the FALLBACK ARM of a new unified `derive_license_class(objective)` with explicit-value-if-present (from wizard commit state, likely `WizardCommitState_v0` sidecar) as primary arm; ONE site (still `services/service_1/license_class_selection.py`), Ruling 4 shared-derivation unchanged. Pre-committed verbatim in module docstring per Ruling 4. **RESOLVED at Phase 7 Stage B-1 close, 2026-07-04:** additive `derive_license_class` two-arm function landed; fallback body byte-identical (slice SHA `ca3b2007f0cee58da3de0562eea3e92492761cda95a8297e632b5346b8d0e41e`).
- **Phase 8 Stage A frozen-contract trajectory restatement plan-debt** (Phase 7 Stage B-1 close, 2026-07-04) — **RESOLVED at Phase 8 Stage A dispatch, 2026-07-04**: prior PM anchor "~22-23 frozen contracts through Phase 8" exceeded at B-1 (parity 26). Per Standing Disposition `Sizing-anchor-declares-snapshot-inclusion`, Phase 8 Stage A restates the trajectory + declares `snapshot_lloc_in_band` per anticipated sub-stage. Restated end-of-Phase-8 parity: **26 (unchanged) OR 27 (only if Escalation E4 lands `EngineerKeyGrant_v0` at Stage B-3)**. Per-sub-stage snapshot declarations: B-1 `no`, B-2 `no`, B-3 `conditional (no default; yes if E4 lands)`, B-4 `no`, B-5 `no`. Resolution on-disk at `/app/docs/stage_a_proposals/phase_8.md` SHA-256 `4e4dd82ae2834f7c680429e1b2771221566d2bdc810e04444b722afda7b5c188`.
- **Phase 8c DPO `wizard_transcript` held-class enumeration** — DPO surface must expose the `wizard_transcript` held-class as **separately-addressable**, so DPO can enumerate and manage this retention class independently of other held-classes. Lands with Phase 8c dispatch under DPO-surface scope. [Owner ruling, Phase 7 B-1 close (E5 seam), 2026-07-04; refined at Phase 8 Stage A dispatch, 2026-07-04]
- **Wizard session-ownership binding plan-debt** — [Owner ruling, Phase 7 Stage B-2 dispatch, 2026-07-04] *"Wizard session-ownership binding lands with Phase 8 auth/key model — recorded as the system-wide auth landing, not a wizard-special."* **RESOLVED at Phase 8 Stage B-1 close, 2026-07-05**: `services/auth/session_binding.py` (69L) lands the `wizard_session_id → identity_id` sidecar table (`db.wizard_session_bindings` unique index on `session_id`) + `bind_session_to_identity` + `get_bound_identity` + `check_binding` primitives with grandfathering carve-out for pre-B-1 sessions. Router-decorator wiring (403 on identity-mismatch via `auth_refusal.emit("auth_identity_mismatch_for_wizard_session")`) is a Phase 8 B-2 surface-scoped mechanical follow-up when the operator wizard becomes authenticated. Close report: `/app/docs/close_reports/phase_8_b_1.md` SHA-256 `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`.
- **Envelope-shim helper triad extraction** — the triad in `services/wizard/admission_handoff.py` (`compose_objective_request_from_frozen_state`, `compose_objective_request_from_frozen_state_with_proposals`, `summarise_dual_deltas`) is duplication-by-design at B-3; extraction to a shared module lands with the Phase 8 backend-refactor slot. Dev flag transcribed verbatim per Owner Decision 3. [Owner ruling, Phase 7 Stage B-3 close acceptance, 2026-07-04] **RESOLVED at Phase 8 Stage B-1 close, 2026-07-05**: `services/wizard/router_shims.py` (159L) is the canonical single-source; `services/wizard/admission_handoff.py` reduced to 38L pure re-export shim (BC preserved for Phase 7 B-3 invariant tests). Grep-negative gate parametrised over 3 triad symbols × 2 routers × 1 gate = 6 GREEN gates confirming neither `routers/wizard_buyer.py` nor `routers/wizard_operator.py` locally defines any triad symbol. Close report: `/app/docs/close_reports/phase_8_b_1.md` SHA-256 `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`.

## 1. Rehydration Checklist (run in order after compaction)
1. Read this file end-to-end.
2. Read `/app/BUILD_JOURNAL.md` — last 300 lines.
3. Read `/app/memory/PHASE_STATE.md` (see §3 below).
4. Read `/app/docs/lift_manifest.json` — get current lifted-vs-net-new LoC totals (v1 accounting).
5. Read `/app/docs/mandates/MANIFEST.md` — confirm canonical specs are on-disk.
6. Read `/app/docs/g4_prep/OPEN_GOVERNANCE.md` — pending Owner/DPO/MEA decisions.
7. Read the latest prep doc for the next phase (e.g. `/app/docs/g3_prep/solva_prep.md`).
8. Read `/app/docs/audits/substrate_drop_v1/` — the four reconciliation docs (Substrate-Drop v1).
9. Run `cd /app && make ci` — confirm current green test count matches PHASE_STATE.md.
10. ONLY THEN respond to the user.

## 2. Phase Ledger (append-only, most recent last)

> Note on estimate framing: any duration / credit / turn number referenced in this ledger or downstream is a **Provisional planning anchor — not a commitment. Relative weight only.** (Norm added at Substrate-Drop v1.)

> Note on LoC columns: only G2a has published per-phase lifted/net-new totals (see BUILD_JOURNAL 2026-07-01T08:30Z "Post-shrink Rule 2 LoC ledger"). Earlier phases froze contracts + reshaped pipeline modules but did not publish per-phase Rule-2 tallies at close; those are marked `UNKNOWN` honestly. Rolling lifted/net-new totals available per-module via `/app/docs/lift_manifest.json` entries.

| Phase | Status | Green tests at close | Lifted LoC | Net-new LoC | Ratio (v1 accounting) | Journal ref |
|---|---|---|---|---|---|---|
| G0 | CLOSED | UNKNOWN (tester 3/3 PASS marker only) | UNKNOWN | UNKNOWN | UNKNOWN | BUILD_JOURNAL 2026-06-30T19:30Z |
| G0.5 | CLOSED | UNKNOWN | UNKNOWN | UNKNOWN | UNKNOWN | BUILD_JOURNAL 2026-06-30T19:30Z (opened) → 2026-07-01T00:00Z (pre-G2 handover) |
| G1 | CLOSED | UNKNOWN | UNKNOWN | UNKNOWN | UNKNOWN | BUILD_JOURNAL §G1 (multiple entries pre-2026-07-01T00:00Z) |
| Pre-G2 | CLOSED | 61 | UNKNOWN | UNKNOWN | UNKNOWN | BUILD_JOURNAL 2026-07-01T00:00Z |
| G2a | CLOSED | 73 (at initial close); post-shrink follow-ups added tests to 73→118→146→149 | 127 (all verifiable-transitive; 0 unverifiable) | 344 (170 discretionary + 174 mandate-§12-forced) | **2.71× overall / 1.34× discretionary-only** (v1 == v0 for G2a) | BUILD_JOURNAL 2026-07-01T07:10Z / T08:30Z (post-shrink) — discretionary counted under pre-§0 standard |
| Substrate-Drop v1 | CLOSED | 158 | 0 (no engine code) | 0 | N/A (docs+CI-invariant phase, no LoC change) | BUILD_JOURNAL 2026-07-01T15:39Z |
| G2b | BLOCKED (awaiting real RMS material Hour A/B/300-unit) | — | — | — | — | — |
| G3 | CLOSED | 211 | 98 (all verifiable-transitive, in-pod; 0 unverifiable) | 437 (~2 discretionary + ~435 mandate-forced per Solva Spec §7–§18) | **4.46× overall / ~0.02× discretionary-only** (v2 accounting) | BUILD_JOURNAL 2026-07-01T16:00Z–T17:15Z — discretionary counted under pre-§0 standard |
| G4 | CLOSED | 271 | 268 (all verifiable-transitive, in-pod; 0 unverifiable) | 785 (~0 discretionary + ~785 mandate-forced per Mtafiti §7 + Targeta §7 + Product v2.1 §2.1) | **2.93× overall / 0.00× discretionary-only** (v2 accounting) | BUILD_JOURNAL 2026-07-01T17:30Z–T18:55Z — discretionary counted under pre-§0 standard |
| G5a | CLOSED | 301 | 159 (transitive: Motor cursor pattern + FastAPI APIRouter boilerplate + Pydantic contract scaffolding + _fact_unit fixture-schema mirror + httpx.AsyncClient test transport; in-pod) | 715 (18 discretionary + 697 mandate-forced per Interface Spec §16 invariant #9 + Northena §7.2/§7.3/§12/§14 + Product v2.1 §8) | **4.50× overall / 0.11× discretionary-only** (v2 accounting, honest line-by-line audit; initial close numbers 30/864/0 → 28.80×/0.00× corrected in ledger completion pass) | BUILD_JOURNAL 2026-07-02T00:00Z + 2026-07-02T00:15Z — discretionary counted under pre-§0 standard |
| G5b | NOT STARTED | — | — | — | — | Frontend: Operator Console 4 surfaces + Consumer Terminal v0 |
| G5b | CLOSED | 359 (backend unchanged) + 12/12 frontend gate | 0 (API-consumption, no source lift) | 1848 (all discretionary — frontend-only) | N/A (no source lift) | BUILD_JOURNAL 2026-07-02T10:00Z |
| G6 | CLOSED | 340 | 80 (transitive: Pydantic contract scaffolding + absorb-function shape rhyme with converge.absorb_solva_trace + test infra transitive from G5a test suites; in-pod) | 1176 (64 discretionary + 1112 mandate-forced per Product v2.1 §21.2 + §22.1 + §29.1 + §30 + §31 Sys-Invariant #8 + §32 + Northena §14 stamp-audit side-channel) | **14.70× overall / 0.80× discretionary-only** (v2 accounting; ratify rationale filed inline per new §0 discretionary-enumeration discipline; ratio high because G6 built new crypto primitives with modest transitive lift — cousin_lineage documented in mint.py docstring though manifest lint keeps mint entry mandate-forced) | BUILD_JOURNAL 2026-07-02T00:45Z — discretionary under strict §0 standard (post-§0-strict) |
| Substrate-Drop v2 (Part 1) | CLOSED | 373 (367 → +6: 3 backfill freeze + 3 parity) | 0 (docs+CI phase) | 0 (backfill by-copy; no source-code lift) | N/A | 2026-07-03 — canonical-name uniformity for `.contract_snapshot.json`; mechanical parity invariant `test_frozen_contract_snapshot_parity.py` landed; snapshot count 11 → 14 |
| Substrate-Drop v2 (Part 2 / Phase 0) | CLOSED | 374 (373 → +1: v2 snapshot test) | 0 (net-new contract; ObjectiveRequest v0 lifted for `DefensibilityFloor` import + shape reference) | ~180 (all mandate-forced per v3 §3.2 verbatim; ~15 discretionary flagged inline as HAZARD-STOP-NOTES for owner narrowing on `depth`, `budget`, `scope_ceiling`) | ~12× overall / ~0.08× discretionary-only (v2 accounting, post-§0-strict) | 2026-07-03 — 15th frozen contract `ObjectiveRequest v2`; v0 byte-identical (SHA `2588c735...`) before AND after |
| Phase 1 — Estate Feasibility Query | CLOSED | 387 (374 → +13: 4 honesty + 1 schema-freeze + 1 readonly + 3 dual-consumer + 4 shared-derivation) | ~30 (opcounters read-only pattern from `test_trace_lens_readonly.py`; ledger-shape from mtafiti registry) | ~490 (16th frozen `FeasibilityResult v0` + compute service + shared derivation + endpoint + 5 test files; ~85 discretionary framing enumerated inline) | ~5.7× overall / ~0.30× discretionary-only (v2 accounting, post-§0-strict) | 2026-07-03 — 16th frozen contract `FeasibilityResult v0`; snapshot count 15→16 via mechanical parity invariant unbroken; §6.1 CONFORMS→EXTENDS verdict recorded as plan-debt (§0.2); Ruling 4 shared-derivation invariant landed; Ruling 3 `feasibility-config@v0-provisional` shipped as plain versioned config (NOT snapshotted). Route: `POST /api/mtafiti/feasibility`. |
| **Phase 2 — Shape-Responsive Execution Scaffold** | CLOSED | 402 (387 → +15: 4 named gates + 5 positive-path + 2 wire-shape + 3 v0-untouched-regression + 1 malformed-body wire-shape) | ~40 (feasibility read-only pattern from Phase 1; ASGITransport pattern from `test_v0_service_1_route`; ObjectiveRequest_v2 shape lifted from Phase 0) | ~460 (dispatch module + v2 route additive edit + 2 test files; ~110 discretionary framing enumerated inline in close report: constant naming, `DispatchResult` field-order, trace_id prefix, route URL `/v2/dispatch`, gate-test docstrings) | ~4.2× overall / ~0.24× discretionary-only (v2 accounting, post-§0-strict) | 2026-07-03 — 0 new frozen contracts; snapshot count remains 16; mechanical parity invariant unbroken. v3 §4 four bullets covered per owner scope declaration (entry dispatch, warm/fresh fork, reach passthrough, output-form routing). Gates 1-4 GREEN; v0 SHA-untouched (contract `2588c735...`, service `05e905ed...`). Route: `POST /api/service_1/v2/dispatch` (501 + placeholder). Deferred bullets logged in §0.2. |
| **Phase 3 — Admission-Refusal Envelope (unified §6.5 + future admission reasons via registry)** | CLOSED | **413** (402 → +11: 1 contract-frozen + 2 registry-governance + 1 firing + 1 actor-appropriate + 1 family-consistency + 1 Phase-2→Phase-3 migration + 1 placeholder-vs-refusal separation + 1 Service1Refusal-untouched + 1 docstring-extension-anchor + 1 reason-pattern-snake-case; also updated 2 Phase-2 gate tests per Condition 5 migration — placeholder iteration list narrowed; MODEL positive-path renamed to `_routes_to_admission_refusal`) | ~35 (Service1Refusal family pattern lifted from A2; registry pattern lifted from qualification_matrix + feasibility_config; ASGITransport pattern lifted from Phase 2) | ~470 (17th frozen contract `AdmissionRefusal_v0` + registry JSON + `admission_refusal.py` service module + dispatch integration + 2 Phase-2-test migrations + 1 new invariant + 1 new gate-tests file; ~85 discretionary framing enumerated inline: docstring wording, off_menu_fact string, what_you_can_do string, snake_case pattern regex tightness, response body 422 status vs body-only settlement, gate-test naming) | ~13.4× overall / ~0.19× discretionary-only (v2 accounting, post-§0-strict) | 2026-07-03 — 17th frozen contract `AdmissionRefusal_v0`; snapshot count 16→17 via mechanical parity invariant unbroken; NEW Standing Owner Disposition landed at §0.1 (admission-refusal reasons extend via versioned registry, never Literal-widening); Phase 3 debt RESOLVED at §0.2; Phase 4 debt RESTATED as net-of-refusal-work (Phase 4 §6.1 hard-input-filter refusal → additional REASON CODE in existing envelope, not new contract). Phase 2 `form=model` scaffold placeholder REPLACED (Condition 5) by AdmissionRefusal_v0 emission at HTTP 422. Route: `POST /api/service_1/v2/dispatch` now returns Union[DispatchResult @501, AdmissionRefusal_v0 @422]. |
| **Phase 4 Stage A — Transform Layer design proposals (design-only, no implementation)** | **CLOSED** | **413** (unchanged; docs-only touch) | N/A (design-only) | N/A (design-only; Stage B does the LoC accounting) | N/A | 2026-07-03 — INITIAL CLOSE VACATED: first close was verdict-summary only, not the artifact-delivery Stage A dispatch required. Second close (same day) delivered Sections 1–8 with full-text artifacts: §6.1 payload freeze proposal (Artifact 6, NEW section 7), ComposedConclusion_v0 full schema + three D4b axes argued verbatim (Artifact 1), license_class three-option comparison A/B/C + Option C concrete (Artifact 2), full grain_compatibility.py pure-function module + guard test (Artifact 3), 4a/4b file-by-file split with concrete import lines (Artifact 4), full scope table with v3 line-anchored dispositions + two counter-verdicts (license_class Option C sidesteps owner's three field-entry candidates; §6.1 payload UNFROZEN not scoped by owner) (Artifact 5), B1–B4 honesty recheck post-full-text inspection. Verdicts: 4a/4b SPLIT (XL ~1310 net-new); ComposedConclusion@v0 FREEZE (18th, at 4b); §6.1 payload UNFROZEN at 4a (D4b bar not met — container of already-frozen inner shapes). No Standing Owner Disposition changes. Substrate-drop gate 9/9 GREEN. CI 413/413 GREEN. Parity 17 entries. Owner rules on Stage A shape + split before Stage B dispatch. |
| **Phase 4a Stage B — §6.1 qualified-data path + shared substrates (implementation)** | **CLOSED** | **434** (413 → +21: 3 grain-compat + 3 license-class-taxonomy + 6 qualified-data-selection + 2 outer-gate-ride/wire-shape + 4 dispatch-grain-refusal + 1 v0-paths-byte-identical + 2 fold-ins including Ruling 3 wire-shape LOAD-BEARING + Ruling 4 Phase-7 seam docstring + Ruling 5 MODEL-cell defense-in-depth) | ~120 (feasibility read-only pattern lifted from Phase 1; outer_gate transform_artifact + build_receipt reused unchanged per Condition B3; ObjectiveRequest_v2 shape lifted from Phase 0; floor_feasibility._CLASS_ORDER imported directly per Ruling 4 shared-derivation) | ~840 (5 NEW source files: grain_compatibility.py 138L, license_classes.v0.json 34L, license_class_selection.py 116L, admission_refusal_reasons.v1.json 26L, qualified_data.py 260L; 3 MODIFIED source files: admission_refusal.py +171/-9 net +162, dispatch.py +73/-31 net +42, routers/service_1.py +31/-10 net +21; 6 NEW test files 1087L; 1 Phase-2 test migration test_dispatch_shape_responsive.py +30/-17 net +13 per Condition-5; ~250 discretionary framing enumerated inline in close report: reason-code string wording, off_menu_fact/what_you_can_do actor-appropriate phrasing per Condition 3, license_classes.v0.json feed_id mapping specifics, qualified_data payload field-order & description strings, MintWindow open-per-call approach, per-unit vs batch _transform_meta stripping, run_id "qd-run-" prefix, artifact_ref version="v2", gate-test docstrings, seed-row region==reach convention preserved from Phase 1) | ~7× overall / ~0.30× discretionary-only (v2 accounting, post-§0-strict); ~950 LoC band target → ~840 actual → **-12% delta UNDER band, no restatement needed** | 2026-07-04 — Phase 4 debt SPLIT closed on 4a arm: §6.1 (qualified_data) live @ v2 dispatch (warm+qualified_data → HTTP 200 with `QualifiedDataPayload` container UNFROZEN per Ruling 3 wire-shape gate + inner-frozen `OuterGateReceipt_v0`). §6.2 (composed_conclusion) + 18th freeze `ComposedConclusion_v0` remain 4b debt. Zero new freezes at 4a (parity stays 17). Three additive reason codes via v1 registry bump (v0.json byte-identical; AdmissionRefusal@v0 snapshot byte-identical `99381316...`). Three rulings landed inline: Ruling 3 wire-shape LOAD-BEARING gate, Ruling 4 Phase-7 seam docstring pre-commitment, Ruling 5 MODEL-cell defense-in-depth. Three new Standing Owner Dispositions at §0.1: (a) registry-bump-additive-extension applied verbatim; (b) §6.1 payload UNFROZEN by named wire-shape gate; (c) license-class Phase 7 seam pre-committed in module docstring. Substrate-drop 9/9 GREEN. Mechanical parity 3/3 GREEN at 17. v0 SHA-identity confirmed on 7 files: objective_request.py `2588c735...`, service_1/service.py `05e905ed...`, service_1_refusal.py `4fe38c21...`, admission_refusal.py `e68a1e38...`, outer_gate/transform.py `90907d22...`, outer_gate/mint.py `01cfe0e0...`, outer_gate/receipt.py `4591e5ff...`. Route: `POST /api/service_1/v2/dispatch` now returns Union[DispatchResult @501, AdmissionRefusal_v0 @422, QualifiedDataPayload @200]. **Delivery history correction (2026-07-04):** Inline close delivery did not reach owner thread on first attempt (relay-channel content drop); re-emitted on owner request with three specific artifacts (A: R3 wire-shape gate full test file text, B: R5 MODEL cells post-fix verbatim, C: R4 module docstring verbatim) plus on-disk canonical landing at `/app/docs/close_reports/phase_4a_stage_b.md` SHA-256 `f5bb38e7d25b3e295bb38aec24bf6e46404bb164ab0b9a5cd639c451234eb866`. This delivery history is recorded — not papered — per Owner's continuity-correction directive. NEW Standing Owner Disposition §0.1 landed same-day: implementation close reports from 2026-07-04 onwards land as on-disk canonical + SHA + inline. |
| **Phase 4b — §6.2 composed-conclusion path + 18th frozen contract (`ComposedConclusion_v0`)** | **CLOSED** | **446** (434 → +12: 2 contract-freeze/parity + 2 Condition-B1 AST/grep-negative + 6 §6.2 dispatch tests + 2 v0-SHA-identity tests) | ~230 (Solva `conclusion_class` boundary reused UNCHANGED per Condition B1; Service1Refusal shape lifted from v0 service.py's Service1Refusal exception class; Northena LedgerRow structure lifted from `services/northena/ledger.py`; qualified_data substrate reused for reach filter + license-class filter; AdmissionRefusal emit-helpers reused; refusal_hints static table reused; grain-compat matrix reused via dispatch layer per Ruling 4 shared-derivation) | ~640 (2 NEW contract-side files: composed_conclusion.py 178L + composed_conclusion.contract_snapshot.json 64L; 1 NEW service file: services/service_1/composed_conclusion.py 329L; 3 MODIFIED files: contracts/__init__.py +3, services/service_1/dispatch.py +21/-2, routers/service_1.py +63/-18; 4 NEW test files 667L; 1 MODIFIED test file: test_frozen_contract_snapshot_parity.py +1; ~105 discretionary framing enumerated inline in close report: field descriptions per contract, module docstrings citing §6.1.6-vs-§6.2.6 reading, `_UnitView`/`_DefensibilityView` minimal-shape dataclass design, answer_text scaffold stub wording, Northena ledger reason payload structure, gate-13 regex construction) | ~2.8× overall / ~0.28× discretionary-only (v2 accounting, post-§0-strict); ~810 LoC band target → ~640 actual → **-21% delta UNDER band, no restatement needed** | 2026-07-04 — Phase 4 debt SPLIT fully closed (4a + 4b arms). §6.2 composed_conclusion live @ v2 dispatch (warm+composed_conclusion → HTTP 200 with `ComposedConclusion_v0` frozen 18th contract; below-floor at conclusion class → HTTP 422 via `Service1Refusal_v0(reason=composition_below_floor)` per §6.2.6). ComposedConclusion@v0 snapshot SHA-256 `a85eaf95349befdacdaf6d88804474df137299a6250cc5e8cababb2670fb00fb`. Mechanical parity 17→18 GREEN. Substrate-drop 9/9 GREEN. v0 SHA-identity verified on all 7 protected files (regression from 4a) + 17 prior frozen contract sources byte-identical. Wire Union widened per Stage A settled table: `Union[DispatchResult @501, AdmissionRefusal_v0 @422, ComposedConclusion_v0 @200, Service1Refusal_v0 @422, <§6.1 payload dict> @200]`. Ruling 3 wire-shape gate `test_qualified_data_wire_shape_pins_governance_keys` VERIFIED GREEN post-widening (fold-in verification per Owner's proportionate call replacing the parked three-artifact verification). Condition B1 LOAD-BEARING at gate 13: composed_conclusion.py imports `conclusion_class` from Solva, calls it (dead-import-guard PASSED), no local recomputation site (AST + grep-negative sweep across services/ + routers/ PASSED). v3 §6.1.6-vs-§6.2.6 reading landed: §6.1.6 hard input filter applies to §6.1 qualified_data path ONLY; §6.2 enforces standard at conclusion class per §6.2.6 (documented in composed_conclusion.py module docstring). Close report on-disk canonical: `/app/docs/close_reports/phase_4b.md` SHA-256 `2781313b28e152277e41f135801e4f5e3f0a3b083aa50a53ef3ab634c9cfb6c7`. |
| **Phase 5 Stage A — Async Delivery Contract design (design-only, no implementation)** | **CLOSED** | **446** (unchanged; docs-only touch) | N/A (design-only) | N/A (design-only; Stage B does the LoC accounting) | N/A | 2026-07-04 — Design-only Stage A landed at `/app/docs/stage_a_proposals/phase_5_stage_a.md`. Owner returned 4 rulings + 3 Standing Owner Dispositions verbatim at Stage B open: (a) five-state ruling (`cancelled` is a 5th state, not a refusal); (b) frozen-field-changes-as-new-versions (`NorthenaLedgerRow_v1` at Phase 5 Stage B); (c) infra-not-refusal (`async_queue_saturated` STRUCK); (d) 20th frozen contract `AsyncDeliveryAccepted_v0`. All 3 Dispositions landed in §0.1 at Stage B close. |
| **Phase 5 Stage B — Async delivery §7 implementation (async admission + 5-state machine + webhook + cancellation)** | **CLOSED** | **504** (446 → +58: 34 Stage-B gate roster [G1–G24 + 5-state + grep-negatives + contract-freeze + idempotency + registry-additive] + 20 byte-identity regression [18 prior contracts + 1 count + 1 synthesis anchor] + 4 net after Phase-2/4a test migrations [5 tests replaced by Phase-5 equivalents]) | ~340 (asyncio.Queue pattern from std lib; HMAC-SHA256 signing pattern from `services/synisense/trust_receipt.py`; Mongo async client pattern from `services/service_1/service.py`; FastAPI APIRouter pattern from existing routers; Pydantic contract-file scaffolding from prior v0 files; ObjectiveRequest_v2 lifted from Phase 0; Service1Refusal exception pattern from A2) | ~2274 (source ~1256 + test ~1018): 2 NEW frozen contracts (northena_ledger_v1.py 80L + async_delivery_accepted.py 50L); 9 NEW service/router files (async_state 306L + async_worker 197L + webhook 106L + idempotency 35L + cancellation 59L + admission_refusal_reasons.v2.json 38L + service_1_refusal_reasons.v0.json 19L + webhook_registration.py 73L + routers/objectives.py 211L); 5 MODIFIED source files (dispatch.py +80/-13 net +67; admission_refusal.py +128 add 2 emit helpers; routers/service_1.py +48/-27 net +21; server.py +21/-3 net +18; contracts/__init__.py +3); 2 NEW test files (Stage-B roster 894L + byte-identity regression 124L); 5 pre-Stage-B test migrations (test_dispatch_shape_responsive.py 3 tests + test_dispatch_grain_form_refusal.py 2 tests + test_composed_conclusion_v0_contract_frozen.py 1 parity assertion) — Condition-5-style migration replacing Phase-2 fresh-fork placeholder expectations with Phase-5 async-accepted expectations; ~230 discretionary framing enumerated inline in close report (id-prefixes `obj-<12hex>`/`trc-<12hex>`, worker count 4, queue max 1024, backoff array [1s,4s,16s,64s,256s], webhook payload wire-shape 5-key naming, env-var name convention `RMS_APP_*`, `PT5M` delivery estimate default, response body field-order, docstring wording) | ~6.7× overall / ~0.68× discretionary-only (v2 accounting, post-§0-strict); ~1600-2200 band target → ~2274 actual → **+3.4% delta ABOVE band, within acceptable variance, no restatement needed** | 2026-07-04 — Phase 5 debt SPLIT closed on Stage B implementation. §7 async admission LIVE at `POST /api/objectives` (202 AsyncDeliveryAccepted_v0 | 422 AdmissionRefusal_v0 | 503 infra) + `GET /api/objectives/{id}` (wire-shape polling; wire-shape gated) + `POST /api/objectives/{id}/cancel` (200 thin cancelled envelope OR existing terminal). v2 dispatch fresh-fork route ALSO widened to include AsyncDeliveryAccepted_v0 @202 (per Owner Q4.d default: fresh-fork async admission reachable at BOTH endpoints). 2 new frozen contracts: NorthenaLedgerRow_v1 (contract 19, snapshot SHA `0ec71fde...`) + AsyncDeliveryAccepted_v0 (contract 20, snapshot SHA `d2027c02...`). Mechanical parity 18→20 GREEN. Substrate-drop 9/9 GREEN. **18 PRIOR frozen contract sources byte-identical pre-5b vs post-5b** — verified by `test_v0_paths_byte_identical_after_5b.py` parametrised over 18 files; SHA table quoted verbatim in close report §6. `services/service_1/composed_conclusion.py:316-321` answer_text synthesis lines SHA-anchored at `d2e72653...` (Q4.c ratification: Owner ratified as truthful metadata; no remediation). 3 new Standing Owner Dispositions landed at §0.1 (frozen-field-changes-as-new-versions + infra-not-refusal + cancellation-is-a-state-not-a-refusal). 3 grep-negative gates enforce STRUCK codes (`caller_cancelled` + `async_queue_saturated`): tests pass across all 4 registries (v0/v1/v2 admission + v0 service_1). G1 LOAD-BEARING kill-and-restart gate: idempotency-guard on `(trace_id, run_id, stage)` fires as Option B fallback per Q4.e (Mongo transactions not required; single-node Mongo compatible). Wire Union at v2 dispatch: `Union[DispatchResult @501, AdmissionRefusal_v0 @422, ComposedConclusion_v0 @200, Service1Refusal_v0 @422, QualifiedDataPayload @200, AsyncDeliveryAccepted_v0 @202]` + HTTP 503 branch out-of-Union (infra doctrine). Close report on-disk canonical: `/app/docs/close_reports/phase_5_stage_b.md` SHA-256 `49ce2262b1f6f6e244bb7294b165734f6de31a1b176a55f73dd8871e94a2def5`. **`make ci` = 504/504 GREEN.** |
| **Phase 6 Stage A — Economics §8 config design (design-only, no implementation)** | **CLOSED** | **504** (unchanged; docs-only touch) | N/A (design-only) | N/A (design-only; Stage B does the LoC accounting) | N/A | 2026-07-04 — Design-only Stage A landed at `/app/docs/stage_a_proposals/phase_6_stage_a.md`. Owner returned 7-axis dispatch ruling + 2 new meta-doctrines at Stage B open: (a) Axis 3 Option α → `AsyncDeliveryAccepted_v1` net-new; (b) Axis 4 TWO delivery bands only (`warm_qualified`, `fresh_extraction`); (c) Axis 5 stamp_audit sidecar to Northena Ledger, NOT new collection; (d) Ruling R4-SD2 arbitration DEFERRED; (e) 3 new admission-refusal codes via v3 bump; (f) 2 new Standing Dispositions: `Disposition-must-cite-owner-ruling` + `Inline-delivery-scope-amended-v2`. |
| **Phase 6 Stage B — §8 economics implementation (quote envelope + delivery estimate + fleet policy + instrumentation + pricing router)** | **CLOSED** | **550** (504 → +46 net: 23 Phase-6-Stage-B gate roster + 20 byte-identity regression [20 prior contracts] + 1 count-invariant bump + 1 6b synthesis-anchor + 1 fresh async-delivery-accepted-v1-supersets) | ~180 (asyncio Queue + Mongo pattern from Phase 5; Pydantic contract scaffolding from prior v0 files; NorthenaLedgerRow pattern from Phase 5b v1; ObjectiveRequest_v2 lifted from Phase 0; instrumentation stamp_audit sidecar pattern reused from Phase 5b Northena Ledger schema; AdmissionRefusal emit-helper pattern from Phase 3) | ~2812 (source ~1700 + tests ~750 + snapshots ~372): 2 NEW frozen contracts (quote_envelope.py 158L + async_delivery_accepted_v1.py 65L); 2 NEW contract snapshots (156L + 216L = 372L machine-generated); 4 NEW config JSON files (fleet_policy.v0.json 27L + price_model.v0-exploratory.json 50L + pricing_tiers.v0.json 16L + admission_refusal_reasons.v3.json 53L); 7 NEW economics service modules (__init__.py 18L + expiry.py 40L + fleet_policy.py 86L + delivery_time.py 81L + price_model.py 147L + instrumentation.py 170L + quote_service.py 187L); 1 NEW router (routers/pricing.py 137L); 2 NEW test files (test_v0_paths_byte_identical_after_6b.py 109L + test_phase_6_stage_b_economics.py 516L); 7 MODIFIED files (admission_refusal.py registry-pointer v2→v3 + 3 emit helpers ~150; dispatch.py quote-mint branch ~15; async_worker.py imports + hook ~50; routers/objectives.py quote-mint branch ~20; contracts/__init__.py exports ~8; server.py router mounts ~3; test_frozen_contract_snapshot_parity.py parity map 20→22 ~2; test_composed_conclusion_v0_contract_frozen.py count-invariant 20→22 ~10); ~250 LoC retroactive citation audit + 2 new dispositions in ORCHESTRATOR_CONTINUITY.md; ~350 discretionary framing enumerated in close report | ~15.6× overall / ~1.28× discretionary-only (v2 accounting, post-§0-strict); ~1800-2400 band target → ~2812 actual → **+17% delta ABOVE band; overage entirely explained by (a) 372 LoC machine-generated snapshot JSON (non-discretionary) + (b) ~250 LoC retroactive citation content mandated by new meta-doctrine `Disposition-must-cite-owner-ruling` + (c) mandatory HAZARD-STOP-NOTES in every economics module per §8 bullet 1 doctrine. Discretionary LoC ≈ 2190.** | 2026-07-04 — Phase 6 debt SPLIT closed on Stage B implementation. §8 economics LIVE: `POST /api/objectives` + `POST /api/service_1/v2/dispatch` fresh-fork mint QuoteEnvelope_v0 at admission via `services.economics.quote_service.issue_quote(...)`; governance-refusal precedence (form_not_offerable → exploratory_tier_expired → pricing_tier_frozen_by_control_surface → fleet_policy_reserved_zero_capacity) each return @422 AdmissionRefusal_v0. Two-band delivery (`warm_qualified`, `fresh_extraction`) with buyer-surface grep-negative on GPU keywords. Fleet policy apportionment sums to 1.0 ± 0.001; arbitration DEFERRED per R4-SD2 (config carries hazard_stop_notes only). Master Admin surface at `/api/pricing/*` + `/api/fleet/*` (env-gated via `RMS_MASTER_ADMIN_TOKEN`). Instrumentation via stamp_audit sidecar on Northena Ledger; idempotent per (trace_id, run_id, stage='converge') — kill-and-restart G1 LB preserved (verified GREEN post-6b). 2 new frozen contracts: QuoteEnvelope_v0 (contract 21, snapshot SHA `83679e7d...`) + AsyncDeliveryAccepted_v1 (contract 22, snapshot SHA `0cdb911b...`). Mechanical parity 20→22 GREEN. **20 PRIOR frozen contract sources byte-identical pre-6b vs post-6b** — verified by `test_v0_paths_byte_identical_after_6b.py` parametrised over 20 files. `services/service_1/composed_conclusion.py:316-321` UNTOUCHED (Verdict A protection regression from 4b/5b): slice SHA `d2e72653f84c4772796a6fb71b61fb70345f057cfd3451d60bbfb15bc2d58159`. Retroactive Standing-Disposition citation audit COMPLETE — 3 Phase 5 dispositions re-landed with `[Owner ruling, Phase 5 Stage A close, 2026-07-04]` citation headers + verbatim quotes. 2 new Standing Owner Dispositions inline at §0.1: `Disposition-must-cite-owner-ruling` + `Inline-delivery-scope-amended-v2`. Registry bump v2→v3 additive: 3 new codes. Close report on-disk canonical: `/app/docs/close_reports/phase_6_stage_b.md`. **`pytest -q` = 550/550 GREEN.** |
| **Phase 7 Stage A — Shaping Wizard §3.3 design (design-only)** | **CLOSED** | **550 (unchanged; docs-only)** | N/A (design-only) | N/A (design-only; Stage B does LoC accounting) | N/A | 2026-07-04 — Design proposal at `/app/docs/stage_a_proposals/phase_7_stage_a.md`. Owner returned 7 escalations (E1-E7) accepted at Stage B-1 dispatch. |
| **Phase 7 Stage B-1 — Shaping wizard §3.3 operator variant + 4 net-new frozen contracts + license-class Option C wrap + provenance-preservation shared-derivation module** | **CLOSED** | **613 (+63 net: 30 wizard invariant gates + 22 parametrised byte-identity + 3 slice/file gates + 8 parametrised ask-vs-propose + count/sanity)** | ~120 (ASGITransport pattern from Phase 6; HMAC-and-Mongo idempotency pattern from Phase 5b; Ruling 4 shared-derivation import from Phase 4a; NorthenaLedgerRow LedgerArtifactRef pattern from Phase 5b; Pydantic contract scaffolding from prior v0 files; FastAPI APIRouter pattern from prior routers) | ~2754 (source ~1402 + snapshots ~396 + tests ~888 + modifications ~68): 4 NEW frozen contracts (wizard_commit_state.py 147L + operator_turn.py 29L + agent_assumption.py 31L + committed_value.py 73L = 280L); 4 NEW machine-generated snapshot JSONs (256L + 42L + 41L + 57L = 396L); 6 NEW wizard service modules (__init__.py 15L + agent_interface.py 136L + operator_state_machine.py 286L + source_tagging.py 93L + session_persistence.py 68L + turn_ledger.py 87L = 685L); 1 NEW shared-derivation module (services/service_1/provenance_preservation.py 173L); 1 NEW router (routers/wizard_operator.py 264L); 3 MODIFIED files (services/service_1/license_class_selection.py +50 additive Option C wrap; server.py +5 router mount + wizard session ensure_indexes; contracts/__init__.py +7 exports [B-1 scaffold]); 2 NEW test files (test_phase_7_stage_b_1_wizard.py 721L + test_v0_paths_byte_identical_after_7b_1.py 167L); 2 test-file additive bumps (test_composed_conclusion_v0_contract_frozen.py +2 count invariant 22→26; test_frozen_contract_snapshot_parity.py +4 new map entries); ~250 discretionary framing (gate names, docstring citation headers, in-memory session cache size, ISO timestamp prefix conventions, illustrative field values in tests, close-report LoC-table row wording) | ~23× overall / ~2.08× discretionary-only (v2 accounting, post-§0-strict); ~1200-1700 anchored band target → ~2754 actual → **+43% ABOVE anticipated ~1930 (from Stage A §7.3 sum-of-parts); overage explained by (a) test surface heavier (888 actual vs 350 projected — 30 gates delivered incl parametrised) + (b) router heavier (264 vs 100 — 5 endpoints not 4, Mongo persistence inline) + (c) provenance_preservation module at B-1 per E7 (173L not in original size projection). Snapshots UNDER projection (396 vs 955). Ratified with documentation per Rule 2 v2 stop-and-judge doctrine.** | 2026-07-04 — Phase 7 wizard-side dispatch plan-debt PARTIALLY RESOLVED (operator variant + license-class Option C wrap). §3.3 operator variant LIVE at 6 endpoints under `/api/wizard/operator/*`. 4 NEW frozen contracts: WizardCommitState_v0 (23) + OperatorTurn_v0 (24) + AgentAssumption_v0 (25) + CommittedValue_v0 (26). Mechanical parity 22→26 GREEN. **22 PRIOR frozen contract sources byte-identical pre-7b-1 vs post-7b-1** — verified by `test_v0_paths_byte_identical_after_7b_1.py` parametrised over 22 files. Ruling 4 shared-derivation preserved (grep-negative gate GREEN on `services/wizard/*`; wizard invokes `services.mtafiti.floor_feasibility.derive_floor_feasibility(...)` directly). License-class Option C wrap: fallback body slice byte-identical (SHA `ca3b2007...`); primary-arm branch discrimination LB gate GREEN. Owner E5 wizard-transcript retention class marker `data_class="wizard_transcript"` fires on every wizard freeze (LB gate GREEN + idempotency gate GREEN). NO LLM code in `services/wizard/*` (LB grep-negative GREEN — enforces `Agent-pluggable-with-stub-agent-first` new Standing Disposition). Owner E7 provenance-preservation shared-derivation module at `services/service_1/provenance_preservation.py` (single-source LB grep-negative GREEN). 2 new Standing Owner Dispositions at §0.1: `Agent-pluggable-with-stub-agent-first` + `Visibility-not-prohibition`. 2 new Plan Debts at §0.2: Phase 8 Stage A frozen-contract trajectory restatement + Phase 8c DPO surface `wizard_transcript` enumeration. Close report on-disk canonical: `/app/docs/close_reports/phase_7_stage_b_1.md` SHA-256 `b34fc38eb69804165dcf1a9eb65351a0c6b0a4648895c17e5c4b408b7b635d9e`. **`pytest -q` = 613/613 GREEN.** |
| **Phase 7 Stage B-2 — Buyer variant + Sonnet 4.6 LLM (SonnetWizardAgent inside Shield) + dual-delta gate + Condition-A pre-LLM Guard-1 landing + Condition-2 grep-negative single-source (× 3 symbols)** | **CLOSED** | **685 (+72 net: intermediate block sequence 613 → 624 (Block A pre-LLM Guard-1 mandatory-tier) → 631 (Block B Sonnet 4.6 inside Shield) → 685 (Block C buyer SM + router + dual-delta + Condition-2 single-source + byte-identity 26 + regressions); 38 named gates including Condition A(i) parametrised × 8 mandatory-fields + Condition 2 grep-negative parametrised × 3 symbols + byte-identity parametrised over 25 contract sources = 72 collected new cases)** | ~500 (`WizardAgent` Protocol from B-1 + operator SM shape via imports + operator router shape mirror + `provenance_preservation.py` declarative-table pattern for dual-delta + `LlmChat`/`litellm` pattern in Shield + gate scaffolding from B-1 test file + `ASGITransport` test pattern) | ~1757 (3 NEW source files: dual_delta.py 91L + buyer_state_machine.py 332L + routers/wizard_buyer.py 267L; 1 NEW test file: test_phase_7_stage_b_2_wizard.py 835L; 5 MODIFIED files: services/synisense/shield/llm_router.py +196 SonnetWizardAgent class + _sonnet_invoke helper + no-silent-degrade path, services/wizard/operator_state_machine.py +12 Condition-A predicate + variant kwarg, routers/wizard_operator.py +18 SourceTagViolation import + 422 boundary, services/wizard/session_persistence.py +4 additive compound index, server.py +2 buyer router mount; 1 docs-only fix: docs/lift_manifest.json +6 pre-existing 8a-lite path repair) | ~3.5× overall / ~1.4× discretionary (v2 accounting, post-§0-strict); **anchored band 1600-2000 (mid ~1800) → 1757 actual → WITHIN BAND (-2.4% delta below mid; +0.4% within top-of-band). No Rule-2 stop-and-judge triggered. Transcription-only per Owner cap; disc/mandate split not re-derived; ratio strings transcribed verbatim.** | 2026-07-04 — Phase 7 wizard-side dispatch plan-debt buyer-variant + LLM portion RESOLVED. §3.3 buyer variant LIVE at 7 endpoints under `/api/wizard/buyer/*` (session/turn/propose/agent-assumption/commit-review/freeze + GET session). SonnetWizardAgent (Claude Sonnet 4.6 via Emergent LLM Key) lives inside `services/synisense/shield/llm_router.py` — `test_no_direct_llm_calls_outside_shield_still_green` GREEN + `test_sonnet_wizard_agent_lives_inside_shield_boundary` GREEN (no LLM SDK import in `services/wizard/*`). Temp 0.2 live / 0.0 hermetic replay (`_invoke` monkeypatched in test seam). No silent model degrade — Sonnet-unavailable → HTTP 503 (Owner Standing Disposition `Infra-not-refusal`). Dual-delta gate (Owner E6 `Visibility-not-prohibition` mechanical application): standard/grain-changing proposals refuse `dual_delta_missing` at `/propose` endpoint when price_delta or class_delta absent; reach-changing proposals admissible without dual-delta. Owner Condition-A pre-LLM Guard-1 mandatory-tier gate on `POST /api/wizard/operator/{sid}/agent-assumption` (3 LB gates): (i) refuse `AgentAssumption_v0` on mandatory-tier field when `variant="operator"` via `SourceTagViolation` at `services/wizard/operator_state_machine.py:189-192`; (ii) never mint operator-source CommittedValue from the endpoint (structural); (iii) never append OperatorTurn (structural). Buyer variant behavior: Guard 1 no-ops on `variant="buyer"` (agent-may-propose per v3 §3.3). Owner Condition-2 grep-negative gates confirm `buyer_state_machine.py` re-implements NONE of {`validate_source_tags`, `validate_guard_1_operator_mandatory_all_operator_supplied`, `_record_feasibility_snapshot`} — imports each from operator-proven module (`test_buyer_state_machine_does_not_reimplement_shared_symbol` parametrised × 3 GREEN). Zero new frozen contracts (parity 26 unchanged). **26 PRIOR frozen contract sources byte-identical pre-7b-2 vs post-7b-2** — verified by `test_prior_contract_file_exists_and_stable_at_7b_2` parametrised over 25 contract source files + `test_prior_26_contracts_count_at_26` count invariant. `services/service_1/composed_conclusion.py:316-321` UNTOUCHED (Verdict A regression preserved from 4b/5b/6b/7b-1). `services/service_1/provenance_preservation.py` UNTOUCHED (byte-identical SHA `1eedde91f797...`); buyer SM imports it (single-source preserved for Owner E7). Ruling 4 shared-derivation preserved (buyer SM imports `_record_feasibility_snapshot` from operator-proven module which itself invokes `services.mtafiti.floor_feasibility.derive_floor_feasibility(...)`). Zero new §0.1 Standing Dispositions (§0.1 FROZEN per Owner correction). 1 new Plan Debt at §0.2: `Wizard session-ownership binding plan-debt` — verbatim: *"Wizard session-ownership binding lands with Phase 8 auth/key model — recorded as the system-wide auth landing, not a wizard-special. [Owner ruling, Phase 7 Stage B-2 dispatch, 2026-07-04]"*. Rule 2 accounting JSON backfilled at close (9 → 28 phases; strict transcription per Owner cap; served through `/api/discipline/lift_manifest`). PHASE_STATE.md dedupe housekeeping applied (`## Live State`, `## Phase Ledger`, `## Pending Decisions` each appear exactly once post-dedupe; git-stat: 121 lines total delta, 28 insertions / 93 deletions). Platform auto-commit `1625c8e` (2026-07-04 18:59:09 UTC) collapsed working-tree Block A/B/C sequence into a single commit. Close report on-disk canonical: `/app/docs/close_reports/phase_7_stage_b_2.md` (SHA quoted in return message). **`pytest -q` = 685/685 GREEN.** |
| **Phase 8a-lite — Ask Console (Frontend, UI Spec v1 §3 landing; legacy G5b archival housekeeping)** | **CLOSED** | **Frontend: 27 / 27 across 5 UI-Spec-v1 suites GREEN; production `yarn build` GREEN. Backend: 685 / 685 unchanged (zero backend surface delta)** | 0 (backend untouched) — frontend N/A (no backend LoC lift; frontend reuses shared components ClassBadge + RefusalCard + LedgerTable + TrustReceiptLink from `src/components/*` per Owner Condition-2-flavored posture, no reimplementation) | ~1173 frontend LoC (6 new files: AskConsolePage 610L + 5 new UI-Spec-v1 gate test files 481L + mechanical import-path adjust on 8 archived legacy pages). 3 legacy `__tests__/*.js` deleted (superseded by copy-forward per Owner ruling verbatim). Zero backend LoC. | N/A (frontend phase; no backend Rule 2 v2 accounting applies). | 2026-07-04 — Ask Console live at `/` (human-visible surface unblocked; webpack build unblocked). §3.1 Ask + §3.2 Answer + §3.3 Refusal binding copy verbatim. `POST /api/service_1/v2/dispatch` consumer via `api.dispatchV2(...)` (extended in `apiClient.js`; single-ingress preserved). Five response branches code-covered: 200 ComposedConclusion → AnswerView, 202 AsyncDeliveryAccepted → AcceptedView, 422 refusal → RefusalView, 422 validation → InfraFaultView, 5xx/network → InfraFaultView (infra-not-refusal doctrine). NO output-form picker on Ask surface (§3.1 verbatim): 6 gates in `test_no_output_form_picker_present_on_ask_surface.test.js` enforce zero `<select>` / zero `role=combobox/listbox/radiogroup` / zero output-form data-testid / zero preset-value `<option>` / quiet-defaults "change" affordance disabled at 8a-lite (enabled at Phase 8 full when Buyer Wizard lands) / Ask binding copy verbatim "What do you need to know?". Legacy G5b archival: 8 pages moved to `src/legacy/pages/`; import paths adjusted; App.js routes them at `/legacy/*` (kept reachable for Trust receipt deep-links from Ask Console answer surface). 3 G5b invariant gates re-landed under `src/__tests__/ui_spec_v1/` (Gate 1 Class inseparable 7 tests + Gate 2 Refusal first-class + validation distinguishability 6 tests + Gate 3 Single ingress + trace_id retention 4 tests). Gate 3 Part A static-analysis excludes `src/legacy/` (archived non-active surface per Phase 8a-lite archival ruling). 2 new UI-Spec-v1 gate tests: `test_g5b_legacy_pages_archived_under_frontend_legacy_directory` (4 gates) + `test_no_output_form_picker_present_on_ask_surface` (6 gates). Component reuse (Owner Condition-2 posture): AskConsolePage reuses `ClassBadge` + `RefusalCard` from `src/components/*` — zero reimplementation. Manual E2E via curl `POST /api/service_1/v2/dispatch` with synthetic ObjectiveRequest_v2 payload: HTTP 202 AsyncDeliveryAccepted_v1 returned cleanly (fresh estate; Phase 5b §7 async path). Refusal 422 path structurally distinguished from validation 422 (Gate 2 T6 GREEN). Standing constraints honoured: zero backend surface delta (pytest 685/685 unchanged); no `git push`; no refactoring; no LLM outside Shield; 26 frozen contracts byte-identical. Close report on-disk canonical: `/app/docs/close_reports/phase_8a_lite.md` SHA-256 `bf4ba9a94f250abad61d33a842bdedf2e7c8571a3fe61b1d3323c25601dbe888`. |
| **Phase 7 Stage B-3 — Wizard trilogy COMPLETE (commit-review extensions + buyer freeze ledger parity + admission handoff endpoints on both variants)** | **CLOSED** | **740 (+55 net from 685: Block A buyer freeze ledger parity + commit-review extensions 7 gates; Block B admission_handoff.py + /handoff endpoints on both variants 13 gates; Block C frozen-contract posture + regressions 10 gates + parametrised expansions: Condition-2 × 3 symbols + prior-contract × 25 files = ~55 collected new cases)** | ~275 (`ObjectiveRequest_v2` frozen shape reused via `.model_validate` + `WizardCommitState_v0` model_copy + `derive_license_class` from B-1 + `record_wizard_freeze` from B-1 + `session_persistence.load_session` + `evaluate_dual_delta` shape from B-2 dual_delta.py + `httpx.AsyncClient(ASGITransport)` pattern from tests + declarative-table style from B-2/B-1 modules) | ~1094 (1 NEW source file: services/wizard/admission_handoff.py 211L; 1 NEW test file: test_phase_7_stage_b_3_wizard.py 574L; 1 NEW Stage A doc: 232L; 2 MODIFIED routers: routers/wizard_buyer.py +176 [commit-review extensions with dual_delta_summary + license_class_drift; freeze ledger call + lawful_basis_ref body param + ledger_run_id in response; NEW /handoff endpoint], routers/wizard_operator.py +101 [commit-review license_class_drift only; NEW /handoff endpoint]; 2 MODIFIED support files: test_phase_7_stage_b_2_wizard.py ±32 point-in-time gate adjustments (assert-in-set for B-2/B-3 postures), docs/lift_manifest.json ±60 8a-lite copy-forward). Envelope-shim helper triad duplicated across both routers by design (additive-only posture at B-3; extraction to shared module deferred to Phase 8 backend refactor). | ~2.24× overall / ~1.6× discretionary-only (v2 accounting, post-§0-strict); **anchored band 800-1100 (mid ~950) → ~1094 actual → WITHIN BAND (+15% of mid; -0.5% within top-of-band). No Rule-2 stop-and-judge triggered.** | 2026-07-04 — Wizard trilogy CLOSED. Operator variant grows 6 → 7 endpoints (adds /handoff); buyer variant grows 7 → 8 endpoints (adds /handoff). E2E verified via curl: buyer freeze → 200 with ledger_run_id, handoff → 202 AsyncDeliveryAccepted_v1 with objective_id, repeat handoff → SAME objective_id (Phase 5 §7 idempotency preserved via deterministic idempotency_key=`handoff-{session_id}`). Close report on-disk canonical: /app/docs/close_reports/phase_7_stage_b_3.md SHA-256 `ea12517cec7deee48818a097e942d08601fda5a0f381e215a7df2c508c801c30`. **`pytest -q` = 740/740 GREEN.** |
| **Phase 8 Stage A — Frontend surfaces trajectory restatement (design-only)** | **CLOSED** | **740 (unchanged; design-only, no code writes)** | N/A (design-only) | N/A | N/A | 2026-07-04 — Design-only Stage A landed at `/app/docs/stage_a_proposals/phase_8.md` SHA-256 `4e4dd82ae2834f7c680429e1b2771221566d2bdc810e04444b722afda7b5c188`. Trajectory restatement (parity 26 unchanged through Phase 8; conditional 27 if E4 lands `EngineerKeyGrant_v0` at B-3) + Six Surfaces per UI Spec v1 + §8 Shared components + Playwright Ask Console smoke shape + `snapshot_lloc_in_band` per sub-stage (B-1/B-2/B-4/B-5 = `no`; B-3 conditional) + 8 escalations (E1-E8; E1/E2/E3 blocked Stage B-1 dispatch until ruled). §0.2 Plan Debt updates: trajectory restatement RESOLVED with SHA; wizard_transcript entry REFINED with "separately-addressable" qualifier + dual citation; envelope-shim triad extraction APPENDED per Owner Decision 3. Owner returned rulings on all 8 escalations (2026-07-05) — see Phase 8 Stage B-1 dispatch row. |
| **Phase 8 Stage B-1 — Auth/key model + envelope-shim triad extraction + shared UI-Spec-v1 §8 barrel + Ask Console-full auth wiring + Playwright chromium smoke** | **CLOSED** | **777 (+37 net from 740: E1 gates 12 [JWT + bcrypt + scope check + endpoints] + E2 gates 14 [4-code registry + body shape + registry-exclusion + endpoint 401 shape] + E3 gates 11 [router_shims triad + grep-negative × 3 symbols × 2 routers + BC import])** | ~500 (JWT via PyJWT vetted lib per Owner E1 no-hand-rolled-crypto; bcrypt vetted lib; FastAPI dependency pattern from prior routers; Pydantic model scaffolding from prior v0 files; Motor async pattern from Phase 5b async_state; auth-refusal emit-helper pattern from `admission_refusal.emit`; Ruling 3 versioned config pattern from `admission_refusal_reasons.v3.json`; Owner Condition-A pre-LLM gate scaffold from B-2; ASGITransport test pattern; React context/hook pattern from prior surface code) | ~1959 raw (12 NEW backend source files + 1 NEW backend test file + 3 MODIFIED backend files + 7 NEW frontend source files + 2 NEW frontend test files + 2 NEW frontend e2e files + 2 MODIFIED frontend files) | ~1.0× overall (transcription-only per Owner cap; disc/mandate not fresh-derived); **anchored band 1150-2150 (upper accepted at Stage A per Owner E3 verbatim) → ~1959 actual → WITHIN BAND (~91% of top-of-band). `snapshot_lloc_in_band=no` at B-1.** | 2026-07-05 — Auth/key model LIVE. Endpoints under `/api/auth/*`: POST /register + POST /login + POST /refresh + GET /me. Owner E2 auth-denial shape `{reason, detail}` — NO `outcome` key. 4-code bounded set at `services/auth/auth_refusal_reasons.v0.json`. Envelope-shim triad extraction: `services/wizard/router_shims.py` canonical single-source; `admission_handoff.py` reduced to 38L re-export shim. 2 §0.2 debts RESOLVED at B-1. Close report `/app/docs/close_reports/phase_8_b_1.md` SHA `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`. |
| **Phase 8 Stage B-2 — Operator surface §2 verbatim + session-binding decorator wiring on operator wizard + GET /api/operator/status + scope-enforcement gate PAIR on POST /v2/dispatch + Playwright completion first commit** | **CLOSED** | **791 (+14 net from 777: session-binding × 4 + operator/status × 2 + scope-gate PAIR × 4 + standing-constraint regression × 4)** | ~600 (session-binding helper pattern from B-1; scope-gate pattern from B-1 key_grants; FastAPI dependency pattern; Playwright test transport pattern) | ~1220 raw (2 NEW backend files: routers/operator.py 78L + tests/invariants/test_phase_8_b_2_operator_and_scope_gate.py 330L + 3 MODIFIED backend files: routers/wizard_operator.py +41L session-binding wiring on 6 endpoints; routers/service_1.py +40L scope-gate + Request dependency; server.py +3L + 3 NEW frontend §2 pages: OperatorHomePage 203L + CommissionWizardPage 243L + CommitReviewPage 250L + 2 MODIFIED frontend files: apiClient.js +30L + App.js +5L + 1 RENAMED e2e file .spec.js → .spec.ts + 1 devDependency @playwright/test@1.61.1 + 1 script test:e2e chromium-only) | ~1.0× overall; **anchored band 980-1820 (mid ~1400) → ~1220 raw LoC → WITHIN BAND (~67% of top-of-band; snapshot_lloc_in_band=no).** | 2026-07-05 — Operator §2 verbatim (§2.1 Home + §2.2 Commission Wizard + §2.3 CommitReview). Session-binding decorator wired across ALL 6 operator wizard endpoints (mismatch → 403 auth_identity_mismatch_for_wizard_session). Scope-gate PAIR on POST /v2/dispatch: granted → dispatch normally; insufficient → 403 auth_scope_insufficient; ZERO envelope delta verified. Playwright completion first commit (was config-only at B-1). Close report `/app/docs/close_reports/phase_8_b_2.md` SHA `4a99dbf35cfda8f9207e1b2e0b18a9f4237bc5243c5235bc9056b3e60f04a305`. |
| **Phase 8 Stage B-3 — Engineer §4 + Buyer §5 surfaces verbatim + E4 EngineerKeyGrantRegistration UNFROZEN under D4b + load-bearing wire-shape gate + ledger integration (issuance/revocation/idempotency) + buyer session-binding decorator wiring + first-commit smokes** | **CLOSED** | **818 (+27 net from 791: 8 load-bearing wire-shape governance-key pin + 3 ledger integration + 9 buyer session-binding parametrised + 7 E2 taxonomy over grant endpoints)** | ~450 (WizardAgent Protocol shape reused; scope-gate pattern from B-2; ledger emit-helper pattern from Northena; router structure from operator variant; Pydantic scaffold from prior v0 files) | ~2896 raw (3 NEW backend source: engineer_key_grant 82L + engineer_key_grant_ledger 67L + engineer_key_grant_service 105L; 1 NEW backend router: engineer 100L; 4 NEW backend test files: 128L + 147L + 116L + 77L; 3 NEW frontend §4 pages: EngineerRegisterAppPage 305L + EngineerFirstCallPage 115L + EngineerAdministerPage 155L; 3 NEW frontend §5 pages: BuyerShapePage 272L + BuyerAcquirePage 182L + BuyerReceivePage 133L; 3 NEW Playwright specs: engineer_register_app 79L + engineer_surface_4_2_4_3 65L + buyer_surface_5 118L; 1 NEW Jest binding-copy: 105L; 5 NEW Block-1 first-commit coverage gates 458L; 2 MODIFIED backend: server.py +3L + wizard_buyer.py +24L; 3 MODIFIED frontend: apiClient.js +48L + App.js +12L + CommissionWizardPage.js EstateCheckChip prop rename) | ~1.5× overall / ~1.6× discretionary (v2 accounting, post-§0-strict); **anchored band 1900-2900 (restated at dispatch per B-1 lesson to include gate work) → ~2896 actual → WITHIN BAND (99.9% of top-of-band). No Rule-2 stop-and-judge triggered.** | 2026-07-05 — Engineer §4 + Buyer §5 verbatim LIVE. Owner D4b resolution: EngineerKeyGrantRegistration UNFROZEN under 4a §6.1 precedent; 7 governance-key fields pinned. §4.2 binding copy "There is no response shape in which the claim is separable from its class." §5 dual-delta pair per E6. Retroactive B-2 EstateCheckChip regression CAUGHT AND FIXED at B-3 first commit. Close report `/app/docs/close_reports/phase_8_b_3.md` SHA (pre-acceptance) `c2863974bf52f69ff8b7256ad1bae07854a546526672c2d099305a98d01bec22`. **B-3 close SHA (post-acceptance-footer, appended at B-4 close acceptance 2026-07-05): `a31b5a9d43c0563140a73762789f765390187052ebd4e24b47d9bb6a528f7215`** (retroactive cross-reference footer added: fixture shape drifted at B-3 → CAUGHT AND FIXED at B-4 fixture-schema gate). |
| **Phase 8 Stage B-4 — Master Admin §6 surface verbatim + JWT master_admin auth reconciliation (RMS_MASTER_ADMIN_TOKEN retired) + Path A tier_lock ledger integration + Path B honest 501 for model_version and fleet_policy + real seams-pending enumeration + §4.2 fixture-schema Jest invariant** | **CLOSED, ACCEPTED 2026-07-05** | **855 (+37 net from 818: 3 tier_lock Path A ledger + 15 auth-reconciliation parametrised (5 endpoints × 3 postures) + 6 pending-seams + 5 audit-trail + 6 Path B 501 + 1 seeded-admin baseline + 1 retired-header zero-effect + updated Gate 16 JWT swap); Jest 60→72 (+12); Playwright chromium 16→24 (+8)** | ~250 (JWT master_admin pattern from B-1 auth; ledger emit-helper pattern from Northena; Pydantic scaffold; FastAPI dependency pattern; React page pattern from B-3) | ~2690 raw (5 NEW backend Pytest gate files 888L + 3 NEW frontend §6 pages 627L + 2 NEW Jest gates 289L + 3 NEW Playwright chromium smokes 285L + MODIFIED: pricing.py JWT+PathA+PathB rewrite 247L, test_phase_6_stage_b_economics.py Gate 16 JWT swap, apiClient.js +42L, App.js +12L, EngineerFirstCallPage.js fixture-export+shape-align refactor, engineer §4.2 smoke +2L, .env RMS_MASTER_ADMIN_TOKEN line PURGED, tier_lock_ledger.py idempotency-by-run_id correction) | ~1.0× overall (transcription-only); **anchored band 1300-1800 (Owner amended +100 for seams-pending banner at Stage-A ratification) → 2690 actual raw LoC → +49% ABOVE top-of-band → snapshot_lloc_in_band=no. OVERAGE SPLIT-DISPOSED (Owner-ruled 2026-07-05): mandate-forced portion (verbatim §6 binding copy + first-commit gates) RATIFIED on record; test-lump underestimate portion (888 actual vs ~330 estimated, 2.7×) is ORCHESTRATOR-OWNED estimation miss. Standing correction internal to orchestrator template: Stage-A sizing enumerates test matrix (endpoints × postures × cases), never a test-LoC lump. B-4 is post-signal where B-1 was pre-signal — same magnitude, different meaning. [Owner ruling, Phase 8 B-4 close acceptance, 2026-07-05]** | 2026-07-05 — Master Admin §6 verbatim LIVE. Auth reconciliation: JWT `master_admin` role exclusive; `RMS_MASTER_ADMIN_TOKEN` retired. Path A `POST /api/pricing/tier_lock` writes NorthenaLedgerRow_v1 with `stamp_audit.data_class="master_admin_rule_change"` + versioned marker `services/economics/tier_lock.vN.json` + idempotent per (rule_id, idempotency_key). Path B `POST /api/pricing/model_version` + `POST /api/fleet/policy` return 501 with plain-language `detail`. Real seams-pending enumeration at `GET /api/master_admin/pending_seams` — 5 seams (Targeta yield / Mtafiti V3 / Northena retention / V2 cumulative-disclosure / MEA source-standing) read from env+config presence. §6.3 audit-trail at `GET /api/master_admin/audit_trail` — plain description + `full_diff_ref` link + COLLAPSED-BY-DEFAULT diff. §4.2 fixture-schema Jest invariant (Owner amendment): 3 fixture-contract pairs subset-checked via fs.readFileSync of .contract_snapshot.json neighbors. Audit-trail inline collapsible pre-block RATIFIED (Owner: modal/drawer below ruling altitude, no pivot). B-4 close SHA (pre-acceptance): `3cf03f809a7e19f9d275e2caddc3f1f75d68d29c33190504f4c37377892591b5`. **B-4 close SHA (post-acceptance-footer): `21d2f5193cfb53fbb3ec2c35f293e78f7bd535f446a969137b4bad8758fdb3b8`**. Zero new §0.1 dispositions (§0.1 FROZEN). Zero new §0.2 debts (seams-pending banner is NOT a plan-debt per Owner amendment). Parity 26/26 byte-identical. |
 Endpoints under `/api/auth/*`: POST /register (201 + tokens; open registration; default role `ask_console_user`) + POST /login (200 + tokens; 401 `{reason:"auth_missing", detail:"Invalid credentials."}` on failure) + POST /refresh (200 + tokens; 401 on expired/invalid refresh) + GET /me (200 Identity; 401 `{reason:"auth_missing", detail:...}` unauth). Owner E2 auth-denial shape NON-NEGOTIABLE: `{reason, detail}` — NO `outcome` key, NO `outcome=refused`, NO `AdmissionRefusal_v0` discriminator. 4-code bounded set at `services/auth/auth_refusal_reasons.v0.json` (Ruling 3 versioned config): auth_missing (401) / auth_expired (401) / auth_scope_insufficient (403) / auth_identity_mismatch_for_wizard_session (403). Server-side per-call scope enforcement via `services/auth/key_grants.check_scope(identity, class, path, floor, scope)` — floor-hierarchy aware (utterance < recorded_statement < established_fact) + ScopeCheckResult envelope-visible. Registry-exclusion gate GREEN: no `auth_*` code appears in `services/service_1/admission_refusal_reasons.v1.json`. Console render-path gate GREEN: AuthDeniedNotice testid-namespace disjoint from RefusalCard's; auth denial NEVER routes through RefusalCard (fourth-not-wearing-first's-clothes preserved). Four render paths at Ask Console: AnswerView (200 ComposedConclusion_v0) + RefusalCard (422 governance refusal outcome=refused) + InfraFaultView (5xx / network infra-not-refusal) + **AuthDeniedNotice (401/403 access-control class)** — the fourth path is OUTSIDE the three. Owner E3 envelope-shim helper triad extraction RESOLVED at B-1: `services/wizard/router_shims.py` (159L) is canonical single-source; `services/wizard/admission_handoff.py` reduced to 38L pure re-export shim (BC preserved for Phase 7 B-3 invariant tests at `test_phase_7_stage_b_3_wizard.py`); grep-negative gate parametrised over 3 triad symbols × 2 routers × 1 gate = 6 GREEN gates confirming neither `routers/wizard_buyer.py` nor `routers/wizard_operator.py` locally defines any of {compose_objective_request_from_frozen_state, compose_objective_request_from_frozen_state_with_proposals, summarise_dual_deltas}. Wizard session-ownership binding plan-debt (§0.2 Phase 7 B-2 dispatch, 2026-07-04) — RESOLVED at B-1: `services/auth/session_binding.py` (69L) lands the sidecar table with grandfathering carve-out; router-decorator wiring deferred to Phase 8 B-2 surface-scoped mechanical follow-up. Envelope-shim triad extraction plan-debt (§0.2 Phase 7 B-3 close acceptance, 2026-07-04) — RESOLVED at B-1: named receiver landed + grep-negative gate GREEN + re-export shim BC preserved. Shared UI-Spec-v1 §8 component barrel at `/app/frontend/src/components/ui_spec_v1/index.js` (28L): re-exports ClassBadge + RefusalCard + OuterGateReceiptInline + StatusBadge + LedgerTable + TrustReceiptLink + B-1 addition AuthDeniedNotice; single-source gate (7 assertions) GREEN — each barrel export is same-reference identity with single-source module (no reimplementation). Ask Console-full auth wiring: `apiClient.js` +67L (tokenStore + Bearer interceptor + auth endpoints + formatApiErrorDetail crash-safe FastAPI 422-detail flattener helper) + `hooks/useAuth.js` (88L; React context; states: null=checking / Identity=authed / false=anon; auto-refresh cycle on 401) + `pages/AuthLoginPage.js` (131L; renders 401 as inline auth-denied, NEVER via RefusalCard) + `pages/AuthRegisterPage.js` (132L; open registration form) + `App.js` +5L (AuthProvider wrap + /auth/login + /auth/register routes). Ask Console at `/` remains ANONYMOUS-friendly at B-1 (Owner E1 posture: auth is scope-enforcement point, not hard gate on answer surface; surface-specific gates land at Operator/Engineer/Buyer/Master Admin/DPO sub-stages). Playwright config `frontend/playwright.config.js` (26L; chromium-only project per Owner E7 ratified) + `frontend/e2e/ask_console_smoke.spec.js` (64L; one authenticated flow scenario: seed token in localStorage → intercept /api/auth/me + /api/service_1/v2/dispatch → assert answer render + trust-receipt href /legacy/trace/{id}). Playwright deps NOT installed at B-1 (config surface only; `yarn add -D @playwright/test` scheduled at Owner discretion; verified locally the spec syntax is valid). Zero new frozen contracts (parity 26 unchanged; verified `test_frozen_contract_snapshot_parity` GREEN). Zero new §0.1 Standing Dispositions (§0.1 FROZEN). §0.2 updates: 2 debts RESOLVED at B-1 (session-ownership + shim-triad). Ruling 4 shared-derivation preserved (auth module does not re-implement any wizard/service_1 primitive). Shield boundary preserved (services/auth/* imports no litellm/anthropic/emergentintegrations). Admin seed on startup via `services/auth/user_store.seed_admin_if_absent(...)` reading `ADMIN_EMAIL` + `ADMIN_PASSWORD` from `.env` — idempotent, does NOT overwrite existing password. Test credentials at `/app/memory/test_credentials.md`. **Live curl attestation:** POST /login with seeded admin → 200 + tokens with 6 roles (admin/operator/engineer/buyer/master_admin/dpo) + 1 KeyGrant (external/live_query/utterance/estate); POST /register new user → 201 + tokens with default role ask_console_user + 0 KeyGrants; GET /me with Bearer → 200 Identity; GET /me no auth → 401 `{reason:"auth_missing", detail:"Authentication required..."}`; GET /me bad token → 401 `{reason:"auth_missing", detail:"Authentication required..."}`; POST /login wrong password → 401 `{reason:"auth_missing", detail:"Invalid credentials."}`. Rule 2 accounting JSON extended 30 → 31 phases (transcription per Owner cap). Stage A E4 + E5 entries updated in `/app/docs/stage_a_proposals/phase_8.md` per Owner steer + standing ruling. Close report on-disk canonical: `/app/docs/close_reports/phase_8_b_1.md` SHA-256 `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`. **`pytest -q` = 777/777 GREEN. Frontend 47/47 GREEN across 7 UI-Spec-v1 suites.** |




## 3. Current Live State (rewritten by e1_dev at every phase close)
- Current gate: **PHASE 8 STAGE B-4 CLOSED (2026-07-05) — ACCEPTED BY OWNER 2026-07-05 (push at this close). MASTER ADMIN §6 SURFACE VERBATIM (§6.1 Home + §6.2 Change-a-rule + §6.3 What-I've-changed audit-trail) + JWT `master_admin` AUTH RECONCILIATION (`RMS_MASTER_ADMIN_TOKEN` RETIRED; ZERO PRODUCTION CONSUMERS) + PATH A `tier_lock` LEDGER INTEGRATION (versioned-file marker + `stamp_audit.data_class="master_admin_rule_change"` + idempotent per (rule_id, idempotency_key)) + PATH B HONEST 501 FOR `model_version` + `fleet_policy` (plain-language `detail`; no config syntax) + REAL SEAMS-PENDING ENUMERATION AT `GET /api/master_admin/pending_seams` (5 seams from env+config presence: Targeta yield / Mtafiti V3 / Northena retention / V2 cumulative-disclosure / MEA source-standing) + §4.2 FIXTURE-SCHEMA JEST INVARIANT (Owner amendment: fixtures aligned to ComposedConclusion_v0 / Service1Refusal_v0 / AsyncDeliveryAccepted_v1 shapes verbatim; subset-property-name check via fs.readFileSync of .contract_snapshot.json neighbours; drift caught at first-commit) + INLINE COLLAPSIBLE PRE-BLOCK AUDIT-TRAIL RATIFIED (Owner: modal/drawer below ruling altitude, no pivot).** Backend CI **855/855** (baseline 818 → +37 net); Jest ui_spec_v1 **72/72** (baseline 60 → +12); Playwright chromium **24/24** (baseline 16 → +8). Zero new frozen contracts (parity 26/26 byte-identical). Zero new §0.1 dispositions (§0.1 FROZEN). Zero new §0.2 debts (seams-pending banner is NOT a plan-debt per Owner amendment). Auth-reconciliation attested by grep-negative gate + `test_retired_master_admin_header_has_zero_runtime_effect` (401 auth_missing when `X-RMS-Master-Admin` sent without JWT); `grep RMS_MASTER_ADMIN_TOKEN /app/backend/.env` exits 1. All prior phases (0-7, 8a-lite, 8 A, 8 B-1, 8 B-2, 8 B-3) remain CLOSED and byte-identical.
- **Overage split-disposition (Owner-ruled 2026-07-05, verbatim):** *"Overage: split disposition, not B-1's blanket narrative. The mandate-forced portion — verbatim §6 binding copy + first-commit gates — is ratified on record. The backend-test underestimate (888 actual vs ~330 estimated, 2.7×) is an estimation miss, not a mandate; the orchestrator owns it. Standing correction, specific and binding on every future dispatch: Stage-A sizing enumerates the test matrix — endpoints × postures × cases — never a test-LoC lump. Every overage in this build traces to tests counted as a lump. B-4 is post-signal where B-1 was pre-signal: same magnitude, different meaning, recorded as such. [Owner ruling, Phase 8 B-4 close acceptance, 2026-07-05]"*
- **[Standing correction, Phase 8 B-4 close acceptance, 2026-07-05]** — Orchestrator Stage-A sizing enumerates the test matrix (endpoints × postures × cases), never a test-LoC lump. Binding on every future dispatch. **NOT a §0.1 disposition — internal to orchestrator template.** Live-state marker only.
- Phase 8 Stage B-4 deliverables (2026-07-05): 5 NEW backend Pytest gate files (888L: test_master_admin_tier_lock_path_a_ledger.py 197L + test_master_admin_auth_reconciliation.py 209L + test_master_admin_pending_seams.py 179L + test_master_admin_recent_actions.py 206L + test_pricing_paths_b_honest_501.py 97L) + 3 NEW backend source files pre-fork scaffold (routers/master_admin.py 142L + services/master_admin/pending_seams.py 114L + services/economics/tier_lock_ledger.py 98L; tier_lock_ledger idempotency-by-run_id correction landed at close) + 3 NEW frontend §6 pages (MasterAdminHomePage.js 161L + ChangeARulePage.js 271L + AuditTrailPage.js 195L) + 2 NEW Jest gates (test_engineer_first_call_fixture_matches_frozen_contracts.test.js 99L + test_master_admin_binding_copy_verbatim.test.js 190L) + 3 NEW Playwright chromium smokes (master_admin_home_smoke.spec.ts 93L + master_admin_change_a_rule_smoke.spec.ts 91L + master_admin_audit_trail_smoke.spec.ts 101L) + 6 MODIFIED (routers/pricing.py rewrite 247L JWT+PathA+PathB; test_phase_6_stage_b_economics.py Gate 16 JWT swap; apiClient.js +42L; App.js +12L; EngineerFirstCallPage.js fixture-export+shape-align refactor; e2e/engineer_surface_4_2_4_3_smoke.spec.ts +2L; backend/.env RMS_MASTER_ADMIN_TOKEN PURGED). Rule 2 accounting JSON extended 33→34 phases. Close report on-disk canonical `/app/docs/close_reports/phase_8_b_4.md` SHA-256 (pre-acceptance) `3cf03f809a7e19f9d275e2caddc3f1f75d68d29c33190504f4c37377892591b5` → (post-acceptance-footer) `21d2f5193cfb53fbb3ec2c35f293e78f7bd535f446a969137b4bad8758fdb3b8`.
- Retroactive B-3 cross-reference (Owner-ruled YES at B-4 close acceptance): fixture shape drifted at B-3 → CAUGHT AND FIXED at B-4 fixture-schema gate. B-3 close SHA (pre-acceptance) `c2863974bf52f69ff8b7256ad1bae07854a546526672c2d099305a98d01bec22` → (post-acceptance-footer) `a31b5a9d43c0563140a73762789f765390187052ebd4e24b47d9bb6a528f7215`.
- Doctrinal-tension resolutions (all Stage B-4 gates LOAD-BEARING GREEN post-8b-4): (a) 26 frozen contracts byte-identical — GREEN. (b) Shield boundary — GREEN (services/master_admin/* imports zero LLM libraries; §6 pages import zero LLM libraries). (c) §0.1 Standing Dispositions FROZEN — GREEN (zero additions). (d) Standing Rule v3 delivery — canonical markdown on-disk + SHA quoted; no full-text inline paste. (e) Substrate-drop 9/9 — GREEN. (f) Read-only route invariant (G5a) — GREEN (`/api/master_admin/pending_seams` + `/api/master_admin/audit_trail` are read-only reads over config-presence + ledger cursor). (g) Outer-gate irreversibility (G6). (h) V2 refusal terminality (G6). (i) Ruling 3 config-as-versioned-not-frozen — `tier_lock.vN.json` is a versioned marker file, not a frozen snapshot. (j) Ruling 4 shared-derivation. (k) Frozen-field-changes-as-new-versions — no in-place mutation. (l) Infra-not-refusal. (m) Auth-not-refusal — 403 does not wear governance-refusal clothes; master_admin auth denials use existing 4-code registry codes exclusively (0 new codes at B-4). (n) Wizard `wizard_transcript` retention marker preserved. (o) Disposition-must-cite-owner-ruling. (p) Sizing-anchor-declares-snapshot-inclusion — B-4 declared `snapshot_lloc_in_band=no` at Stage A + confirmed at close (2690 vs 1300-1800 anchored band → +49% ABOVE top-of-band, split-disposed). (q) Owner E1 federation-forward preserved. (r) Owner E1 no-hand-rolled-crypto. (s) E1 scope-enforcement per call. (t) E2 4-code bounded set preserved. (u) **NEW at B-4: `RMS_MASTER_ADMIN_TOKEN` retirement + `X-RMS-Master-Admin` header retirement — highest-privilege surface uses JWT `master_admin` role check exclusively; grep-negative gate GREEN across all runtime code paths.** (v) **NEW at B-4: `See full diff` collapsed-by-default — §6.3 Rule verbatim "the diff exists in the record; it is never the primary display" enforced by Playwright + Jest gates.** (w) **NEW at B-4: §4.2 illustrative fixtures aligned to their frozen contract shapes; Owner-amended Jest invariant makes drift-catch first-commit-enforced.**
- **Awaiting:** Owner push confirmation at this close. B-5 (Regulator/DPO §7 per UI Spec v1) SUPERSEDED by UI Spec v2.1 mandate ingested 2026-07-05 (see rows below). **Two Steps queued and NOT begun** per Owner Part-1-only dispatch: Step 2 (commercial cut per BCR v1.4 §12 — subtractive change with mandatory preservation) waits for Owner push confirmation; Step 3 (B-5a Compliance Console read/prove per UI Spec v2.1 §4.1-4.3) waits for cut close.
- **New canonical mandates ingested 2026-07-05 (record-only, no code changes):** (i) `/app/docs/mandates/RMS_UI_Specification_v2_1.md` SHA-256 `ef6da4b498117608a3091033b5cfa43571ad8a7a38b5954cae7c4a1a698de5e2` (176 paragraphs, 3 tables, 207 LoC; supersedes v1); (ii) `/app/docs/mandates/RMS_Build_Completion_Requirements_v1_4.md` SHA-256 `d1f49bc5d7cbf1dea044ca4069a1dc2d45f01876e531b7500d860ae3f48aebdd` (325 paragraphs, 2 tables, 341 LoC; §12 commercial cut + 14 requirement IDs CUT-1..4 / PRES-1..3-ALT / MAN-1 / MAN-G1..G3 / BND-1..2 all present). `RMS_UI_Specification_v1.md` marked SUPERSEDED at file top (Owner: do NOT delete prior files; retained for archive).
- Last green CI: **backend 855 / 855** at 2026-07-05 (baseline 818 → +37 net at Phase 8 Stage B-4). **Frontend Jest 72 / 72** at 2026-07-05 (baseline 60 → +12 net at B-4). **Playwright chromium smoke 24 / 24 EXECUTED** GREEN at 2026-07-05 (baseline 16 → +8 net at B-4). Webpack compilation GREEN. Substrate-drop gate 9/9 green. Mechanical parity invariant 3/3 green at **26** entries.
- Data source posture: unchanged. G2b measurement still BLOCKED.
- Canonical specs on-disk: 8/8 CURRENT (v1 marked SUPERSEDED; v2.1 + BCR v1.4 ingested and canonical).
- Frozen contracts: **26** (unchanged; B-4 introduced ZERO new frozen contracts).
- Wire surfaces: v2 dispatch Union unchanged. Wizard trilogy at 15 endpoints unchanged. Auth endpoints 4 at /api/auth/*. Operator status 1 at /api/operator/status. Engineer grant CRUD at /api/engineer/key_grants. **NEW at B-4: 2 read-only endpoints at /api/master_admin/{pending_seams, audit_trail} + 1 write endpoint `POST /api/pricing/tier_lock` (Path A ledger integration) + 2 honest-501 write endpoints `POST /api/pricing/model_version` + `POST /api/fleet/policy`.**
- E4 RESOLVED at B-3 (grant Pydantic runtime record UNFROZEN under D4b + 7 governance-key fields pinned by load-bearing wire-shape gate).
- Open HAZARD-STOP flags: **0**. Master Admin §6 landed cleanly. Ingest of v2.1 + BCR v1.4 clean, all expected anchors present.

## 3-legacy. Phase 8 Stage B-2 (superseded by §3 above; kept for continuity)
- Current gate: **PHASE 8 STAGE B-2 CLOSED (2026-07-05) — OPERATOR SURFACE §2 VERBATIM (§2.1 Home + §2.2 Commission Wizard + §2.3 CommitReview) + SESSION-BINDING DECORATOR WIRING ON OPERATOR WIZARD + GET /api/operator/status + SCOPE-ENFORCEMENT GATE PAIR ON POST /v2/dispatch (Owner E1+E2 symmetric-cut, ZERO envelope delta, curl-attested) + PLAYWRIGHT COMPLETION FIRST COMMIT (deps installed + chromium browser + test:e2e script + smoke EXECUTED GREEN)**. Playwright at B-1 was CONFIG-ONLY (deps absent, spec not executed); at B-2 first commit the gate LANDED — retroactive cross-reference for B-1's record: `/app/docs/close_reports/phase_8_b_1.md` SHA `b6d5c7a1...` (config surface only) → Playwright deps + smoke execution CLOSED at B-2 first commit (this close SHA `4a99dbf35cfda8f9207e1b2e0b18a9f4237bc5243c5235bc9056b3e60f04a305`). All prior phases (0-7, 8a-lite, 8 A, 8 B-1) remain CLOSED and byte-identical.
- Phase 8 Stage B-2 deliverables: 2 NEW backend files (routers/operator.py 78L + tests/invariants/test_phase_8_b_2_operator_and_scope_gate.py 330L 14 tests) + 3 MODIFIED backend files + 3 NEW frontend source files (OperatorHomePage.js 203L + CommissionWizardPage.js 243L + CommitReviewPage.js 250L) + 2 MODIFIED frontend files + 1 RENAMED e2e file + 1 devDependency + 1 script. Close report on-disk canonical: `/app/docs/close_reports/phase_8_b_2.md` SHA-256 `4a99dbf35cfda8f9207e1b2e0b18a9f4237bc5243c5235bc9056b3e60f04a305`.
- Last green CI at B-2 close: **backend 791 / 791**. **Frontend Jest 47 / 47** unchanged. **Playwright chromium smoke 1 / 1 EXECUTED** GREEN.
- Open HAZARD-STOP flags: **0** — Operator surface + session-binding wiring + scope-gate pair + Playwright completion all landed cleanly.

## 3-legacy. Phase 8 Stage B-1 (superseded by §3 above; kept for continuity)
- Current gate: **PHASE 8 STAGE B-1 CLOSED (2026-07-05) — AUTH/KEY MODEL + ENVELOPE-SHIM TRIAD EXTRACTION + SHARED UI-SPEC-V1 §8 BARREL + ASK CONSOLE-FULL AUTH WIRING + PLAYWRIGHT CHROMIUM SMOKE LIVE**. Auth/key model per Owner E1 ratified (custom JWT via PyJWT 2.13.0 + bcrypt 4.1.3, standard-library-only, no hand-rolled crypto). Endpoints at `/api/auth/*`: POST /register + POST /login + POST /refresh + GET /me. 4-code auth-refusal bounded set at `services/auth/auth_refusal_reasons.v0.json` (Ruling 3 versioned config, NOT frozen contract): auth_missing (401) / auth_expired (401) / auth_scope_insufficient (403) / auth_identity_mismatch_for_wizard_session (403). Body shape verbatim per Owner E2 non-negotiable: `{"reason": <code>, "detail": <string>}` — NO `outcome` key, NO `outcome=refused`, NO `AdmissionRefusal_v0` discriminator. Registry-exclusion gate GREEN. Console render-path gate GREEN (AuthDeniedNotice testid-namespace disjoint from RefusalCard's). Server-side per-call scope enforcement via `services/auth/key_grants.check_scope(identity, class, path, floor, scope)` — floor-hierarchy aware. Envelope-shim triad extraction (Owner E3 ratified) — `services/wizard/router_shims.py` (159L) canonical single-source; `admission_handoff.py` reduced 212 → 38L pure re-export shim (BC preserved). Grep-negative gate parametrised × 3 triad symbols × 2 routers = 6 GREEN gates. Shared UI-Spec-v1 §8 component barrel at `components/ui_spec_v1/index.js` — re-exports 6 v1 §8 shared components + AuthDeniedNotice. Ask Console-full auth wiring: `apiClient.js` (+67L) + `hooks/useAuth.js` (88L) + `pages/AuthLoginPage.js` (131L) + `pages/AuthRegisterPage.js` (132L) + `App.js` (+5L AuthProvider wrap + /auth/login + /auth/register routes). Playwright chromium-only e2e config (Owner E7 ratified) + 1 authenticated smoke scenario. Wizard session-ownership binding plan-debt RESOLVED at B-1 (§0.2 debt named receiver: `services/auth/session_binding.py` sidecar table + grandfathering carve-out). Envelope-shim triad extraction plan-debt RESOLVED at B-1. Zero new frozen contracts (parity 26 unchanged); zero new §0.1 Standing Dispositions (§0.1 FROZEN). All Phase 7 Stage B-3, 8a-lite, and prior phases (0-7) remain CLOSED and byte-identical.
- Phase 8 Stage B-1 deliverables (2026-07-05): (i) **12 NEW backend source files** — `services/auth/__init__.py` 28L + `identity.py` 81L + `password_hash.py` 25L + `jwt_service.py` 99L + `auth_refusal.py` 63L + `auth_refusal_reasons.v0.json` 22L + `key_grants.py` 94L + `user_store.py` 111L + `session_binding.py` 69L + `dependencies.py` 100L + `routers/auth.py` 146L + `services/wizard/router_shims.py` 159L. (ii) **1 NEW backend test file** — `tests/invariants/test_phase_8_b_1_auth_and_shims.py` 393L (37 gates covering E1+E2+E3). (iii) **3 MODIFIED backend files** — `services/wizard/admission_handoff.py` 212→38L (pure re-export shim; -174L extraction), `server.py` +9L (auth router registration + admin seed on startup + auth indexes), `.env` +3 vars (JWT_SECRET/ADMIN_EMAIL/ADMIN_PASSWORD), `requirements.txt` +2 deps (PyJWT==2.13.0/bcrypt==4.1.3). (iv) **7 NEW frontend source files** — `components/AuthDeniedNotice.js` 86L + `components/ui_spec_v1/index.js` 28L + `hooks/useAuth.js` 88L + `pages/AuthLoginPage.js` 131L + `pages/AuthRegisterPage.js` 132L. (v) **2 NEW frontend test files** — `__tests__/ui_spec_v1/test_auth_denied_notice_not_refusal_card.test.js` 104L + `test_shared_components_single_source.test.js` 68L. (vi) **2 NEW frontend e2e files** — `playwright.config.js` 26L + `e2e/ask_console_smoke.spec.js` 64L. (vii) **2 MODIFIED frontend files** — `apiClient.js` 40→107L (+67L: auth endpoints + Bearer interceptor + tokenStore + formatApiErrorDetail helper), `App.js` 44→49L (+5L: AuthProvider + auth routes). (viii) **1 close report** at `/app/docs/close_reports/phase_8_b_1.md` SHA-256 `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`. (ix) **Rule 2 accounting JSON extended 30 → 31 phases** (transcription-only per Owner cap; snapshot_lloc_in_band=no). (x) **Stage A E4 + E5 entries updated in `/app/docs/stage_a_proposals/phase_8.md`** per Owner steer (E4: grant schema as Pydantic runtime record; freeze-or-not argued at B-3 open) + standing ruling (E5: DPO surface renders inheritance-as-default with wizard_transcript separately-addressable). (xi) **Zero new §0.1 Standing Dispositions**. (xii) **2 §0.2 Plan Debts RESOLVED** (wizard session-ownership binding + envelope-shim helper triad extraction). (xiii) **Test credentials at `/app/memory/test_credentials.md`** (admin@rms.example.com / admin-b1-test-pw with 6 roles + 1 KeyGrant seeded via startup).
- Doctrinal-tension resolutions (all Stage B-1 gates LOAD-BEARING GREEN post-8b-1): (a) 26 frozen contracts byte-identical — GREEN. (b) Shield boundary (LLM only inside `services/synisense/shield/*`) — GREEN (auth module has zero litellm/anthropic/emergentintegrations imports). (c) §0.1 Standing Dispositions FROZEN — GREEN (zero additions). (d) Standing Rule v3 delivery — on-disk canonical + SHA quoted in return. (e) Substrate-drop 9/9 — GREEN. (f) Read-only route invariant (G5a) — GREEN. (g) Outer-gate irreversibility (G6) — GREEN. (h) V2 refusal terminality (G6) — GREEN. (i) Ruling 3 config-as-versioned-not-frozen — auth_refusal_reasons.v0.json follows pattern. (j) Ruling 4 shared-derivation — auth module does not re-implement any wizard/service_1 primitive. (k) Frozen-field-changes-as-new-versions — no in-place mutation. (l) Infra-not-refusal — preserved (auth is a distinct fourth class, not infra fault). (m) **NEW at B-1: Auth-not-refusal — the fourth render path is OUTSIDE the three governance paths, not wearing their clothes (Owner E2 non-negotiable, enforced by console render-path gate + registry-exclusion gate).** (n) Wizard `wizard_transcript` retention marker preserved. (o) Disposition-must-cite-owner-ruling — §0.2 RESOLUTION entries carry citation headers. (p) Sizing-anchor-declares-snapshot-inclusion — B-1 declared `snapshot_lloc_in_band=no` at Stage A + verified at close. (q) Owner E1 federation-forward posture preserved (JWT claim shape is invariant; OAuth adapters mint same shape later). (r) Owner E1 no-hand-rolled-crypto — PyJWT + bcrypt only. (s) Owner E3 grep-negative gate parametrised × 3 triad symbols — GREEN.
- Awaiting: Owner acceptance of Phase 8 Stage B-1 close report at `/app/docs/close_reports/phase_8_b_1.md` SHA `b6d5c7a1ea0aaffa7b2a27dc31d96fd8c64f1ff071caf75913ffe6dde6c3f1fe`. Ready for Phase 8 Stage B-2 (Operator surface per UI Spec §2) dispatch.
- Last green CI: **backend 777 / 777** at 2026-07-05 (baseline 740 → +37 net at Phase 8 Stage B-1). **Frontend 47 / 47** across 7 UI-Spec-v1 suites at 2026-07-05 (baseline 27 → +20 net at B-1). Webpack compilation GREEN. Substrate-drop gate 9/9 green. Mechanical parity invariant 3/3 green at **26** entries.
- Data source posture: unchanged. G2b measurement of price/delivery/fleet real values still BLOCKED.
- Canonical specs on-disk: 7/7 CURRENT.
- Frozen contracts: **26** (unchanged; B-1 introduced ZERO new frozen contracts).
- Wire surfaces: v2 dispatch Union unchanged. Wizard trilogy at 15 endpoints (operator 7, buyer 8) unchanged. **NEW at B-1: 4 auth endpoints at `/api/auth/*`** (register/login/refresh/me).
- **Closed seams (unchanged from Phase 7 Stage B-3 + zero new seams at B-1):** all remain gated. Phase 8 Stage B-1 introduced ZERO new seams.
- Discipline observation carried forward (X1): unchanged.
- Open HAZARD-STOP flags: **0** — Auth landing complete, envelope-shim triad extracted, shared UI-Spec-v1 barrel formalised, no known regressions.

## 3-legacy. Phase 7 Stage B-3 (superseded by §3 above; kept for continuity)
- Current gate: **PHASE 7 STAGE B-3 CLOSED (2026-07-04) — WIZARD TRILOGY COMPLETE**. Commit-review extensions (both variants: `license_class_drift`; buyer only: `dual_delta_summary`) + buyer freeze ledger parity with operator (writes `record_wizard_freeze` + `ledger_run_id` in response + `lawful_basis_ref` body param) + admission handoff endpoints on both variants (`POST /api/wizard/{variant}/{sid}/handoff` → mints `ObjectiveRequest_v2` via `services/wizard/admission_handoff.compose_objective_request_from_frozen_state_with_proposals` → POSTs to `/api/objectives` in-process via `httpx.AsyncClient(ASGITransport)` → returns HTTP 202 AsyncDeliveryAccepted_v1 | 422 wizard_not_frozen | 422 AdmissionRefusal_v0 passthrough | 404 unknown session | 503 infra). Operator surface: 6 → 7 endpoints (adds /handoff). Buyer surface: 7 → 8 endpoints (adds /handoff). NEW module `services/wizard/admission_handoff.py` (211L; pure fn; no LLM; no I/O; no network — verified by `test_admission_handoff_pure_no_llm_imports` LB GREEN). Zero new frozen contracts (parity 26 unchanged); zero new §0.1 Standing Dispositions (§0.1 FROZEN); zero new §0.2 Plan Debts. **No new refusal codes at B-3** per Owner ruling verbatim (registered admission_refusal + service_1_refusal registries unchanged; `wizard_not_frozen` is an ad-hoc router-layer 422 body, NOT registered as an admission-refusal reason). Owner Condition-2 grep-negative gates × 3 confirm `admission_handoff.py` imports (does NOT reimplement) `derive_license_class` + `_record_feasibility_snapshot` + `evaluate_dual_delta`. Deterministic idempotency_key `handoff-{session_id}` — repeat handoff returns same objective_id per Phase 5 §7 guarantee (E2E verified: same objective_id=`obj-fc9c056a48c1` across two calls). Dual-delta acceptance recording persists through handoff into `envelope.floor_feasibility["dual_delta_summary"]` (buyer only; open-shape dict per Substrate-Drop v2 Part 2). Owner E5 wizard-transcript retention class marker preserved (buyer freeze now writes ledger via `record_wizard_freeze`). Shield boundary preserved (services/wizard/* remains LLM-free). Phase 7 Stage B-1 + Phase 7 Stage B-2 + Phase 8a-lite + all prior phases (0-6) CLOSED. Backend surface FROZEN except additive-only B-3 landing (1 new source file 211L + 1 new test file 574L + 1 new Stage A doc + 2 modified routers +277L + 2 modified support files ±92L; ZERO v0 mutations; ZERO SHA drift on any of the 26 prior frozen contract source files verified by `test_prior_contract_file_exists_and_stable_at_7b_3` parametrised over 25 sources).
- Phase 7 Stage B-3 deliverables (2026-07-04): (i) **1 NEW source module** — `services/wizard/admission_handoff.py` (211L; `compose_objective_request_from_frozen_state(wizard_state)` + `compose_objective_request_from_frozen_state_with_proposals(wizard_state, proposals)` + `summarise_dual_deltas(proposals)`). (ii) **1 NEW test file** — `tests/invariants/test_phase_7_stage_b_3_wizard.py` (574L; 30 named gates + parametrised expansions = ~55 collected new cases). (iii) **1 NEW Stage A doc** — `/app/docs/stage_a_proposals/phase_7_stage_b_3.md` (232L) SHA-256 `040c4099aa031ddb4b8133ac64f301457e6bdafc13ad12cf5c58fb1ba90f4698`. (iv) **2 MODIFIED routers** — `routers/wizard_buyer.py` +176L (commit-review dual_delta_summary + license_class_drift extensions; freeze ledger call + lawful_basis_ref body param + ledger_run_id response; NEW /handoff endpoint; envelope-shim helper triad); `routers/wizard_operator.py` +101L (commit-review license_class_drift extension only; NEW /handoff endpoint; envelope-shim helper triad). (v) **2 MODIFIED support files** — `tests/invariants/test_phase_7_stage_b_2_wizard.py` ±32L (4 point-in-time gate adjustments to assert-in-set for B-2/B-3 postures); `docs/lift_manifest.json` ±60L (3 legacy `__tests__/*.js` entries retargeted to `frontend/src/__tests__/ui_spec_v1/*.js` per 8a-lite copy-forward). (vi) **1 close report** at `/app/docs/close_reports/phase_7_stage_b_3.md` SHA-256 `ea12517cec7deee48818a097e942d08601fda5a0f381e215a7df2c508c801c30`. (vii) **Rule 2 accounting JSON extended 28 → 30 phases** (transcription-only per Owner cap; served through `/api/discipline/lift_manifest` — verified 30 keys live). (viii) **Zero new §0.1 Standing Dispositions**. (ix) **Zero new §0.2 Plan Debts**.
- Doctrinal-tension resolutions (all Stage B-3 gates LOAD-BEARING GREEN post-7b-3): (a) Ruling 4 shared-derivation — preserved; admission_handoff.py imports `derive_license_class` from `services.service_1.license_class_selection`; NEVER re-implements `_record_feasibility_snapshot`; single-source LB grep-negative × 3 GREEN. (b) Ruling 3 wire-shape gate — preserved; composed ObjectiveRequest_v2 passes `model_validate` on every handoff call. (c) Infra-not-refusal — preserved; async admission's existing infra fault → 503 passes through ASGI transport. (d) Frozen-field-changes-as-new-versions — 26 prior contract source files SHA-identical. (e) No new refusal codes at B-3 — LB gate GREEN (`wizard_not_frozen` is router-layer precondition, NOT admission-refusal code). (f) Owner E5 wizard-transcript retention class marker — preserved via `record_wizard_freeze` structural in buyer freeze at B-3. (g) Owner E4 proof-order — preserved from B-2. (h) Owner E7 provenance-preservation single-source — preserved; admission_handoff.py imports from proven modules. (i) Standing Rule v3 delivery — close report on-disk canonical + SHA. (j) Idempotency (Phase 5 §7 guarantee) — deterministic `idempotency_key = f"handoff-{session_id}"` → repeat handoff returns same objective_id (E2E verified via curl). (k) Dual-delta acceptance recording (buyer only) — persists through handoff into `envelope.floor_feasibility["dual_delta_summary"]` (LB gate GREEN).
- Awaiting: Owner acceptance of Phase 7 Stage B-3 close report at `/app/docs/close_reports/phase_7_stage_b_3.md` SHA `ea12517cec7deee48818a097e942d08601fda5a0f381e215a7df2c508c801c30`. Hold before Phase 8 full dispatch (Operator / Engineer / Buyer / Master Admin / DPO surfaces per UI Spec §2).
- Last green CI: **backend 740 / 740** at 2026-07-04 (baseline 685 → +55 net at Phase 7 Stage B-3). **Frontend 27 / 27** across 5 UI-Spec-v1 suites unchanged from 8a-lite. Production `yarn build` GREEN. Substrate-drop gate 9/9 green. Mechanical parity invariant 3/3 green at **26** entries.
- Data source posture: unchanged. G2b measurement of price/delivery/fleet real values still BLOCKED.
- Canonical specs on-disk: 7/7 CURRENT.
- Frozen contracts: **26** (unchanged; B-3 introduced ZERO new frozen contracts).
- Wire surfaces: v2 dispatch Union unchanged. Wizard trilogy complete at 15 endpoints across two variants (operator 7, buyer 8). New /handoff endpoint on both variants delivers `AsyncDeliveryAccepted_v1 @202` (via ASGI transport to existing `/api/objectives` handler; single-source).
- **Closed seams (unchanged from Phase 7 Stage B-2 + zero new seams at B-3):** all remain gated. Phase 7 Stage B-3 introduced ZERO new seams.
- Discipline observation carried forward (X1): unchanged.
- Open HAZARD-STOP flags: **0** — Wizard trilogy closed, admission handoff live, all doctrinal LBs GREEN.

## 3-legacy. Phase 7 Stage B-2 (superseded by §3 above; kept for continuity)
- Phase 8a-lite deliverables (2026-07-04): (i) **NEW page** `src/pages/AskConsolePage.js` (610L; §3 verbatim UI + §7 accepted branch + infra-fault branch). (ii) **EXTENDED** `src/apiClient.js` (+18L; `dispatchV2(objectiveRequestV2)` method with `validateStatus: (s) => s >= 200 && s < 500`). (iii) **REWROTE** `src/App.js` (rewrite ~43L; `/` → AskConsolePage, `/legacy/*` → legacy G5b shell, catch-all → `Navigate to="/"`). (iv) **5 NEW UI-Spec-v1 gate test files** at `src/__tests__/ui_spec_v1/` (481L total): 2 new tests (`test_g5b_legacy_pages_archived_under_frontend_legacy_directory` 4 gates + `test_no_output_form_picker_present_on_ask_surface` 6 gates) + 3 re-landed G5b gates (Gate 1 Class inseparable 7 tests + Gate 2 Refusal first-class + validation distinguishability 6 tests + Gate 3 Single ingress + trace_id retention 4 tests). (v) **MECHANICAL IMPORT-PATH ADJUST** on 8 archived legacy pages (`src/legacy/pages/*.js`): `../hooks/` → `../../hooks/`, `../apiClient` → `../../apiClient`, `../components/` → `../../components/`. (vi) **3 DELETED files** at `src/legacy/__tests__/*.test.js` (superseded by copy-forward per Owner ruling verbatim). (vii) **Zero new §0.1 Standing Dispositions** (§0.1 FROZEN). (viii) **Zero new §0.2 Plan Debts** — the Wizard session-ownership binding debt from Phase 7 Stage B-2 still holds and lands with Phase 8 auth/key model. (ix) **Zero backend LoC / zero backend surface delta** — `pytest -q` = 685 / 685 GREEN pre- and post-phase.
- Doctrinal-tension resolutions at 8a-lite (all frontend-doctrine-facing): (a) infra-not-refusal — `AskConsolePage.catch(e)` branch routes network/5xx errors to `InfraFaultView` with clear "infrastructure fault, not a governance decision" copy. (b) Refusal-first-class body-shape (A2 doctrine) — `apiClient.dispatchV2` uses `validateStatus: (s) => s >= 200 && s < 500` so 422 refusals are returned as `body`, not thrown; `isRefusal(body)` discriminates on `body.outcome === "refused"`, NOT status code. (c) §3.1 no-picker — 6 gates GREEN. (d) Ruling 3 wire-shape gate — Ask Console consumes ObjectiveRequest_v2 with strictly frozen shape (verified by curl smoke returning HTTP 202 AsyncDeliveryAccepted_v1). (e) Standing Rule v3 delivery — close report on-disk canonical + SHA; no full-text inline pastes. (f) Owner Condition-2-flavored posture (component reuse) — AskConsolePage reuses shared `ClassBadge` + `RefusalCard` unchanged; Gate 3 re-landing reuses shared `LedgerTable` + `TrustReceiptLink` unchanged.
- Awaiting: Owner acceptance of Phase 8a-lite close report at `/app/docs/close_reports/phase_8a_lite.md` (SHA `bf4ba9a94f250abad61d33a842bdedf2e7c8571a3fe61b1d3323c25601dbe888`). Held before Phase 8 full ratification.
- Last green CI: **backend 685 / 685** unchanged at 2026-07-04. **Frontend 27 / 27** across 5 UI-Spec-v1 suites at 2026-07-04. Production `yarn build` GREEN. Substrate-drop gate 9/9 green. Mechanical parity invariant 3/3 green at 26 entries.
- Data source posture: unchanged. G2b measurement of price/delivery/fleet real values still BLOCKED.
- Canonical specs on-disk: 7/7 CURRENT.
- Frozen contracts: **26** (unchanged; 8a-lite is frontend-only).
- v2 dispatch wire Union (unchanged): `Union[DispatchResult @501, AdmissionRefusal_v0 @422, ComposedConclusion_v0 @200, Service1Refusal_v0 @422, QualifiedDataPayload @200, AsyncDeliveryAccepted_v0/v1 @202]` + HTTP 503 out-of-Union (infra-not-refusal). New frontend consumer: `api.dispatchV2(...)` at `src/apiClient.js`.
- **Closed seams (unchanged from Phase 7 Stage B-2 + zero new seams at 8a-lite):** all remain gated. Phase 8a-lite introduced ZERO new seams.
- Open HAZARD-STOP flags: **0** — Ask Console live, human-visible surface unblocked, no known regressions.

## 3-legacy. Phase 7 Stage B-2 (superseded by §3 above; kept for continuity)
- Phase 7 Stage B-2 deliverables (2026-07-04): (i) **3 NEW source files** — `services/wizard/dual_delta.py` (91L; declarative-table pattern mirroring `provenance_preservation.py`; Owner E6 `Visibility-not-prohibition` mechanical application), `services/wizard/buyer_state_machine.py` (332L; mirrors operator SM shape; imports `_record_feasibility_snapshot` + `validate_source_tags` + `validate_guard_1_operator_mandatory_all_operator_supplied` from operator-proven modules per Owner Condition 2), `routers/wizard_buyer.py` (267L; 7 endpoints mounted at `/api/wizard/buyer/*`). (ii) **1 NEW test file** — `test_phase_7_stage_b_2_wizard.py` (835L; 38 named gates + parametrised expansions = 72 collected cases). (iii) **5 MODIFIED source files** — `services/synisense/shield/llm_router.py` +196 (`SonnetWizardAgent` class + `_sonnet_invoke` helper + no-silent-degrade path); `services/wizard/operator_state_machine.py` +12 (Condition A(i) predicate at `record_agent_assumption` + `variant` kwarg default operator); `routers/wizard_operator.py` +18 (`SourceTagViolation` import + 422 boundary for Condition A(i)); `services/wizard/session_persistence.py` +4 (additive compound index on `variant + session_id`); `server.py` +2 (buyer router mount). (iv) **1 docs-only fix** — `docs/lift_manifest.json` +6 (pre-existing 8a-lite path repair carry-through). (v) **0 NEW Standing Owner Dispositions** at §0.1 (§0.1 FROZEN per Owner correction). (vi) **1 NEW Plan Debt** at §0.2: `Wizard session-ownership binding plan-debt` — verbatim: *"Wizard session-ownership binding lands with Phase 8 auth/key model — recorded as the system-wide auth landing, not a wizard-special. [Owner ruling, Phase 7 Stage B-2 dispatch, 2026-07-04]"*. (vii) **Rule 2 accounting JSON backfill** — `/app/docs/rule2_accounting.json` extended 9 → 28 phases (transcription-only from ORCHESTRATOR §2 + PHASE_STATE row narratives per Owner cap). (viii) **PHASE_STATE.md dedupe** — three duplicated section headers collapsed to a single authoritative section each; git-stat 28 insertions / 93 deletions.
- Doctrinal-tension resolutions (all Stage B-2 gates LOAD-BEARING GREEN post-7b-2): (a) Ruling 2 (Literal-widening HAZARD-STOP) — unchanged; no Literal touched. (b) Ruling 3 (wire-shape LB gate) — unchanged, GREEN. (c) Ruling 4 (shared-derivation) — unchanged; buyer SM imports `_record_feasibility_snapshot` from operator-proven module which itself invokes `derive_floor_feasibility(...)` — LB grep-negative × 3 GREEN. (d) Ruling 5 — unchanged. (e) Elevated Doctrine — no validators added to frozen contracts. (f) Loose-as-frozen — unchanged. (g) frozen-field-changes-as-new-versions — 26 prior contracts SHA-identical (parametrised regression gate GREEN over 25 sources + count invariant). (h) infra-not-refusal — LOAD-BEARING at B-2: `SonnetWizardAgent` Sonnet-4.6-unavailable → HTTP 503, NEVER an `AdmissionRefusal_v0` / `Service1Refusal_v0` shape (3 shape-negative gates GREEN). (i) cancellation-is-a-state-not-a-refusal — untouched. (j) Disposition-must-cite-owner-ruling — enforced; new Plan Debt carries `[Owner ruling, Phase 7 Stage B-2 dispatch, 2026-07-04]` citation header. (k) Inline-delivery-scope-amended-v3 — applied at this close report (on-disk canonical `/app/docs/close_reports/phase_7_stage_b_2.md` SHA quoted in return message). (l) Owner E1 branch-discrimination — preserved from B-1 (unchanged). (m) Owner E5 wizard-transcript retention class marker — preserved from B-1. (n) Owner E4 proof-order (Agent-pluggable-with-stub-agent-first) — HONOURED at B-2: `WizardAgent` Protocol from B-1 unchanged; `SonnetWizardAgent` is a NEW implementation of same interface; `DeterministicStubAgent` still runs all B-1 gates unchanged (LB gate `test_sonnet_wizard_agent_implements_wizard_agent_protocol` GREEN). (o) Owner E6 (Visibility-not-prohibition mechanical application) — LOAD-BEARING at B-2: dual-delta gate refuses `dual_delta_missing`, NOT `proposal_refused`; visibility on-wire is mandatory not prohibitive. (p) Owner E7 provenance-preservation single-source — LB grep-negative preserved; buyer SM imports `provenance_preservation` (single-source unchanged). (q) NEW LB at B-2: Owner Condition A(i) — `record_agent_assumption(variant="operator", field_name=<mandatory>)` raises `SourceTagViolation` at `services/wizard/operator_state_machine.py:189-192`; endpoint boundary at `routers/wizard_operator.py:159-171` returns HTTP 422 with `{"violations": [...], "refused": True}`. (r) NEW LB at B-2: Owner Condition A(ii)/(iii) — endpoint never mints operator-source `CommittedValue` nor appends `OperatorTurn` (structural + LB gates GREEN). (s) NEW LB at B-2: Owner Condition 2 grep-negative single-source × 3 symbols — buyer SM re-implements NONE of `validate_source_tags` / `validate_guard_1_operator_mandatory_all_operator_supplied` / `_record_feasibility_snapshot`.
- Awaiting: Owner acceptance of Phase 7 Stage B-2 close report at `/app/docs/close_reports/phase_7_stage_b_2.md` (SHA quoted in return message). Hold before Phase 7 Stage B-3 dispatch (commit-review + buyer freeze + admission handoff to `POST /api/objectives`) AND before Phase 8a-lite ratification (Ask Console frontend in-flight per Owner ruling 2026-07-04).
- Last green CI: **685/685** backend at 2026-07-04 (baseline 613 → +72 net at Phase 7 Stage B-2). Frontend: 18/18 gate tests unchanged from G5b (now under `/app/frontend/src/legacy/__tests__/` per 8a-lite in-flight archival). Substrate-drop gate 9/9 green. Mechanical parity invariant 3/3 green at **26** entries.
- Data source posture: unchanged. G2b measurement of price/delivery/fleet real values still BLOCKED.
- Canonical specs on-disk: 7/7 CURRENT.
- Frozen contracts: **26** (unchanged from Phase 7 Stage B-1 close). All 26 have canonical `.contract_snapshot.json` files; mechanical parity invariant enforces bijection at 26 entries. Frontend surface count: pending 8a-lite ratification (legacy G5b pages relocated to `src/legacy/`).
- v2 dispatch wire Union (unchanged shape from 7b-1): `Union[DispatchResult @501, AdmissionRefusal_v0 @422, ComposedConclusion_v0 @200, Service1Refusal_v0 @422, QualifiedDataPayload @200, AsyncDeliveryAccepted_v0/v1 @202 (with populated `quote`)]` + HTTP 503 branch out-of-Union (infra-not-refusal). Wizard surface additions at B-2: 7 new buyer endpoints under `/api/wizard/buyer/*`. Operator surface at B-2: 6 endpoints at `/api/wizard/operator/*` unchanged (Condition A(i) 422 boundary is an additive edit at the existing `/agent-assumption` path).
- **Closed seams (unchanged from Phase 7 Stage B-1 + zero new seams introduced at Phase 7 Stage B-2):** all remain gated. Phase 7 Stage B-2 introduced ZERO new seams.
- Discipline observation carried forward (X1): unchanged.
- Open HAZARD-STOP flags: **0** — all Stage B-2 gates GREEN; retroactive citation audit UNCHANGED (all §0.1 entries carry citation headers; §0.1 FROZEN per Owner correction). 6th-endpoint HAZARD-STOP resolved via Owner Condition-A landing at Block A.

## 3-legacy. Phase 7 Stage B-1 (superseded by §3 above; kept for continuity)
- Prior gate at close: **PHASE 7 STAGE B-1 CLOSED (2026-07-04)** — shaping wizard §3.3 operator variant + 4 net-new frozen contracts + license-class Option C wrap + provenance-preservation shared-derivation module landed live at 6 endpoints under `/api/wizard/operator/*`. Uses `DeterministicStubAgent` (NO LLM at B-1 per Standing Disposition `Agent-pluggable-with-stub-agent-first`). Guard 1/2/3 discipline structurally enforced on WizardCommitState_v0 + CommittedValue_v0 (XOR invariant) + OperatorTurn_v0 (min_length feasibility_snapshot_ref). License-class Option C wrap primary-arm gate landed additively; fallback body slice byte-identical (SHA `ca3b2007...`). Turn ledger stamp_audit sidecar carries `data_class="wizard_transcript"` per Owner E5. Close report on-disk canonical: `/app/docs/close_reports/phase_7_stage_b_1.md` SHA `b34fc38eb69804165dcf1a9eb65351a0c6b0a4648895c17e5c4b408b7b635d9e`.

## 4. Frozen Contracts (do not mutate without explicit re-bless)

Twenty-six frozen contracts (10 pre-G6 + 3 additions at G6 + 1 at A2 + 1 at Substrate-Drop v2 Phase 0 + 1 at Phase 1 + 1 at Phase 3 + 1 at Phase 4b + 2 at Phase 5 Stage B + 2 at Phase 6 Stage B + 4 at Phase 7 Stage B-1). All snapshot tests live in `/app/backend/tests/invariants/`.

| # | Contract file | Snapshot file(s) | Invariant test(s) | Added at |
|---|---|---|---|---|
| 1 | `backend/contracts/five_rings.py` | `five_rings.contract_snapshot.json` | `test_invariant_contract_snapshots.py` | pre-G0 |
| 2 | `backend/contracts/objective_request.py` | `objective_request.contract_snapshot.json` | `test_invariant_contract_snapshots.py` | pre-G0 |
| 3 | `backend/contracts/qualification_matrix/loader.py` | `qualification_matrix.schema_snapshot.json` + `.v0.content_snapshot.json` | `test_invariant_contract_snapshots.py` | pre-G0 |
| 4 | `backend/contracts/signal_ring.py` | `signal_ring_dimensions.v0.content_snapshot.json` | `test_signal_ring_dimensions_v0.py` | pre-G2 |
| 5 | `backend/contracts/extraction_params.py` | `extraction_params.v0.content_snapshot.json` | `test_extraction_params_v0.py` | pre-G2 |
| 6 | `backend/contracts/northena_ledger.py` | `northena_ledger_row.contract_snapshot.json` | `test_northena_ledger_row_v0.py` + `test_ledger_absorbs_outer_gate_and_v2_via_stamp_audit.py::test_northena_ledger_row_contract_snapshot_unchanged_at_g6` (G6 invariant guarding no-mutation) | G2a |
| 7 | `backend/contracts/mtafiti_registry.py` | `mtafiti_registry_record.contract_snapshot.json` | `test_mtafiti_invariants.py::test_registry_record_contract_frozen` | G4 |
| 8 | `backend/contracts/targeta_plan.py` | `targeta_mining_plan.contract_snapshot.json` | `test_targeta_invariants.py::test_mining_plan_contract_frozen` | G4 |
| 9 | `backend/contracts/trace_lens.py` | `trace_lens_envelope.contract_snapshot.json` | `test_trace_lens_cross_engine_correlation.py::test_trace_lens_envelope_contract_frozen` | G5a |
| 10 | `backend/contracts/lift_manifest_response.py` | `lift_manifest_envelope.contract_snapshot.json` | `test_trace_lens_cross_engine_correlation.py::test_lift_manifest_envelope_contract_frozen` | G5a |
| 11 | `backend/contracts/outer_gate_receipt.py` (OuterGateReceipt) | `outer_gate_receipt.contract_snapshot.json` | `test_outer_gate_irreversibility.py::test_outer_gate_receipt_contract_frozen` | **G6** |
| 12 | `backend/contracts/v2_refusal.py` (V2RefusalEnvelope) | `v2_refusal_envelope.contract_snapshot.json` | `test_v2_gate_refusal_cumulative.py::test_v2_refusal_envelope_contract_frozen` | **G6** |
| 13 | `backend/contracts/cumulative_disclosure.py` (CumulativeDisclosureLedger) | `cumulative_disclosure_ledger.contract_snapshot.json` | `test_v2_gate_refusal_cumulative.py::test_cumulative_disclosure_ledger_contract_frozen` | **G6** |
| 14 | `backend/contracts/service_1_refusal.py` (Service1Refusal) | `service_1_refusal.contract_snapshot.json` | `test_service_1_refusal_envelope.py::test_service_1_refusal_schema_frozen` | **A2** |
| 15 | `backend/contracts/objective_request_v2.py` (ObjectiveRequest_v2) | `objective_request_v2.contract_snapshot.json` | `test_objective_request_v2_contract_frozen.py::test_objective_request_v2_contract_frozen` | **Substrate-Drop v2, Part 2 (Phase 0), 2026-07-03** |
| 16 | `backend/contracts/feasibility_result.py` (FeasibilityResult_v0) | `feasibility_result.contract_snapshot.json` | `test_feasibility_result_v0_contract_frozen.py::test_feasibility_result_v0_contract_frozen` | **Phase 1 (Estate Feasibility Query), 2026-07-03** |
| 17 | `backend/contracts/admission_refusal.py` (AdmissionRefusal_v0) | `admission_refusal.contract_snapshot.json` | `test_admission_refusal_v0_contract_frozen.py::test_admission_refusal_v0_contract_frozen` + reason-registry & family gates at `test_admission_refusal_dispatch.py` | **Phase 3 (Admission-Refusal Envelope, unified §6.5 + future admission reasons via registry), 2026-07-03** |
| 18 | `backend/contracts/composed_conclusion.py` (ComposedConclusion_v0) | `composed_conclusion.contract_snapshot.json` | `test_composed_conclusion_v0_contract_frozen.py::test_composed_conclusion_v0_contract_frozen` + Condition B1 gate at `test_composed_conclusion_class_from_solva_boundary.py` + §6.2 dispatch gates at `test_composed_conclusion_dispatch.py` | **Phase 4b (§6.2 composed-conclusion path + 18th frozen contract), 2026-07-04** |
| 19 | `backend/contracts/northena_ledger_v1.py` (NorthenaLedgerRow_v1) | `northena_ledger_v1.contract_snapshot.json` | `test_phase_5_stage_b_async_delivery.py::test_northena_ledger_v1_contract_snapshot_matches` + `test_northena_ledger_v1_supersets_v0` | **Phase 5 Stage B (Async delivery §7; first application of Standing Owner Disposition `frozen-field-changes-as-new-versions`), 2026-07-04** |
| 20 | `backend/contracts/async_delivery_accepted.py` (AsyncDeliveryAccepted_v0) | `async_delivery_accepted.contract_snapshot.json` | `test_phase_5_stage_b_async_delivery.py::test_async_delivery_accepted_v0_contract_snapshot_matches` + `test_accepted_body_wire_shape_pins_governance_keys` + `test_governance_travels_inline_on_async_response_body` | **Phase 5 Stage B (v3 §7 §7.1 acceptance envelope; 202 body from `POST /api/objectives`), 2026-07-04** |
| 21 | `backend/contracts/quote_envelope.py` (QuoteEnvelope_v0 + QuoteInstrumentationSeed_v0) | `quote_envelope.contract_snapshot.json` | `test_phase_6_stage_b_economics.py::test_quote_envelope_frozen_at_v0` + `test_pricing_tier_not_a_literal` + `test_delivery_time_has_exactly_two_bands` + `test_delivery_time_never_reports_gpu_numbers_on_buyer_surface` | **Phase 6 Stage B (v3 §8 economics quote envelope), 2026-07-04** |
| 22 | `backend/contracts/async_delivery_accepted_v1.py` (AsyncDeliveryAccepted_v1) | `async_delivery_accepted_v1.contract_snapshot.json` | `test_phase_6_stage_b_economics.py::test_async_delivery_accepted_v1_supersets_v0` + mechanical parity at 22 entries | **Phase 6 Stage B (Option α narrowing of `quote` field; second application of `frozen-field-changes-as-new-versions` Standing Owner Disposition), 2026-07-04** |

Additionally: `lift_manifest_schema.snapshot.json` (discipline-freeze) + `outer_gate_transform.snapshot.json` (transform-primitive stability snapshot at G6).

## 5. Communication Contract with Orchestrator
- e1_dev MUST update this file's §2 and §3 at every phase close.
- e1_dev MUST NOT modify §0, §1, §4 unless user explicitly re-blesses.
- Orchestrator will cite section numbers from this file when giving briefs.

## 6. Pending User Decisions (rolling)
- **Item 4 (Docs-Pass 2026-07-02) RESOLVED 2026-07-03: SYNTHETIC v1 = standing test substrate; real material = operational/benchmark input; no supersede semantics between them. Fixture-augmentation-file pattern (Phase 1 precedent) carries forward for test-only additions.**
- [x] ~~Adoption path for incoming fixture pair~~ — RESOLVED at G3 opening (Path 1 — Reject).
- [x] ~~Open G3?~~ — CLOSED at 2026-07-01T17:15Z.
- [x] ~~Open G4?~~ — CLOSED at 2026-07-01T18:55Z.
- [x] ~~Open G5a?~~ — CLOSED at 2026-07-02T00:00Z.
- [x] ~~Open G6?~~ — CLOSED at 2026-07-02T00:45Z.
- [ ] Open G5b (Frontend Operator Console + Consumer Terminal)?
- [ ] Deliver real RMS material for G2b?
- [ ] **Owner/DPO — unlock Targeta yield seam** (`min_efficiency_gain`, `coverage_alpha`, `held_out_set_composition`). Seam built.
- [ ] **Owner/DPO — unlock Mtafiti V3 overlay seam** (`fact_precision`, `genre_accuracy`, `inter_annotator_floor` + real labelled held-out set). Seam built.
- [ ] **DPO — set Northena Ledger retention window** (currently INDEFINITE default). Deletion invariant re-blessed alongside implementation if window is set.
- [ ] **DPO — unlock V2 cumulative-disclosure arm** (NEW at G6): set `RMS_G6_K_ANONYMITY_THRESHOLD` + `RMS_G6_L_DIVERSITY_THRESHOLD` + `RMS_G6_DP_EPSILON_BUDGET` env vars (Product v2.1 §21.2 primitives; §29.1 "Until V2 passes"; §32 pattern). Seam built + tested LOAD-BEARING when env set.
- [ ] **MEA — deliver real source-standing table**. Currently placeholder covering 8 fixture feed_ids.
- [ ] **Service 2 landing (future track):** when Service 2 (Objective-Extraction with Solva conclusion boundary) is built, seed a run that populates SolvaTrace and verify `/trace/{trace_id}` Consumer Terminal renders the Solva section correctly. Tester's G5b forward-looking suggestion — not blocking, tracked for future.
- [x] ~~Solva/Targeta/Mtafiti spec re-drop~~ — DONE at Substrate-Drop v1.
- [x] ~~Interface + UX Architecture spec landing~~ — DONE at Substrate-Drop v1 addendum.

## 7. Last Orchestrator ⇄ User Exchange (rolling, one paragraph)

User dispatched **A2 — Service1Refusal envelope + composition_below_floor branch** with eight locked decisions (D1b include new branch, D2a static hint table, D3a flat JSONResponse, D4b 14th freeze, D6a Ring-5-upstream-boundary Path A, D7a max reduction, D8a `len(eligible)==0` trigger, X1 discipline observation tracked separately). e1_dev executed the phase autonomously to acceptance gate. STEP 0 preconditions: CI baseline 347/347 green; §204/§247 wording check confirmed source specs prescribe content categories only (`corroboration / accountable source` in `< >` placeholder syntax), NOT verbatim strings — HAZARD-STOP (f) did NOT fire, user's rewrite is authoritative. Shipped: `contracts/service_1_refusal.py` (14th frozen contract, 7 fields: outcome discriminator + reason + run_id + trace_id + asked + supported_class + what_would_raise_it); `service_1_refusal.contract_snapshot.json` (11th snapshot file); `services/service_1/refusal_hints.py` (static per-reason table, three user-locked strings); service layer wired at all three refusal sites via `Service1Refusal(reason, run_id, trace_id, *, asked, supported_class, what_would_raise_it)` exception + `_max_supported_class` helper reading Ring-5-governed per-unit `defensibility_class` with local `_CLASS_ORDER` mirror (D6a doctrine, no `solva_depth` import — service.py docstring L10-11 `"Does NOT invoke Solva"` intact); new `composition_below_floor` raise site fires on `if not eligible:` after Targeta filtering (D8a); router replaced `raise HTTPException(422)` with `return JSONResponse(status_code=422, content=refusal.model_dump())` for flat top-level body (D3a); `responses={200: Service1RunSummary, 422: Service1RefusalContract}` documented on the decorator for OpenAPI; added `objective_text: str` field to `Service1RunRequest` and plumbed through 5 test call-sites (`test_service_1_invariants.py` 4 sites + `test_trace_lens_readonly.py` 1 + `test_trace_lens_cross_engine_correlation.py` 2). Test suite: 7 new HTTP-layer invariant tests + 1 snapshot invariant, all in `test_service_1_refusal_envelope.py`, all green (validation-vs-refusal distinguishability via `body.outcome === "refused"` field-check, NOT status code; infrastructure-fault guard proves 500-not-refusal; D6a governed-not-recomputed via signal-ring mutation test; D7a max-not-min via mixed-class fixture). `.github/workflows/g0-gate.yml` gained `workflow_dispatch`. `docs/handoff/backend_contract_surface_v1.md` realigned across §1 (13→14), §1.14 new subsection, §2 route #17 error cell (fixed self-contradicting "500 (not 500)"), §2.1 request/response examples corrected (previous doc showed `ObjectiveRequest`-shape not `Service1RunRequest`-shape) + three refusal branches enumerated + validation-vs-refusal distinguishability + infrastructure-fault non-conflation, §4 Service 1 row updated with 7-field envelope reference, §5.3 tightened per user brief ("Verified across contracts" → "these payloads carry the class; Gate 1's build-time test enforces completeness"), §5.4 backed by the new envelope, §8 signature bumped to `-v1.1-a2-e1_dev-20260702T021500Z` + CI 340→355. `docs/lift_manifest.json` gained 4 new entries. `BUILD_JOURNAL.md` A2 close entry filed with strict §0 inline discretionary enumeration. **`make ci` = 355/355 green** (delta +15 from G6: +7 handoff route + +7 A2 refusal tests + +1 A2 snapshot invariant). No HAZARD-STOPs raised. Backend surface still FROZEN; A2 was a targeted additive amendment, not a re-open. Parked; G5b dispatch to `e1_web_frontend_dev` still awaits user go with the amended artifact validated.
