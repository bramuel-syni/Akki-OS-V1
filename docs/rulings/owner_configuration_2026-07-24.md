OWNER DISPATCH — Configuration Ruling + Sequenced Plan · 2026-07-24
Class: Owner-verbatim ruling + phase sequencing. Carries one governance amendment (§0-CAL), one substrate-drop sanction, one contract-check binding, and estimation-discipline corrections. Standing Rule v3 applies: this document is filed at docs/rulings/owner_configuration_2026-07-24.md, SHA'd, and its text is not builder-modifiable. D-11 canon-before-ruling: read cited artifacts before executing any step that consumes them.
§1 · Standing findings (record, then act)
LT-1 — ANSWERED by artifact (docs/close_reports/standing_queries_as_ci.md, 2026-07-11). Struck from status-owed. Do not re-report.
"Canonical Name Register relay" — CLOSED as the Product Canon Relay, superseded at docs/rulings/verification_runner_canon_admission_2026-07-15.md. Nothing owed. Use on-disk names in all future dispatch text; untraceable aliases are a D7 vector.
"Product Doc v3.1 rebuild" — D7-UNTRACEABLE. No register row, no ruling. Either produce the sanctioning citation (path + SHA) in your §2 status reply, or record it as a D7 finding and drop it. It is not priced, not queued, not implied.
LT-2 — STILL OWED. sequencing_harness_stage_a.md provenance: when created, by what instruction. Execution-scheduling inside sequence-G-13 does not discharge the provenance demand. If unsanctioned pre-work: D7 finding, file held, not deleted.
Prior credit-estimate document — VOIDED as a planning artifact. Two defects: unlabeled figures (violates the provisional-anchor labeling rule) and pricing of phantom/closed items (LT-1, Canon Relay, Product Doc v3.1). Its phase decomposition survives only as hypothesis for the §5 audit to confirm or break. No figure from it is citable.
§2 · Governance amendment — §0-CAL (calibrated discretionary enumeration)
Ratified by this dispatch. Land as a sibling amendment to the tiered ruling model (docs/governance/tiered_ruling_model.md, new §; predecessor sections byte-identical per Standing Rule v3). Effective for all phases dispatched after this ruling; no retroactive recount.
§0-CAL.1 — Bright line by path, not judgment:
Per-line discretionary enumeration remains MANDATORY for: backend/contracts/**, backend/services/**, backend/routers/**, any migration/tool that touches persistent state, and any test file asserting an invariant (AST cells, snapshot cells, gate cells, isolation cells).
Component-level enumeration applies to frontend presentation files (frontend/src/** excluding test files): one enumeration row per component — (a) what it renders, (b) which spec § it serves, (c) which gate cell covers it. Icon choices, utility-class strings, testid names, copy strings are covered by the component row, not per-line.
Exception clause: any frontend line implementing a gate-asserted behavior (refusal rendering, class badges, trace-link retention, receipt-safe-field filtering, and equivalents) reverts to per-line.
§0-CAL.2 — The substitution (load-bearing, not optional): every UI-bearing phase must land its gate-cell roster at Stage A — named, testable invariants of the G5b Gate 1/2/3 class — before execution is sanctioned. A UI phase with component-level enumeration and no gate roster is incomplete at Stage A; do not proceed to execution. Enumeration is being re-pointed at the mechanism with a catch-record, not relaxed.
§0-CAL.3 — Nothing else moves. Frozen contracts + snapshots + Parity, AST hard-fail cells, HAZARD-STOP taxonomy, closed seams, D-11, verbatim carriers, R4 sidecars, D-10 tables, Standing Rule v3, make ci step zero, Rule 2 band-relative trichotomy: all retained verbatim at all scales.
§3 · Estimation discipline (binding)
Every duration/credit figure in any future builder artifact carries, verbatim: "Provisional planning anchor — not a commitment. Relative weight only." No exceptions, including tables and rollups.
No pricing of un-audited scope. Module-build bands are generated once, from the §5 reconciliation's enumerated CODE_IMPACT + CONFLICT rows, after §5 closes — not before.
Evidence-gated loops are never priced by iteration count. BM-V INVESTIGATE remediation is recorded as: gated cycle · per-iteration band · iteration count undefined-by-construction. Its standing clauses (closes on INVESTIGATE · V1 PARTIAL until PASS · no production mining on INVESTIGATE) are the safety mechanism; a projected loop count would be D7.
§4 · Sequenced motion (dispatch order, D-9 auto-proceed between steps unless a hazard fires)
STEP 1 — Status-owed reply (standalone, no code rides): LT-2 provenance disposition + §1.3 citation-or-D7. Nothing else.
STEP 2 — make ci step zero against current main. Full sweep attested (pytest/Jest/Playwright counts stated). Any red halts the sequence.
STEP 3 — Substrate-Drop v3 (read + file + audit only; zero product code):
File the 8 uploaded artifacts (Product & System Document · Connect · Registry · Extract · Govern · Prove · Team · Shared Components · User Stories) as .md under docs/mandates/module_specs/, authoring-direction .md-canonical per the standing inversion.
SHA each into docs/mandates/MANIFEST.md; extend phase_source_requirements.yaml.
Reconciliation audit against the shipped surface with per-artifact CODE_IMPACT enumeration (NET-NEW / PARTIAL-EXTEND / CONFORMS per feature, §-anchored) and CONFLICT rows wherever the drop disagrees with UI Spec v2.2 / Product Spec v3 / SJM v1 — each CONFLICT surfaced for per-conflict Owner ruling. No auto-supersession in either direction.
Zero-loss constraint: every succession attestation (incl. Sponsor 3-party with CEO), waiting period, refusal shape, receipt behavior, notification category, and usage-rights enum in the 8 documents enters the audit intact. De-scoping is not an audit output; conflicts get ruled, never silently resolved.
The audit must additionally mint new Owner-decision register rows (not buried phase content): OD-8 mail-provider binding (Notification Center email fanout); OD-9 public-surface exposure posture for Public Receipts (no-login page: rate limiting, link entropy, revocation-under-caching — rides PH-R3 domain/TLS); OD-10 scheduler primitive for census debounce/auto-trigger.
Close report + register sibling per standing pattern. Your prior CODE_IMPACT preview table carries no evidentiary weight; where the audit contradicts it, the audit wins.
STEP 4 — EAB-2 execution atomic (resumes under D-9 on STEP 3 close), with one binding addition: at Stage-A refresh, check Service1Refusal@v1's envelope against the Prove spec's three refusal shapes (not-extracted-yet + gap-queue affordance + estimated effort · evidence-can't-support · something-broke) — the drawer and Answer Card will render this exact contract. Shape mismatch = HAZARD-STOP (a) before the Parity 31→32 seal, not after.
STEP 5 — Lane 2b re-band + module dispatch. Generate the module-phase banding from STEP 3's enumerated rows (labeled per §3.1). Dispatch module phases in spec-dependency order, each under §0-CAL with its gate-cell roster at Stage A, each new contract seat (quote_release_terms, model_acceptance_scorecard, destruction_attestation, rule_change_certificate, governance_succession_attestation, release_review_decision, memo_envelope, public_receipt — subject to audit confirmation) landing as its own additive Parity seal event. Structural directive: the Answer Card + refusal-shape component family is built once in the shared shell and consumed by both Prove and the Ask Akki drawer — the spec declares them the same structure; two implementations is self-inflicted drift and will be treated as a D-class finding.
STEP 6 — Sequence tail resumes unmodified: EAB-3 → Critic-pass → G-13 (Commercial Thesis reassigned Owner-side per register §8; remove from builder scope, one-line ledger annotation) → UI-1 → UI-2.
Lane 1 — no builder motion. 9.2b remains gated on OD-4 + 9.2-OWN-2 + PH-R2/R4, all Owner-side. On unlock: 9.2b atomic per the standing envelope (additive model-registry v0→v1 with SHA + license · whisper-tiny stays CI-fixture-only · PERCEPTION_EXECUTION_MODE=gpu · real batching behind the existing seam · census-at-scale · post-census 300-unit slice · BM-V as gated loop per §3.3). Self-dispatching any of it is the violation, not the diligence.
§5 · Close criteria for this dispatch
This dispatch is discharged when: (a) STEP 1 reply on-disk; (b) make ci attested green; (c) Substrate-Drop v3 close report + MANIFEST rows + CONFLICT/OD-8/9/10 rows on-disk; (d) §0-CAL amendment landed as governance sibling; (e) EAB-2 sealed or HAZARD-STOP (a) surfaced with the shape-mismatch evidence. Every terminal reply carries the D-1..D-11 self-audit table per standing practice.
