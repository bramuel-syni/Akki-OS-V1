OWNER RULING · STEP-2 Surfaces 1–3 · 2026-07-24 · FINAL, not re-openable
Surface 1 · Makefile red → (b)-modified. HALT LIFTS.
Full-sweep GREEN (1,296 pytest + 154 Jest + 57 Playwright · 1 skipped) is the authoritative STEP 2 attest. D-9 resumes to STEP 3.
The builder's forensic claim is FALSE and must be corrected in the filed finding: tests/test_rms_adversarial_v1_roundtrip.py existed — BUILD_JOURNAL attests it running 23 cells in make ci at G3 (211/211) and G4 (271/271) — and it exists today as backend/tests/test_instance_fixture_a_roundtrip.py, renamed at MC-E5's class-(b) fixture de-tuning (2026-07-14). The Makefile went stale at that rename, not at repo birth; "stale since initial commit" read squashed remote history against on-disk canon. A false forensic claim inside a HALT surface is itself a D-class defect.
Removing the target is refused — it would silently drop 23 fixture-conformance cells. Tier-3 repair rides STEP 3 (Makefile = build tooling, inside the zero-product-code fence): repoint the target to tests/test_instance_fixture_a_roundtrip.py, rename it instance_fixture_a, and restore the standing norm make ci == pytest -q — the chain still echoes "G2a CI gate PASSED" and enumerates only G2a-era targets, which is how one stale line survived 22 days. make ci green is a STEP 3 close criterion.
Surface 2 · LT-2 held file → (a). Retained as evidentiary. (b) refused — the file is now evidence. (c) refused — its body claims "Dispatched: 2026-07-11" while git shows creation 2026-07-14, after both the claimed date and the D7 fence; retroactive sanction would reward a back-dated dispatch line. Companion dispositions: phase-ledger open row → HELD-D7, open count 1→0, annotation citing docs/audits/lt2_sequencing_harness_provenance_d7_2026_07_24.md. When sequence-G-13 opens, its sequencing-harness surface is authored fresh from Registry Doctrine §8.1; lifting from the held file without a ruling is a new D7.
Surface 3 · Product Doc v3.1 → accepted and closed. No re-sanction.
Sequence on filing: STEP 2 discharged → STEP 3 (Substrate-Drop v3 per owner_configuration_2026-07-24.md §4, carrying the Makefile repair + chain-parity restoration + corrected D-class finding as Tier-3 riders) → STEP 4 EAB-2 with the Service1Refusal@v1-vs-Prove-refusal-shapes check before the Parity 31→32 seal → STEP 5 re-band from audit rows → STEP 6 tail. Lane 1: no builder motion.
