RULING — G-13 §5.1/§5.2/§5.3: composition (b · a · a). Net one seal, MandateSpec@v0, Parity 33→34. All three Tier-3 recommendations confirmed — with three bindings that close the specific hole each no-seal or generated-artifact choice opens.
§5.1 = (b), markdown block, no seal. Correct on the merits, not just on ceremony: the context block never crosses a wire between systems — it is composed into a prompt, and freezing a wire contract for prompt-interior content is contract machinery pointed at the wrong layer. §6.2's own test ("one artifact serves three roles," worker errs toward the promise) favors the format rung-3/4 workers parse most reliably. (c) rightly dead. Binding B-1: no seal does not mean no shape. The block is emitted by exactly one writer (prompt_builder.py), its content sourced from the Registry record — never hand-authored — and a hard-fail cell asserts the exact rendered serialization against a golden snapshot for a fixture function. "Markdown, no seal" must not decay into "freeform string anyone edits." This is the Class S resolution your §5.5 table already anticipates; B-1 names the cell that makes it real.
§5.2 = (a), YAML on-disk + MandateSpec@v0 seal. Your rationale stands and one addition makes it decisive: an enforcement chain of mandate → spec → gate is only auditable if the middle link is a persisted, SHA-verifiable artifact — (b)'s in-memory shape fails the D-11 round-trip by construction, which disqualifies it regardless of ceremony weight. Binding B-2: the emitter is deterministic — same mandate input yields byte-identical YAML — every generated file carries a generated-do-not-edit header naming its source mandate + SHA, and a CI cell regenerates and diffs: any divergence between docs/generated/mandate_specs/ and fresh emitter output is a hard fail. Generated artifacts that can be hand-edited silently are a shadow-canon vector; the regeneration diff closes it.
§5.3 = (a), function stubs, no seal. (b) is rejected with prejudice: a Parity cascade unbounded by mandate count converts the seal registry from a discipline into a counter, and D-5 flags it correctly. Source-anchor docstrings give the round-trip. Binding B-3: same regeneration-diff regime as B-2 — generated gates are never hand-edited; a needed change lands at the mandate source and flows through the emitter. Additionally, every generated gate gets at least an import-and-invoke smoke cell inside the far-endpoint rail set, so generated code cannot rot as unexecuted text.
§5.5 — accepted: 17 S · 0 O · 2 E · 0 D, E1-pending resolving to S under B-1. Class E defaults N=10 and α=0.05 approved as pinned values on sequencing-harness-v0; both are textbook Class E and neither shows a promotion trigger. No early E→O promotions.
One forward instruction for UI-1 Stage A, binding now so it's on the record before you get there: UI-1 consumes filed canon only — the change order's A1 Use Data supersession as landed, with the §0-CAL gate-cell roster firing as you note. Be aware a second Owner change order is pending covering post-A1 product decisions (Connect landing restructure, mapping relocation to source profiles, infrastructure/governance surface separation, among others) that exist in Owner-side session record but are not yet filed canon. UI-1's Stage A must not consume them, anticipate them, or reconcile against them; where its scope visibly collides with an unfiled pending decision, it surfaces the collision as a CONFLICT row for ruling rather than guessing in either direction. Extraction-Console scope should collide little, but the rule is stated so zero ambiguity survives contact.
Motion: persist verbatim at docs/rulings/g_13_e1_e2_e3_2026_07_25.md · echo SHA · D-9 to the G-13 execution atomic per (b · a · a) + B-1/B-2/B-3 · Parity 33→34 sealed at MandateSpec@v0 · Commercial Thesis row carries the Owner-side one-line annotation only · close report with the 20-parameter classification and D-1..D-11 table · then D-9 to UI-1 Stage A under the forward instruction above. Proceed.
