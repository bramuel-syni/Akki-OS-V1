RULING — Critic-pass §5.1 E1: (a1), additive fields on existing frozen contracts, Parity 33 held, with one binding that closes a hole in (a1) as declared.
Grounds: (a1) is not merely the lighter option — it is the doctrinally closer fit to the anchor. CIF §12 says "schema-required fields on every verdict-bearing artifact"; (a1) puts them on the artifact, so the format-gate is a pure schema check. (a2)'s FK indirection means "unmanifested verdict rejects at submission" requires a resolution read before the gate can even evaluate — a heavier ceremony that weakens the fail-closed property it's meant to serve. Add the EAB-2 A3 precedent and the D-6 linear progression, and (a2)'s only residual advantage (manifest/verdict separation) buys nothing this atomic. The shared-substructure coupling trade-off is accepted and bounded: ManifestEntry is frozen on landing; its evolution is additive (ManifestEntry_v1), same as any contract — the coupling is to a frozen shape, which is the coupling we want.
Binding B-1 (part of the ruling, test at the execution atomic): default_factory=list makes an empty manifest type-legal, which means the schema alone no longer enforces "unmanifested verdict rejects." Therefore the format-gate must reject empty or missing manifest_entries at submission for all five verdict types, and this lands as a hard-fail cell in the execution atomic — per your own A3.1 line, a rail without a hard-fail cell does not exist. The default factory is a serialization convenience, never permission. If any verdict type has a legitimate zero-manifest state, that is a HAZARD-STOP, not a silent exemption.
§5.5 classification — accepted as tallied: 28 S · 0 O · 7 E · 1 D. The seeded-defect corpus as Class D is confirmed, with the A3.3 lifecycle applying in full including the addition/removal-edit asymmetry — an edit to a seeded defect changes what the catch-rate measures, so gating edits is correct there too.
Early E→O promotion of the verdict sampling rate — DECLINED, without prejudice. "Decays as measured reliability accumulates" describes a schedule, and a schedule can live as a Class E deterministic decay function pinned per version — runtime tunability is not yet demonstrated as needed, and pre-emptive promotion is exactly the reclassification creep A3.2 exists to prevent. Land it Class E with the decay rule explicit in the engine spec. Revisit via A3.2 when accumulated reliability data shows the schedule itself needs operational adjustment; your disclosure is noted on the record as the anticipated trigger.
Downgrades §5.2–§5.4 accepted as declared; §5.4's Owner-side sampling ceremony is acknowledged as mine and stays out of builder scope.
Motion: persist verbatim at docs/rulings/critic_pass_e1_2026_07_25.md · echo SHA · D-9 to the Critic-pass execution atomic per (a1) + B-1 · Parity 33 attested held · close report with the 36-parameter classification table and D-1..D-11 self-audit · then D-9 to G-13 Stage A, where the Commercial Thesis row carries the Owner-side one-line annotation only, per the change order. Proceed.
