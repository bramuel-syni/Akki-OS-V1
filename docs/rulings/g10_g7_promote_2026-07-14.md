# Owner Ruling · G-10 · G-7 · PROMOTE · 2026-07-14

**Authority:** Owner-ratified (atomic commit dispatched alongside IF-1 reconciliation, 2026-07-14).
**Target register rows:** `docs/briefs/outstanding_work_and_gap_register_v1.1.md` §4 G-7 (STATUS OWED, then OWNER if gap) · §4 G-10 (PENDING EVIDENCE → OWNER) · §6 OD-2 (/legacy/* disposition) · §6 OD-6 (Trace-lens build timing IF G-7 returns data-only).
**Predecessor:** register v1.1 at SHA `01016e2f679064613f6290ee4f50bdaa9874fb2e6b3c3973ccad53d3339f3224` · amendment ruling 2026-07-12 at SHA `06f51bf67d3c3b34024a814fd5d00bc6eb62c749181ac8945cf45f56f309ba27`.
**Supersession:** additive; v1.1 body remains canonical; the close carries in v1.2 §12.

## §1 · Evidence read from disk (D-11 canon before ruling)

**G-7 (SolvaTrace three-lens rendering surface):** `frontend/src/legacy/pages/TraceReceiptPage.js` existed on-disk with a full three-lens render — Ledger section (`trace-ledger-section`), Solva reasoning section (`trace-solva-section`), Mining plans section (`trace-plans-section`), Registry records section (`trace-registry-section`) — wired to `apiClient.traceLens(traceId)` → `GET /api/northena/trace/{traceId}`, rendered on the nested `/legacy/trace/:traceId` route. **The surface existed;** the register question "surface exists / data-only" resolved with the first answer.

**G-10 (frontend /legacy/* disposition):** Deviation-audit Part B recorded 1,118 LoC across eight legacy pages. Grep of live consuming routes at IF-1 close returned: `TraceReceiptPage` linked from `AskConsolePage.js` (two `href="/legacy/trace/${trace_id}"` refs — the Trust receipt buttons on both "answered" and "accepted" outcomes) — the only live consumer. The other seven pages (`LandingPage`, `OperatorDashboard`, `RunsPage`, `RunDetailPage`, `DisciplinePage`, `EnginesPage`, `ComposePage`) reachable only from within the `/legacy/*` nested tree via the AppShell chrome — zero external consumers.

## §2 · Ruling (verbatim)

Both rows close in one atomic commit:

1. **G-7 closes PROMOTE.** TraceReceiptPage lifts out of `/legacy/*` and mounts at public `/trace` and `/trace/:traceId` (top-level routes, no auth guard — three-lens Trust Receipt is a public read surface per UI Spec §5). Back-link resolves to Ask Console at `/` per single-ingress doctrine. Ask Console's Trust receipt hrefs update from `/legacy/trace/${trace_id}` to `/trace/${trace_id}` (both callsites).

2. **G-10 closes SHAVE.** The remaining seven legacy pages retire whole. The `<Route path="legacy">` nested block and the AppShell chrome (`src/components/AppShell.js`, whose only consumer was the legacy shell) retire with them.

3. **Reflexive attests land in v0.4 supplement.** Two G-10/G-7 rows (§S1) + seven legacy-page shave-attestation rows (§S2) = nine R4 reflexive rows in `docs/registry/function_promise_registry_v0.4_supplement.md`. Prior source-of-truth SHAs preserved.

4. **The G5b archival gate (Jest, `test_g5b_legacy_pages_archived_under_frontend_legacy_directory.test.js`) is rewritten as the G-10/G-7 PROMOTE invariant** — asserts (a) `src/legacy/` no longer exists, (b) `AppShell.js` no longer exists, (c) TraceReceiptPage lives at `src/pages/trace/`, (d) `App.js` imports it from that path and declares both `<Route path="trace">` and `<Route path="trace/:traceId">`, (e) `<Route path="legacy">` block absent, (f) `AskConsolePage.js` no longer references `/legacy/trace/`. Predecessor invariant (archival under `/legacy/`) is closed by this ruling — the successor invariant lives in the same file so any future re-litigation reads the current truth.

5. **Playwright smoke lands** at `frontend/e2e/trace_smoke.spec.ts` — asserts the promoted `/trace/:traceId` route renders `trace-receipt-page` container + all three lens sections, and `/trace` renders the search form. Runs alongside `ask_console_smoke.spec.ts` under the chromium-only project.

## §3 · What did NOT change (Parity 31, D7)

- The 31 frozen Pydantic contracts under `/app/backend/contracts/` — byte-identical (verified: 31 `.contract_snapshot.json` files under `tests/invariants/`, all 1001 invariant tests GREEN).
- Registry v0.md source-of-truth (SHA `598a7ad4d326dd5c0fc003fe8091a52fd215fb63e76d5c04befd1aa4c25584b0`) and prior supplements v0.1 · v0.2 · v0.3 — byte-identical.
- Backend route `GET /api/northena/trace/{traceId}` — unchanged; the promotion is a UI-surface move only, the API contract is the same.
- No new scope minted. No new promises minted (conservation-not-authorship posture held).

## §4 · Exit gates satisfied

| Gate | Status | Evidence |
|---|---|---|
| Triad reconciliation (test_shield_custody_chain × test_northena_invariants) | GREEN | `pytest tests/registry/test_shield_custody_chain.py tests/test_northena_invariants.py -q` → 14/14 |
| Frontend UI-spec gates | GREEN | `yarn test --watchAll=false` → 24/24 suites · 154/154 tests |
| Frontend build clean | GREEN | `yarn build` compiled successfully; 103.31 kB gzipped |
| Playwright smokes (chromium) | GREEN | `npx playwright test e2e/trace_smoke.spec.ts e2e/ask_console_smoke.spec.ts` → 3/3 |
| MRR gates (G1-G6 + Parity + DataBlind + SourceSHA) | GREEN | `python -m tools.registry.regenerate --check` → 7/7 GREEN |
| Registry run_queries coherence (no source-of-truth drift) | GREEN | `python -m tools.registry.run_queries --check` → OK · 6 artifacts regenerated (no write) |
| Backend full sweep | GREEN | `pytest tests/ -q` → 1241 passed · 1 skipped · 0 failed |

## §5 · Doctrine anchors

- Standing Rule v3 (canonical on-disk deliverables).
- D-11 canon before ruling (evidence read from disk before wiring the promotion).
- D7 scope conservation (no scope minted beyond the dispatch — the two register-open rows close; nothing added).
- Governance §14 additive supplement pattern (v0.4 supplement beside locked v0.md + prior supplements).
- Registry Doctrine v1.0 §3.2 (11-field schema for the nine reflexive rows).
- Single-ingress doctrine (UI Spec §3.1): back-link resolves to `/`.

═══════════════════════════════════════════════════════════════════

*End of ruling. Row status transitions: G-7 STATUS OWED → CLOSED; G-10 PENDING EVIDENCE → CLOSED; OD-2 discharged (no separate Owner decision needed — the atomic PROMOTE-and-SHAVE ruling covers it); OD-6 discharged (three-lens surface is live, no separate build-timing decision needed).*
