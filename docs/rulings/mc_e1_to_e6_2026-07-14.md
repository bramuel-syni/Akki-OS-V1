# Owner Ruling · Multi-Instance Capability MC-E1..MC-E6 · 2026-07-14

**Authority:** Owner-ratified (dispatch 2026-07-14 · atomic execution loop closed same day).
**Predecessor Stage A:** `docs/stage_a_proposals/multi_instance_capability_stage_a.md` (SHA `e60e623fa3e8ed3ae0c437b6bdec8e884293683ccc5524123d928187bb6102d8`).
**Band:** `[1,900, 2,700]` raw LoC RATIFIED per Governance §9 (metric-verdict-in-derivation-unit).
**Pointer-disclosure resolution:** Governance §9 was the intended reference; Op. Values §9 is acceptance criteria — Owner accepted defect.

## Ruling body (verbatim)

Pointer disclosure first: the builder's resolution is correct and my citation was the defect — "per §9" meant Governance §9 (LoC verdict authority); Operating Values §9 is acceptance criteria. Precedent-based resolution was exactly right.

Six rulings. Band [1,900, 2,700] RATIFIED; §4.2 split available as Tier-2, dev's judgment. Execute on receipt.

MC-E1 — α. Zero contract mutation. The contract's own design text settles it ("every field must carry audio/video/image/text cases without forcing a text-only shape") — tabular rows are text-modality units with locator carrying {table, row, cols} and connector params in extraction_params. license_class stays at the receipt layer per Op. Values §7. Parity 31 held; β mints a modality for zero delta; γ inverts layer discipline.

MC-E2 — α, one condition. Constraint architecture is D-6 canonical: mandatory instance_id on every persistent row, accessor refuses unscoped queries, compound indexes, adversarial cross-instance cells. Condition — legacy backfill: existing rows carry no instance_id; the same commit backfills all existing data to the instance-#1 identity and attests the backfill count, or every live call site breaks on the accessor's refusal. γ's DB-per-instance multiplies provisioning and PH-R2 drills for isolation α achieves in one field.

MC-E3 — α, with the initial-set ledgered. The onboarding write establishes pre-birth defaults; dual-control protects changes to a live posture — β's chicken-and-egg (two operators required before an instance exists) proves the strict reading wrong. Condition, stated as confirmation of standing doctrine rather than new machinery: every initial seam-value set writes a ledger row marked as initial-set — the ceremony is waived, the audit trail is not. Changes thereafter carry full §6 ceremony where required.

MC-E4 — α. Default internal_only, fail-closed: artifacts derived from default-classed units refuse the outer gate until rights are explicitly set. The asymmetric-cost logic is exactly right — a license fail-open is irreversible in the same way a class-C deletion is. Note for execution: S2.onboard forces rights posture per source per Op. Values §8, so the default is the safety net for sources registered outside full onboarding, not the expected steady state. γ was correctly never a builder path.

MC-E5 — α. Contract-tier tokens are class-(c): citations to the platform's own mandate filenames are architectural history, not customer identity — the code shape is already organization-agnostic. List-only, byte-identical, Parity 31 held. β/γ/δ all pay a re-bless (and δ a canon rewrite) for zero agnosticism gain. This is the "HALT rather than re-bless silently" instruction, resolved by not touching.

MC-E6 — split ruling, and the evidence basis stated plainly: there are no external production integrators — zero customers, preview environment only. A dual-alias deprecation window (α) would maintain two code paths to protect nobody: rule-proliferation cost with no promise behind it, and this is the cheapest moment the clean rename will ever have.

HTTP headers + env var names → β, hard cutover now: X-Akki-App-ID / X-Akki-Webhook-URL; AKKI_* env vars. Guard: if any non-fixture external app registration with live keys exists in the DB, HALT and report before the header cutover — the no-integrators premise must survive an evidence check, not an assumption.

DB_NAME → α's disposition: variable name unchanged; instance-#1's value stays rms_intelligence (data preserved — the value is instance identity, correctly config-resident); new instances mint new values.

Retired-token grep-negative gates → preserved verbatim: they test that retired tokens are not emitted; renaming them defeats their semantics.

Execute: atomic (or split at dev's judgment) → close with: de-tuning audit table final dispositions, backfill attestation, isolation cell results, seam-value ledger evidence, cutover guard result, R4 rows via v0.5 supplement, register v1.2 → v1.3 (OD-1 discharge recorded), parity 31, D-10 riding.

═══════════════════════════════════════════════════════════════════

*End of ruling. Six rulings ratified: MC-E1 α · MC-E2 α (backfill condition) · MC-E3 α (initial-set ledgered) · MC-E4 α · MC-E5 α · MC-E6 β (headers+env-vars, hard cutover) + MC-E6 α (DB_NAME preserved) + retired-token gates preserved. Standing Rule v3 · on-disk canonical.*
