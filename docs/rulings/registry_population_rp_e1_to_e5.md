# Registry Population · Rulings Record (RP-E1..RP-E5)

**Landing:** 2026-07-11 · Owner rulings on Tier-1 escalations RP-E1..RP-E5 + band ratification + close-return spec.
**Stage A basis:** `/app/docs/stage_a_proposals/registry_population_stage_a.md` · SHA `63d78ca5451a7a0e019a2231d9e59c054312f02c9cea3ed68580988295705e59`.
**Doctrine basis:** `/app/docs/governance/registry_doctrine_v1.md` · SHA `0bfe65c47e2c55f35e2a860fec405c05b8ed32b3473bcb63a0a259fb810ab471`.
**Governance:** 3-tier ruling model through §13 · Registry Doctrine v1.0 R4/D-10 in force · §12 close-ratification-on-own-text.
**Standing Rule v3:** on-disk canonical.

---

## §1. Owner rulings — verbatim carrier

**RP-E1 · Promise-consolidation judgment posture:**

> **RP-E1 — α, one condition: tie-break toward distinct.** The 60% token rule + governor/surface class + logged merges + close-time review is sound machinery, but the error asymmetry demands a direction: under-merging is self-correcting (Q1 redundancy exists precisely to catch it later, mechanically); over-merging loses distinctions silently and nothing catches it. So where the rule is borderline, keep the promises distinct and let Q1 earn its rent. Consolidation log co-lands as proposed.

**RP-E2 · Q2/Q3 findings touching client promise:**

> **RP-E2 — α.** Findings publish verbatim, client-promise-touching Q2/Q3 escalate at close, zero builder retirement. This is the "findings are the deliverable" framing preserved exactly; β would silently retire in the very first population — archaeology corrupted at birth.

**RP-E3 · Scope-boundary (governor behavior vs implementation detail):**

> **RP-E3 — α with a correction the verbatim text requires.** As written, α's iff silently drops the most important case: a behavior named in the mandate but with no testable surface fails condition (ii) and vanishes from the record — yet that is precisely a Q3 gap, the exact deliverable this phase exists to surface (and the exact flaw for which γ was rejected). Amended rule: (i)∧(ii) → registers as a function row; (i)∧¬(ii) → registers as a Q3 gap finding, never dropped. Sub-steps of a named-and-tested behavior fold into the parent's mandate field as proposed.

**RP-E4 · Ladder-rung assignment ambiguity:**

> **RP-E4 — α.** unknown is the honest value; a guessed rung is a fabricated value — the anti-fabrication instinct applies to our own records exactly as it applies to briefs. The harness resolves unknowns when it's dispatched, from measurement.

**RP-E5 · §4.2 threshold + governance-doc carve-out applicability:**

> **RP-E5 — α.** Band applies as stated; §4.2 crossing is Tier-2 disclosure, non-blocking. The builder's own caution is correct and adopted as the ruling's rationale: a Registry-class carve-out is governance-amendment territory, never a case-by-case precedent minted mid-phase.

**Band ratification:**

> Band `[1,300, 2,900]`: **RATIFIED** — brackets all scenarios; where it lands follows from E1's consolidation outcome, disclosed at close.

**Close-return spec (Owner verbatim):**

> Execute: atomic doc commit → close with: promise count, function count, Q2/Q3 findings lists (client-promise-touching items flagged for ruling), consolidation log, coverage metrics against the 134 gate identifiers, band actual in raw LoC. D-10 self-audit rides the close per standing.

---

## §2. Ruling-derived execution deltas (Stage A → execution)

### §2.1 RP-E1 condition applied: tie-break toward DISTINCT

Under-merge self-corrects via doctrine §3.4 Q1 redundancy query (mechanical, later). Over-merge is silent — nothing catches it. At the 60% token boundary, keep promises distinct. Explicit `TIE-BROKE-TOWARD-DISTINCT` records land in the consolidation log.

### §2.2 RP-E3 amendment applied: (i)∧¬(ii) → Q3 gap finding, never dropped

Amended registration rule:
- **(i)∧(ii)** — mandate-Tier-1 vocabulary named AND has a testable machine-enforceable surface → registers as a **function row** in §3.
- **(i)∧¬(ii)** — mandate-Tier-1 vocabulary named BUT no testable machine-enforceable surface exists → registers as a **Q3 gap finding** in §5. **Never silently dropped.**
- **Sub-steps of a named-and-tested behavior** — fold into parent row's `mandate` field.

### §2.3 RP-E4 posture: `unknown` is the honest value

`ladder_rung` = `unknown` where the source does not evidence a specific rung. No builder-guessed values. Anti-fabrication instinct applies to Registry records exactly as it applies to Opportunity Briefs (OB-E1 α precedent).

### §2.4 RP-E5 posture: band applies as stated

Band `[1,300, 2,900]` applies to the deliverable's raw LoC. §4.2 crossing = Tier-2 disclosure per §12.1 (never blocking). No governance-doc carve-out minted at this phase.

### §2.5 RP-E2 sequencing: client-promise-touching findings escalate at close

Q2 orphan findings whose gate protects a client-promise class AND Q3 gap findings whose journey step is client-promise-bearing land in the deliverable with `[CLIENT-PROMISE · ESCALATE-AT-CLOSE]` markers. Owner rules retirement/gap-fill at a **subsequent turn**. The deliverable ships regardless of open findings — findings ARE the deliverable per Owner framing.

---

## §3. §12 auto-ratification pre-clearance criteria

Per Owner directive: if close-landing satisfies —
- **(a)** named gates green (RP-G1..RP-G6 + auxiliary),
- **(b)** rulings + RP-E1 tie-break condition + RP-E3 amendment attested as applied,
- **(c)** no new Tier-1 escalation surfaced during execution,

— then close ratifies on its own text.

Client-promise-touching Q2/Q3 findings are **DELIVERABLES**, NOT new Tier-1 escalations — they are surfaced verbatim at close for Owner ruling at a subsequent turn.

**HALT-for-Owner condition:** ONLY if a new Tier-1 surfaces mid-execution not covered by RP-E1..RP-E5 + Owner amendments.

---

## §4. Standing Rule v3 posture

This ruling record is on-disk canonical. Reply body at execution close carries SHAs (this file + deliverable + consolidation log + close report) + band actual + parity attest + coverage metrics + D-10 self-audit verdict + client-promise-touching Q2/Q3 items verbatim.

═══════════════════════════════════════════════════════════════════

*End of RP-R1 rulings record. Registry Doctrine v1.0 R4/D-10 in force · all five escalations ruled α with RP-E1 tie-break condition + RP-E3 amendment · band ratified · atomic execution pre-cleared for auto-ratification at close-landing.*
