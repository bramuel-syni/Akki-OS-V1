# RMS Intelligence System — backend requirements (G0).
# Pinned conservatively; matches the legacy substrate's versions where the
# port is verbatim (Shield).
fastapi==0.118.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2
motor==3.6.0
boto3==1.35.49
cryptography==43.0.3
# LLM chokepoint (Shield's only outbound seam).
emergentintegrations
litellm==1.51.3
# De-id substrate. spaCy model loaded lazily by deidentifier.py.
spacy==3.8.2
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
# Tests + lint.
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
PyJWT==2.13.0
bcrypt==4.1.3
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.27.0
av==18.0.0
flatbuffers==25.12.19
