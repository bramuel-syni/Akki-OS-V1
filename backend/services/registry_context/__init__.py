"""G-13 · Worker context-harnessing (Registry-as-context).

Owner ruling §5.1 = (b) markdown block · NO SEAL · single-writer discipline
per Binding B-1. Rides existing Registry v1 canon; no new frozen contract.

Registry Doctrine §6.2 verbatim (line 131):
    "An LLM worker's behavior is shaped by the context it receives; the
    Registry is that context. Harnessing rule (enters as code on Owner
    dispatch): every model worker receives the promises in force on its
    task — mandate, promise, and service trace for the functions it
    touches — as part of its operating prompt. One artifact thereby
    serves three roles: source of truth for humans, compile-source for
    gates, and system context for workers."

Binding B-1 (Owner-verbatim): *"no seal does not mean no shape. The
block is emitted by exactly one writer (prompt_builder.py), its content
sourced from the Registry record — never hand-authored — and a hard-fail
cell asserts the exact rendered serialization against a golden snapshot
for a fixture function."*
"""
