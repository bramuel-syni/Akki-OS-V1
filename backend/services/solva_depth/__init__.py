"""Placeholder to declare /app/backend/services/solva_depth as a package.

The G1 Solva Depth v1 module lives at
`services/g1_defensibility/solva_depth/` (byte-identical to G1).
This new `services/solva_depth/` package is the *sibling* home for the
G2 Solva admit-assist module (`admit_assist.py`) per Northena mandate
§4/§9 — new behaviour, NOT a contract change to G1 Solva Depth.
"""
