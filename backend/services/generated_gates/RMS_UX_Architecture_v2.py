# GENERATED · DO NOT EDIT
# Source: docs/mandates/RMS_UX_Architecture_v2.md
# Source SHA-256: e072fd307e00b207cd2a451791bc3650ad59f5f71b28ac5d4c04b1144b841d59
# Generator: backend/services/far_endpoint/gate_generator.py
# Regenerate: python -m services.far_endpoint.gate_generator

def RMS_UX_Architecture_v2_gate_000(payload):
    """Source anchor: docs/mandates/RMS_UX_Architecture_v2.md#stanza-000

    Refusal reason: Mandate gate 000 from docs/mandates/RMS_UX_Architecture_v2.md · RMS Intelligence System — UX Architecture Specification
    Condition (verbatim from source): ## 1. What the experience must deliver
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_UX_Architecture_v2_gate_001(payload):
    """Source anchor: docs/mandates/RMS_UX_Architecture_v2.md#stanza-001

    Refusal reason: Mandate gate 001 from docs/mandates/RMS_UX_Architecture_v2.md · RMS Intelligence System — UX Architecture Specification
    Condition (verbatim from source): The system produces governed intelligence — claims honest about how defensibly they may be asserted. The experience must make that intelligence usable by people who are not analysts, and make the governance **felt as trust, not operated as machinery**. No surface mirrors the system's internals; every surface is designed from its user's promise inward. An interface that renders the build (engines, gates, phases) instead of the product is a defect regardless of how faithfully it renders.
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_UX_Architecture_v2_gate_002(payload):
    """Source anchor: docs/mandates/RMS_UX_Architecture_v2.md#stanza-002

    Refusal reason: Mandate gate 002 from docs/mandates/RMS_UX_Architecture_v2.md · RMS Intelligence System — UX Architecture Specification
    Condition (verbatim from source): 1. **Entry point** — internal work-order (Day Zero, shaped through the wizard) or external request (Day-to-Day, arrives complete through the API or is refused at admission).
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_UX_Architecture_v2_gate_003(payload):
    """Source anchor: docs/mandates/RMS_UX_Architecture_v2.md#stanza-003

    Refusal reason: Mandate gate 003 from docs/mandates/RMS_UX_Architecture_v2.md · RMS Intelligence System — UX Architecture Specification
    Condition (verbatim from source): 3. **Output** — four declared fields: **form** (qualified data · knowledge artifact · model · callable skill · composed conclusion), **consumer** (person · system · training pipeline), **grain** (per-claim · aggregated · synthesized-whole), **standard** (the defensibility floor the output must meet).
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_UX_Architecture_v2_gate_004(payload):
    """Source anchor: docs/mandates/RMS_UX_Architecture_v2.md#stanza-004

    Refusal reason: Mandate gate 004 from docs/mandates/RMS_UX_Architecture_v2.md · RMS Intelligence System — UX Architecture Specification
    Condition (verbatim from source): Two stages: **mine** (extraction to qualified intelligence, sized by reach) then **transform** (qualified intelligence into the declared output, inside the system). **Provenance bound:** the transform produces the shaped output only where the declared standard survives it; an output whose form or grain would destroy required provenance is refused at shaping time with a path forward. Surfaces **render** outputs; they never re-shape them — a different form or grain is a new objective (no micro-analytics drift through export).
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None
