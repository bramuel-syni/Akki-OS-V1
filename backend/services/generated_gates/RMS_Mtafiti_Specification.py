# GENERATED · DO NOT EDIT
# Source: docs/mandates/RMS_Mtafiti_Specification.md
# Source SHA-256: 664fb76680cd8b9e62cfeac084a9d7d9410122a26d692f873c0242b59c78a1da
# Generator: backend/services/far_endpoint/gate_generator.py
# Regenerate: python -m services.far_endpoint.gate_generator

def RMS_Mtafiti_Specification_gate_000(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-000

    Refusal reason: Mandate gate 000 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): This document is binding. Part I states what Mtafiti must do and why;
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_Mtafiti_Specification_gate_001(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-001

    Refusal reason: Mandate gate 001 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): threshold decisions. It is a forward specification: it defines what must
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_Mtafiti_Specification_gate_002(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-002

    Refusal reason: Mandate gate 002 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): | **Services the extraction objective** | Never overstate standing. The measure is a targeting and flooring prior, not a truth verdict; it must never make a claim look more defensible than it can be defended, and it must fail toward caution. |
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_Mtafiti_Specification_gate_003(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-003

    Refusal reason: Mandate gate 003 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): <p># MUST NOT import verdict.py; MUST NOT assign
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_Mtafiti_Specification_gate_004(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-004

    Refusal reason: Mandate gate 004 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): Binding. Any implementation that violates one is incorrect regardless of
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def RMS_Mtafiti_Specification_gate_005(payload):
    """Source anchor: docs/mandates/RMS_Mtafiti_Specification.md#stanza-005

    Refusal reason: Mandate gate 005 from docs/mandates/RMS_Mtafiti_Specification.md · RMS_Mtafiti_Specification
    Condition (verbatim from source): - **Behavioral delta when opened:** real editorial-standing declarations flow into Mtafiti per-feed governance; `MtafitiRegistryRecord` composition begins reflecting MEA-authority-scored standings rather than synthetic defaults. Unrecognized `feed_id`s at composition time need a decided fallback — probably: refuse with `no_source_standing_for_feed` — extend at unlock time.
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None
