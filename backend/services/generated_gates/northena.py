# GENERATED · DO NOT EDIT
# Source: docs/mandates/northena.md
# Source SHA-256: ab0beeddf23c9530cc54c6ddd4255b4b3d0435df0d4c156d05de478e65af8345
# Generator: backend/services/far_endpoint/gate_generator.py
# Regenerate: python -m services.far_endpoint.gate_generator

def northena_gate_000(payload):
    """Source anchor: docs/mandates/northena.md#stanza-000

    Refusal reason: Mandate gate 000 from docs/mandates/northena.md · northena
    Condition (verbatim from source): behavioural depth: Part I states what Northena must do and why; Part II
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_001(payload):
    """Source anchor: docs/mandates/northena.md#stanza-001

    Refusal reason: Mandate gate 001 from docs/mandates/northena.md · northena
    Condition (verbatim from source): specification: it defines what must be true of any correct
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_002(payload):
    """Source anchor: docs/mandates/northena.md#stanza-002

    Refusal reason: Mandate gate 002 from docs/mandates/northena.md · northena
    Condition (verbatim from source): Northena is one of three governors on three orthogonal axes; it must not
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_003(payload):
    """Source anchor: docs/mandates/northena.md#stanza-003

    Refusal reason: Mandate gate 003 from docs/mandates/northena.md · northena
    Condition (verbatim from source): **Why it must not learn.** The moment Northena learns — adjusts gating
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_004(payload):
    """Source anchor: docs/mandates/northena.md#stanza-004

    Refusal reason: Mandate gate 004 from docs/mandates/northena.md · northena
    Condition (verbatim from source): must be answerable deterministically: admitted because these fields were
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_005(payload):
    """Source anchor: docs/mandates/northena.md#stanza-005

    Refusal reason: Mandate gate 005 from docs/mandates/northena.md · northena
    Condition (verbatim from source): the objective or a sub-objective warm, fresh, or refused; Layer D and
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_006(payload):
    """Source anchor: docs/mandates/northena.md#stanza-006

    Refusal reason: Mandate gate 006 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>decision: str # admitted|refused|warm|fresh|</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_007(payload):
    """Source anchor: docs/mandates/northena.md#stanza-007

    Refusal reason: Mandate gate 007 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>return refuse(draft, 'missing_lawful_basis')</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_008(payload):
    """Source anchor: docs/mandates/northena.md#stanza-008

    Refusal reason: Mandate gate 008 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>return refuse(draft, 'incomplete_artifact')</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_009(payload):
    """Source anchor: docs/mandates/northena.md#stanza-009

    Refusal reason: Mandate gate 009 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>return refuse(draft, 'scope_unresolved')</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_010(payload):
    """Source anchor: docs/mandates/northena.md#stanza-010

    Refusal reason: Mandate gate 010 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>return refuse(draft, 'floor_malformed')</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_011(payload):
    """Source anchor: docs/mandates/northena.md#stanza-011

    Refusal reason: Mandate gate 011 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>ledger.write(stage='gate', decision='refused',
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_012(payload):
    """Source anchor: docs/mandates/northena.md#stanza-012

    Refusal reason: Mandate gate 012 from docs/mandates/northena.md · northena
    Condition (verbatim from source): within the retention window. Out-of-scope and refused items are
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_013(payload):
    """Source anchor: docs/mandates/northena.md#stanza-013

    Refusal reason: Mandate gate 013 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>if frozen.refused: return frozen</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_014(payload):
    """Source anchor: docs/mandates/northena.md#stanza-014

    Refusal reason: Mandate gate 014 from docs/mandates/northena.md · northena
    Condition (verbatim from source): <p>execute(routing) # Layer D / warm-serve / refuse</p>
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_015(payload):
    """Source anchor: docs/mandates/northena.md#stanza-015

    Refusal reason: Mandate gate 015 from docs/mandates/northena.md · northena
    Condition (verbatim from source): | Gate → execution     | out           | warm-serve (Intelligence tier) \| fresh (Layer D) \| refuse.                                                                               |
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_016(payload):
    """Source anchor: docs/mandates/northena.md#stanza-016

    Refusal reason: Mandate gate 016 from docs/mandates/northena.md · northena
    Condition (verbatim from source): | test_no_run_without_lawful_basis  | An intent with no lawful_basis is refused at Admit; no downstream stage runs.               |
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_017(payload):
    """Source anchor: docs/mandates/northena.md#stanza-017

    Refusal reason: Mandate gate 017 from docs/mandates/northena.md · northena
    Condition (verbatim from source): must respect them.
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_018(payload):
    """Source anchor: docs/mandates/northena.md#stanza-018

    Refusal reason: Mandate gate 018 from docs/mandates/northena.md · northena
    Condition (verbatim from source): -   **Refusals are recorded, not hidden.** Out-of-scope and refused
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_019(payload):
    """Source anchor: docs/mandates/northena.md#stanza-019

    Refusal reason: Mandate gate 019 from docs/mandates/northena.md · northena
    Condition (verbatim from source): Binding. Any implementation that violates one is incorrect regardless of
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None


def northena_gate_020(payload):
    """Source anchor: docs/mandates/northena.md#stanza-020

    Refusal reason: Mandate gate 020 from docs/mandates/northena.md · northena
    Condition (verbatim from source): 8.  Out-of-scope and refused items are recorded in the Ledger, never
    """
    # Gate condition placeholder · executable via import-and-invoke smoke cell (B-3).
    # Concrete condition body evolves per engine-version bump (Class E discipline).
    return None
